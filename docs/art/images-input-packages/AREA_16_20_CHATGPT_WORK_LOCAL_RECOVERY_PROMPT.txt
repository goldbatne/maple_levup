[ChatGPT Work Local 검증·재실행 지시 — AREA 16~20]

프로젝트 루트는 `D:\maplestory_levup`이다. 기존 `20260914_005624` 실행과 ZIP을 수정·삭제·덮어쓰지 않는다. 새 batch_run_id를 사용한다.

먼저 다음 공통 규칙을 전부 읽고 그대로 실행한다.

`D:\maplestory_levup\docs\art\images-input-packages\CHATGPT_WORK_LOCAL_ART_RULES_V2.md`

기존 상태를 PASS로 그대로 상속하지 않는다. AREA 16은 앵커만 통과한 미완료 상태다. AREA 17~20은 OUTPUT ZIP이 있으나 크로마/검정 매트 기반 알파 복구를 사용하면서 구버전 규칙에는 그 방식을 금지한 모순이 있다.

먼저 AREA 17~20의 실제 source, 변환 스크립트, 최종 PNG를 V2의 `SOLID_MATTE_RECOVERY` 기록·알파·오염 게이트로 독립 재검증한다. 다음 중 하나라도 확인되지 않으면 해당 스킬은 재생성한다.

- 사용한 단색 매트와 변환 방식이 추적 가능함
- 몬스터/공식 VFX/AREA00 참조 경로와 해시가 존재함
- 최종 RGBA가 흰색·검정색·회색 합성에서 키 색 잔류나 검은 테두리가 없음
- 크로마 제거로 의도 색이나 어두운 외곽이 훼손되지 않음
- 프레임 피봇·크기·리듬과 ICON 연결이 INPUT에 맞음

검증 또는 재생성은 AREA 16→17→18→19→20 순서로 한 Area씩 진행한다. 기존 후보를 positive reference로 사용하지 않는다. 해당 Area 전체가 PASS일 때만 새 실행 폴더에 새 OUTPUT ZIP을 만든다. AREA 20까지 처리한 뒤 멈춘다.

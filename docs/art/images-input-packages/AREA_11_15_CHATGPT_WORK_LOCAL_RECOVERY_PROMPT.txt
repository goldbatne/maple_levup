[ChatGPT Work Local 재실행 지시 — AREA 11~15]

프로젝트 루트는 `D:\maplestory_levup`이다. 기존 `20260914_005352` 실행과 INVALID/AUDIT 자료는 수정·삭제·덮어쓰지 않는다. 새 batch_run_id를 사용한다.

먼저 다음 공통 규칙을 전부 읽고 그대로 실행한다.

`D:\maplestory_levup\docs\art\images-input-packages\CHATGPT_WORK_LOCAL_ART_RULES_V2.md`

그다음 AREA 11, 12, 13, 14, 15의 기존 INPUT ZIP과 각 `AREA_XX_CHATGPT_PRODUCTION_PROMPT.txt`를 읽는다. INPUT 준비는 병렬로 해도 되지만 이미지 생성 호출은 AREA 11→12→13→14→15 순서로 한 작업선씩 수행한다.

기존 다섯 Area는 모두 직접 알파 게이트에서 멈췄다. 같은 직접 투명 요청을 Area마다 반복하지 않는다. V2의 배치 공통 알파 사전 검사로 `DIRECT_RGBA` 또는 `SOLID_MATTE_RECOVERY`를 한 번 확정하고 전체 구간에 동일하게 적용한다.

각 Area의 첫 필수 VFX 앵커가 아트·알파·오염 게이트를 모두 통과해야 나머지 스킬을 만든다. RGB 체커보드와 기존 실패본은 참조하거나 납품하지 않는다. 모든 필수 스킬이 통과한 Area만 정상 OUTPUT ZIP을 만든다. AREA 15까지 처리한 뒤 멈춘다.

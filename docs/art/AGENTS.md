# Art INPUT production baseline

- User decision (2026-09-13): use `docs/art/images-input-packages/` and its existing files as the authoritative baseline for future INPUT creation.
- Do not automatically select or recreate version-suffixed directories such as `images-input-packages-v2` or `images-input-packages-v2_4`.
- Historical audit and failed production records may mention removed versioned folders; those references are provenance, not current production instructions.
- Preserve the existing baseline packages. Future changes should follow the user's requested scope and preserve skill identity and approved monster references.
- BATCH_01 generation and the AREA 01 refinement task remain stopped unless explicitly resumed by the user.

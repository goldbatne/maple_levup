# 로그라이트 복구 위험 목록 및 후속 작업 순서

작성일: 2026-09-21 KST. 대상: `D:/maplestory_levup`의 현재 미커밋 작업 트리.
브랜치: `codex/art-packages-cleanup-20260915`; HEAD: `2eb7498eda8f53440648ef6107d6c933ba55d5c6`.
HEAD만의 분석이 아니다. 대량의 미커밋 코드·맵·UI·데이터 변경과 미추적 파일이 존재한다.
이번 작업은 읽기 전용 점검과 이 보고서 작성이며 게임 코드·데이터·UI·계정 저장은 변경하지 않았다. Play/Refresh/Save도 이번 점검에서는 실행하지 않았다.

## 현재 확인 수준

- Maker MCP: edit 모드, map01. 읽은 Build Console은 07:20:16 기록이며 Error 0 / Warning 2 / Info 1. 이번에 새 빌드를 실행한 결과가 아니다.
- 직전 점검에서 Save 요청 후 오류 팝업 미재현과 데이터 파일 위치 유지를 확인했다. 저장 문제의 영구 해결이나 재시작 내구성까지 입증한 것은 아니다.
- `Mislocated`에는 `.directory` 4개가 남아 있다. 데이터 파일이 다시 이동한 상태는 아니며, 폴더 메타데이터 자체가 오류 원인인지는 미확인이다.
- 과거 `MERGE_RUNTIME_REPORT.md`의 Solo/Seed/슬롯 통과 기록은 당시 기록이다. 이후 테이블 복구가 있었으므로 현재 트리 전체의 통과 인증으로 재사용하지 않는다.
- 신규 로그라이트 플래그 1, 이전 Phase 1/3 플래그도 1. 다중 플래그 자체는 버그가 아니나 신규 모드 우선 분기 누락을 검사해야 한다.

## 우선순위별 위험 및 종료 조건

| ID | 우선순위/상태 | 근거와 위험 | 후속 작업·종료 조건 |
|---|---|---|---|
| R01 | 최우선 / 코드 확인, 손실 여부 미확인 | `Save/PlayerDBManager.mlua:33,179,690,694`는 기존 Perm/Run 키를 읽고 쓴다. `SavePermanentData.mlua:153`의 RoguelitePermanent는 Perm JSON 안의 분리된 객체이며 독립 DataStorage 키는 아니다. 새 기록 저장이 레거시 필드 재직렬화와 결합된다. | 기존 저장을 임의 초기화하지 않는다. 격리 저장으로 구 데이터→로드→신규 기록→저장→재로드를 비교하고, 신규 Run 일시 상태가 레거시 Run에 섞이지 않는지 검증한다. 같은 키라는 이유만으로 이미 손실됐다고 판정하지 않는다. |
| R02 | 최우선 / 코드 확인 | `PlayerDBManager.mlua:443-467`은 ItemTable에서 찾지 못한 아이템/장착을 메모리에 복원하지 않는다. `:855-869`는 현재 테이블/메모리 기준으로 다시 저장한다. 테이블 누락 상태에서 저장하면 보유 기록이 빠질 위험이 있다. 이전 누락 세션의 실제 영구 손실은 이번에 조사하지 않았다. | 테이블 로드·검증 실패 시 계정 적용/저장이 안전하게 중단되는지 우선 확인·보완. 과거 계정 값을 추측해서 복구하지 않는다. |
| R03 | 최우선 / 잔존 확인, 원인 미확인 | Mislocated/MyDesk.directory, GameData.directory, Models.directory, Models/Monsters.directory 잔존. 이전 복구 중 Maker 메모리와 디스크 배치가 달라 파일이 재이동한 이력이 있다. | 식별자·부모 관계·실제 참조를 조사하고 Maker 등록 상태와 대조. 임의 삭제/ID 재발급 금지. Save/Refresh/재시작 후 경로·해시 안정성 확인. |
| R04 | 높음 / 데이터 값 확인, 런타임 미확인 | 복원 데이터와 현재 코드의 조합 재검증 필요. MonsterTable 해시는 감사 기준과 일치. SkillTable 해시는 다르지만 기준 사본과 비교한 112행의 필드값은 일치한다. | wrapper의 테이블 이름/등록과 모든 필수 열을 확인. 격리 Play에서 6개 핵심 테이블 로드, nil/미존재 아이템/모델 오류 재발 없음 확인. 바이트 차이만으로 스킬 변경으로 분류하지 않는다. |
| R05 | 높음 / 코드 확인 | `Progress/PlayerSkillSlots.mlua:556-559`: 후보가 있으면 새 후보는 로그만 남기고 false 반환. 로그의 '보류'와 달리 이후 제공할 저장 큐는 없다. | 연속 처치 시 후보 유지/정리 정책을 명확히 구현·표시하고 조용한 획득 기회 소실을 방지. 선택 요청 재전송이 다음 후보까지 처리하지 않는지 검증. 스킬 효과 변경 금지. |
| R06 | 높음 / 코드 확인, 현재 조우에서 재현 미확인 | `GameData.mlua:1679-1683`: 최종방 여부만 검사하고 전달된 monsterId를 검사하지 않아 그 방에서 어떤 몬스터든 사망하면 목표 완료가 된다. | 지정 목표 개체와 비목표 개체를 구분한 완료 판정 검증. 현재 최종방에 단일 목표만 있으면 바로 드러나지 않을 수 있다. |
| R07 | 높음 / 사양 미완료 | `GameData.mlua:55,1460` 및 개편 보고서: 현재 목표는 최종 조우 처치 중심이며 보스방을 우선 최종방으로 택한다. '모든 Area 의무 보스 금지' 최종 사양을 충족한 다양한 목표 구조로 볼 수 없다. | 이미 승인된 도달/지정 강적 등 소수 목표 중 구현 가능한 것을 Area 설정과 연결. 목표 없는 미완성 Area를 정상 완료 콘텐츠로 노출하지 않음. |
| R08 | UI 우선 / 코드 확인 | `UI/PlayerHud.mlua:98-108`: RogueliteRunActive일 때만 레벨/EXP를 숨기며 로비·종료 후에는 다시 활성화한다. 신규 모드와 현재 Run 진행 여부가 혼용된다. | 신규 모드 기준으로 표시 정책 통일. 로비→Run→성공/실패→로비에서 불필요한 RPG UI가 다시 켜지지 않는지 확인. |
| R09 | UI 우선 / 과거 보고 + 현재 실행 미확인 | 과거 보고서에 상단 스탯/스킬/가방 시각 잔존. `AreaSelectPanel`은 같은 Area 첫 클릭 준비, 두 번째 클릭 시작이며 참가자 명단/준비 전용 UI 미완료. Mask/중앙 패널/입력차단 잔존은 아직 확정하지 않음. | HP/고정5슬롯/쿨다운/목표/참가자 중심으로 정리. 발견 컬렉션은 유지. 모든 창의 열기·닫기·ESC·맵 이동 후 배경/Mask/입력 정상 확인. |
| R10 | 높음 / 검증 공백 | 완료/실패 경로(`GameData.mlua:1688-1770`)가 슬롯·후보를 직접 비우는 반면 `EndRogueliteRun`도 별도로 존재한다. 타이머·쿨다운·HP·상태효과·후보 정리 누락 가능성은 아직 미확인. | 정상 완료/사망/이탈 뒤 다음 Run을 연속 실행하여 공통 시작값과 빈 슬롯 확인. Run 내부 이동·부활은 쿨다운 유지, 새 Run은 초기화라는 두 계약을 각각 검사. |
| R11 | 높음 / NOT_RUN | 투사체 벽 충돌·돌진 이탈·벽 너머 범위·포탈 끼임·VFX 판정 일치 전수 미검증. 생성 그래프 도달성과 실제 충돌 통과 가능성은 다르다. | 유형별 대표 스킬부터 새 지형에서 검사. 의도적 관통과 버그를 구분하고 계수/범위/VFX를 임의 변경하지 않음. |
| R12 | 높음 / NOT_RUN | 실제 2~4인 협동, 개별 후보/쿨다운, HP 배율, 집결·관전·복귀·90초 유예·재접속 검증 공백. 1인 서버 함수 호출은 멀티 검증이 아님. | 실제 독립 클라이언트 수별 실행. 도구가 지원하지 못하는 인원은 NOT_RUN으로 유지하며 지원 완료로 표시하지 않음. |
| R13 | 보통 / 현재 경고 확인 | Build: mano/MovementComponent.InputSpeed, bowmaster/MonsterAttack.AvatarAttackPlayRate 경고. OpenRoomInfo의 GetTamedCount 관련 Info. | 모델 속성과 메서드 메타데이터 연결 확인. 현재 경고를 컴파일 오류나 실제 오동작으로 단정하지 않음. |
| R14 | 보통 / 문서 불일치 | Archive/As-built.md는 초기 랜덤 모드 설명, 복사된 구현 보고서는 원본을 수정하지 말라는 과거 지시와 21/16 정적 검사 수가 혼재. 기준본 백업 위치도 과거 premerge 디렉터리와 현재 ZIP 기록이 다름. | 현재 원본 실행 프로젝트·최신 사양·증빙 위치를 후속 상태 문서에서 명확히 연결. 역사 문서를 현행 실행 지침으로 오인하지 않도록 표시. baseline 원문/스냅샷은 수정하지 않음. |

## 데이터 비교 근거

- 기준 SkillTable: 보존된 roguelite-rework/Mislocated/MyDesk/GameData/SkillTable.csv, SHA-256 `3A84B837EDC8615E423DCC1DA6A89A0237A36D3C481F310338FBE016A211023B`.
- 현재 SkillTable: RootDesk/MyDesk/GameData/SkillTable.csv, SHA-256 `3A4AF00F0CC9EF3439E9C4CBDA4E80F0CFE86CD4072202AD5B08A994A5828C36`.
- CSV 파싱 결과 양쪽 112행, 현재 행의 id와 각 필드값 비교에서 차이 없음. 인코딩/줄바꿈 차이가 정확한 원인인지는 별도 확인하지 않았다.
- 현재 MonsterTable SHA-256 `6783014B6990F67988B4D17D5AE3B0B741896433FCEFE3849449B7E06073B5E2`, 기존 감사 기록과 일치.

## 작업 순서

1. **안전 기반**: R01~R04. 저장 보호, 테이블 유효성, 엔트리 등록 안정성. 이 단계 전 실제 계정 Play를 반복하지 않는다.
2. **최소 기동 확인**: 격리 저장으로 Refresh/새 Build/Play, 현재 원본의 로비·Run 진입과 로그 확인. Build PASS와 Runtime PASS 분리.
3. **UI 정리**: R08~R09. 로비·전투·결과 화면의 모드 분기, 불필요한 버튼/문구 숨김, 5슬롯·쿨다운·교체/포기·목표·준비 상태, Mask/입력 회귀검사. 새 미술이나 새 대형 시스템은 추가하지 않는다.
4. **솔로 게임 흐름 보완**: R05~R07/R10. 연속 획득, 목표 개체, 종료/실패/다음 Run. 코드가 있는 것과 실제 플레이 완료를 구분한다.
5. **생성과 스킬 지형 호환**: Seed 재현·실제 필수 경로·유효 스폰·대표 타입 판정. 이후 미검증 Area로 확대.
6. **협동 검증과 보완**: R12. 가능한 실제 인원부터 개인 상태와 공유 진행/재접속 확인.
7. **마무리**: 남은 경고/문서 정합성과 회귀검증. 재현 로그·변경 파일·FAIL·NOT_RUN을 남긴다.

각 단계의 실패를 다음 UI/콘텐츠 작업으로 덮지 않는다. 사용자에게 몬스터별/파일별 재승인을 요구하지 않고 승인된 범위 내에서 순서대로 진행하되, 신규 기획 또는 추가 권한이 필요한 결정만 별도로 확인한다.

# 지식 체계 감사 보고서 — 2026-09-07

## 기준

- OpenAI `gpt-5.6-sol`: 1,050,000 context window, 128,000 max output, reasoning effort `none`~`max`, Responses API에서 Skills·MCP·apply_patch·computer use 등 지원
- MSW AI ToolKit: LocalWorkspace와 ExtendedScriptFormat 필요, MCP 서버와 AI Skill Set을 각각 연결
- MSW MCP: 서버형 HTTP/API Key 방식과 로컬 Maker stdio/무인증 방식 분리
- MSW AI Skill Set: 공식 설치물을 프로젝트 Git 이력에서 제외

공식 출처:

- https://developers.openai.com/api/docs/models/gpt-5.6-sol
- https://maplestoryworlds-creators.nexon.com/ko/docs?postId=1374
- https://maplestoryworlds-creators.nexon.com/ko/docs?postId=1368
- https://maplestoryworlds-creators.nexon.com/ko/docs?postId=1373

## 발견한 문제

- `CLAUDE.md`, 프로젝트 현황, 인수인계, 작업지시서가 각각 전체 작업 역사를 반복해 기본 조회량이 과도했다.
- 같은 활성 문서 안에서도 최신 표와 과거 공식이 충돌했다. 대표적으로 EXP `60/1.105`와 `20/1.12`가 공존했다.
- 활성 보고서에 몬스터 28종, 보스방 6개, 장비 셀 28개처럼 이후 작업에서 바뀐 수치가 남았다.
- 보스 HP ×3, 전직 패시브 +1%, area_06 지하 하수도처럼 대체된 설계가 현재 지시와 분리되지 않았다.
- `.agents/`, `.codex/` 공식 도구 설치물이 Git 미추적 파일로 계속 노출됐다.
- 정적 검사와 Maker 런타임 검사의 증명 범위가 문서에서 명확히 분리되지 않았다.

## 적용한 개편

- `docs/README.md`를 단일 지식 라우터로 만들었다.
- 현재 사실, 설계, 밸런스, 지역, 결정, 인수인계, 검증의 책임을 각각 한 파일로 분리했다.
- 7천 줄이 넘는 과거 문서는 `docs/archive/legacy/`와 `docs/archive/reports/`로 이동해 삭제 없이 보존했다.
- 기존 링크 호환이 필요한 파일명에는 짧은 현행 정본 또는 안내 파일을 다시 만들었다.
- 루트 `CLAUDE.md`는 관리형 `AGENTS.md` import와 문서 라우팅만 남겼다.
- `.gitignore`에 `.agents/`, `.codex/`를 추가했다.
- `verify-knowledge-base.cjs`로 필수 문서, 로컬 링크, 장문 중복, 노후 수치, CSV 정본, 역사 보존과 ToolKit ignore를 검사한다.

## 검증 결과

- 지식 체계 검증: PASS
- 콘텐츠 커버리지 검증: PASS
- 포획 대상 30종, 몬스터 스킬 30종, 아이템 33종, 보스방 8개 확인
- 보스 HP ×10, DEF ×1, ATK ×1.5, 제한시간 300초 확인
- 히어로 STR +10, 보우마스터 DEX +10, 각 5개 상한 확인
- 활성 문서는 약 800줄, 역사 보관 문서는 7,200줄 이상으로 분리해 기본 조회 경로를 크게 축소
- `area_00` 구조 검증 통과: 8맵, 각 448타일, 상호 연결, RectTileMode 1
- Maker 긍정 로그에서 Lv1 필요 EXP 60, EXP 공비 1.105, 포획 상한 5를 확인

## 남은 위험

`area_00` 8맵은 SectorConfig에 아직 없다. 이는 이번 문서 개편 문제가 아니라 동시에 진행 중인 T58의 Maker Refresh/저장 미완료 상태다. 따라서 새 지역의 런타임 완료 표시는 보류했다.

Maker Play 중 `GameDataVerify`가 `r_job_02 south → r_29`의 `north` 역연결 누락을 오류로 보고했다. Build Console에는 마노 `InputSpeed`, 보우마스터 `AvatarAttackPlayRate` 경고 2건도 남아 있다. 기존 구현 의도를 임의로 바꾸지 않고 현재 인수인계에 후속 확인 항목으로 기록했다.

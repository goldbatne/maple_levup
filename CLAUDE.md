<!-- >>> managed by mswai >>> -->
@AGENTS.md
<!-- <<< managed by mswai <<< -->

# 프로젝트 규칙

메이플스토리 월드 게임 개발 (mLua). 1인 개발 + AI 협업.

## 게임 한 줄 정의
**성장할수록 세계가 열리는, 방(房) 단위 탑다운 탐험 성장 RPG.**
- 코어(왜 하는가) = 세계를 한 방씩 헤쳐나가는 맛 (영웅서기식)
- 엔진(무엇으로 굴리나) = 숫자가 오르는 성장의 손맛 (Re:END식)
- 판단 기준: "이건 코어를 강화하나, 엔진을 강화하나?" 둘 다 아니면 넣지 않는다.

## 참조 문서
- `README.md` — 저장소 진입점. 읽는 순서와 코드 지도.
- `docs/프로젝트_현황.md` — **한 파일 스냅샷.** 완료된 것 / 코드는 있는데 배치 안 된 것 /
  초안 수치 / 함정 목록 / 다음에 할 일. 상세는 다른 문서로 넘기고 현재 상태만 담는다.
  → **태스크를 완료할 때마다 이 문서도 갱신할 것.**
- `docs/인수인계_현재상태.md` — **무엇이 실제로 돌아가고 무엇이 문서에만 있는지.**
  기획서를 곧이곧대로 믿기 전에 읽을 것. 문서-구현 차이 **9건**(문서에 있으나 구현 안 된 것
  7건 + 문서 근거 없이 구현된 것 2건)이 여기 있다.
  → **태스크를 완료할 때마다 이 문서도 갱신할 것.**
- `docs/게임기획서_v0.3.md` — 시스템 설계 전체
- `docs/작업지시서_v2.0.md` — 태스크 순서(T0~T10)와 프롬프트
  - T10은 원래 계획에 없었다. T9 실측에서 "스탯이 판마다 의미가 없다"가 드러나 파생됐다.
- `docs/밸런스_확정수치.md` — **수치 읽기용 정본.** 레벨 곡선·몬스터 계단·방별 위험도.
  `GameBalance.csv`에서 생성했다. 셋이 어긋나면 CSV가 옳다.
- `docs/밸런스시트_v0.2.xlsx` — 설계·시뮬레이션용 작업 시트
  - ⚠ 수식 777개의 **캐시가 비어 있어 Excel 밖에서는 값을 읽을 수 없다.** 그래서 위의
    `밸런스_확정수치.md`를 읽기용 정본으로 둔다.
  - v0.1은 남겨 둔다. v0.2가 무엇을 왜 고쳤는지는 작업지시서 T9-4 절에 있다.
- `docs/프로젝트_재정리_개발운영_가이드_v1.0.md` — 히스토리. 현행 기준 아님.

## 코드 작성 시 항상 지킬 것
1. **실행공간을 상단 주석에 명시** (`-- @서버` / `-- @클라이언트`)
2. **반드시 서버에서 실행**: 데미지 계산, 스탯 분배, 경험치/레벨, 포획 판정,
   환생 처리, 데이터 저장. 클라이언트는 입력과 연출만.
3. **수치 하드코딩 금지** — 데이터 테이블 또는 상수 모듈에서 읽을 것.
4. **존재하지 않는 API 추측 금지** — 불확실하면 "확인 필요"로 표시하고 대안 제시.
5. **데이터 분리**: 영구(스킬 컬렉션·랜드마크·환생 횟수·개방 구역)와
   회차(레벨·경험치·스탯 분배·현재 직업)를 저장 구조에서 분리.
6. 코드 제공 후, 내가 직접 확인해야 할 것을 3줄 이내로 알려줄 것.

## 핵심 수치 (요약 — 정본은 `docs/밸런스_확정수치.md`)
> 아래는 자주 쓰는 값만 옮겨 적은 것이다. **어긋나면 `GameBalance.csv`가 옳다.**
- 데미지 = 스킬계수 × 계수스탯² / (계수스탯 + DEF), 기준 계수 1.2
  - **자동 공격의 계수 스탯 = `max(ATK, INT)`**. 스킬은 각자의 `scaling_stat`(ATK/INT/LUK)
  - 어느 스탯을 쓸지 고르는 곳은 `PlayerAttack.CalcDamage`, 산술은 `CombatFormula`
- 스탯 4종: **STR / DEX / INT / LUK**, 레벨당 5포인트 (기본값 10/10/10/**0**)
  — ⚠ **이름 체계가 둘이다**: 분배 스탯은 STR/DEX/INT/LUK이고, 그것이 만드는
  파생 수치는 ATK(물리 공격력)/INT(마법 공격력)/DEF(방어력)/LUK(포인트)다.
  `ItemTable`·`SkillTable.scaling_stat`·`RoomGate.gate_key`는 **파생 수치 쪽** 이름을 쓴다
  — 2026-08-27 재설계. 최대 HP = 1000 + STR x 20.
  공격력·방어력은 아직 구 계수(`atk_per_stat_point` = 1)로 돈다 (T27-2에서 옮긴다)
- Lv1 필요 EXP **60** (공비 **1.105**), 레벨 캡 200 → 누적 2,432억 EXP / 1,025P / 총 3,883마리
  — 2026-08-28 T34에서 20/1.12에서 바꿨다. **지역 20칸(각 10레벨)** 구조에 맞춘 값이다
- **몬스터는 선형이다** (2026-08-27 T28): HP = 144 + 34.2×(Lv-1) / DEF = 80 + 19×(Lv-1) /
  ATK = 157 + 11.5×(Lv-1). **EXP만 기하급수** 5×1.10^(Lv-1) — 필요 EXP 곡선이 기하급수라서다.
  설계 목표는 **동레벨 = 3대에 잡고 12대 맞으면 죽는다** (대표 선택)
- 보스 배율 HP ×3 / **DEF ×1** / **ATK ×1.5** / EXP ×3 — DEF와 ATK 배율은 곱해지므로 DEF는 안 건드린다
- 포획 = 3% + LUK×0.1%p, 종별 40킬 천장

## 슬롯 구조 (핵심 설계)
- ~~**직업 스킬 슬롯**: 고정. 직업 선택 시 자동 장착, 항상 사용 가능.
  몬스터 슬롯을 잠식하지 않는다. → 직업을 선택하는 이유.~~
  → **2026-08-16 폐기.** 직업 선택이 없어져 근거가 사라졌다.
  → **2026-08-31 T56 변경.** 직업 캐릭터의 스킬은 플레이어 보상에서 완전히 제외하고
  보스 자신의 전투 패턴으로만 둔다. 플레이어 슬롯에는 포획한 몬스터 스킬만 들어간다.
- **스킬 슬롯**: **차수 + 1칸, 상한 5.** 0차 1칸 → 1차 2칸 → … → 4차 5칸.
  ~~확장 2축(보스 클리어 + 환생)~~ → **2026-08-20 폐기. 늘어나는 축은 차수 하나뿐.**
  포획한 몬스터 스킬만 이 칸을 놓고 경쟁한다.
- **차수 = 전직 레벨.** 시작 0차 / **Lv20·30·60·100·200 → 1~5차** (2026-08-21).
  환생 횟수가 아니다 — 환생이 무조건 가능해지면서 근거가 될 수 없어졌다.
- 슬롯 수는 영구 데이터. 누적하지 않고 차수에서 **매번 다시 계산**한다
  (`RecomputeSlotCount`) — 누적하면 로드가 두 번 돌 때 계속 늘어난다.

## 문서 갱신 규칙
개발 중 얻은 사실은 문서에 반영할 것:
- 확인된 API/제약, 실측 수치, 완료된 태스크 체크 → `docs/` 문서에 바로 갱신
- 새로 알게 된 사항은 "미확인" 항목에서 제거하고 확정 내용으로 이동
- 문서를 갱신했으면 커밋 메시지에 남길 것
  (예: `[T3] 데미지 공식 구현 + 기획서 실측 반영`)
- 태스크를 완료할 때마다 **"현재 진행 상태"** 섹션과
  **`docs/인수인계_현재상태.md`**를 갱신할 것
  (완료/진행 중/다음, 체크리스트, 마지막 갱신 날짜)
- 문서와 구현이 어긋나게 되면 **`인수인계_현재상태.md` 3절**에 한 줄 추가할 것.
  "슬라이스 범위 밖이라 안 만들었다"도 반드시 적는다 — 안 적으면 다음 사람이
  기획서를 읽고 있다고 착각한다.
- `GameBalance.csv`의 수치를 바꿨으면 **`docs/밸런스_확정수치.md`를 다시 생성하고,
  위 "핵심 수치" 요약도 같이 고칠 것.** 이 세 곳이 같은 숫자를 들고 있으므로 함께 움직여야 한다
  (요약은 손으로 관리하는 사본이라 가장 먼저 낡는다)
- 설계 변경을 승인받았다면 **"설계 변경 이력"**에 날짜·변경 내용·사유를 한 줄로 기록
- 이 문서들은 나중에 다른 AI나 새 프로젝트로 이관될 수 있다.
  처음 읽는 사람이 "지금 어디까지 왔는지"를 바로 알 수 있어야 한다.

단, **게임 설계(슬롯 구조, 게이트 3종, 코어 루프, 성장 방식, 환생)는 임의로 변경하지 말 것.**
구현이 어렵거나 더 나은 방법이 보이면:
1. 문서를 고치지 말고
2. 무엇이 문제인지, 대안이 무엇인지 설명하고
3. 내 승인을 받은 뒤에 반영할 것

"구현이 복잡해서 단순화했다"는 사후 통보는 금지.

## 작업 방식
- 작업지시서의 태스크 순서대로 진행. 한 태스크 완료 → 직접 실행 검증 → 커밋 → 다음.
- 완료 조건(✅)을 통과하지 못하면 다음 태스크로 넘어가지 않는다.
- 커밋은 "작동하는 상태"에서만. 메시지 형식: `[T3] 데미지 공식 구현 (서버)`
- 한 문제에 2시간 이상 막히면 멈추고 별도 검증 요청.

## 진행 방식 (반드시 지킬 것)
- **한 번에 한 태스크만.** 여러 태스크를 미리 앞서 만들지 말 것.
- 태스크를 완료하면 **거기서 멈추고** 보고할 것:
  1. 무엇을 만들었는지 (파일·함수 단위로 간단히)
  2. 내가 에디터에서 확인해야 할 것 (실행 절차)
  3. 완료 조건(✅) 충족 여부
- 내가 "확인했다"고 하기 전까지 다음 태스크로 넘어가지 말 것.
- 태스크 안에서도 큰 덩어리는 쪼갤 것. 200줄 넘는 코드를 한 번에 쏟아내지 말고,
  단위별로 만들고 설명할 것.
- 확실하지 않은 API나 설계 선택이 있으면 추측해서 진행하지 말고 먼저 물어볼 것.

---

## 현재 진행 상태
- **T56 히어로 보상 패시브 전환 — 로컬 구현·정적 검증 완료, Maker 실측 대기.**
  히어로 격파의 직업 스킬 5종 지급을 제거하고 `히어로의 검` 확정 드랍으로 바꿨다.
  장착 없이 인벤토리에 가진 1개당 STR +1%, 최대 5개(+5%)가 공격력·최대 HP에 자동 적용된다.
  히어로 스킬 5종은 `boss/enemy` 전용 전투 패턴으로 남아 히어로만 사용한다. 옛 저장의
  히어로 스킬은 로드·장착 대상에서 제외한다. 가방은 30칸이며 `xN/5`와 누적 효과를 표시한다.
  `verify-content-coverage.cjs`에서 데이터·보상 분리·스탯 배선·30칸 UUID를 모두 통과했다.
- **T50 area_05 노틸러스 ✅ — 지역 20칸의 5번째 (Lv41~50).** 대표 지시:
  *"이런 방식으로 200레벨 지역까지 가보는게 좋은거 같네"*. 커닝시티(T49)·엘리니아(T47)와
  **같은 형식**(오브젝트 0 · 바닥이 지역을 말한다)으로 다섯 번째 지역을 세웠다.
  실측: 방 8개 전부 스폰(9~10마리 · 보스방 1) · **깨진 연결 0개** · 몬스터 5종 `[공식 일치]` ·
  8맵 **448칸씩 통과불가 0** · 미니맵 `노틸러스 방 8개` **격자 5x3 겹침 0** ·
  `랜드마크 Lv40 — 구역 개방 [노틸러스]` · **보스 배율 정확**(HP 1820→**5460** ×3 /
  DEF **1011** ×1 / ATK **1081.5** ×1.5) · 빌드 에러 0 · 화면으로 해안가·갑판 확인
  ```
                    r_05a 노틸러스 화물칸 [stat 245]  ← 막다른 보상방
                       ↑N
   r_051 ─E→ r_052 ─E→ r_053 ─E→ r_054 [stat 215] ─E→ r_055 (보스 주니어 발록)
   (마을 W)    ↓S       (사거리)
              r_056 ─E→ r_057 ───N───┘
  ```
  - **진입 방은 포탈이 둘뿐이다** (T48 교훈을 처음부터 지켰다) — 마을(W)과 본선(E).
    갈림은 두 번째 방부터고, `r_053`이 사거리(N/E/S/W)다. 격자가 닫혀 순환이 지도에 다 보인다.
  - **`stat 215`는 공식값이다** — 설계 §9의 `5 × (10k+3)`, 노틸러스는 k=4라 `5 × 43`.
    해금 `Lv40`도 공식(`Lv 10k`)이다. 진입 방은 `boss r_045`(커닝시티 보스 레이스).
  - ⚠ **`key` 게이트를 못 썼다.** 막다른 보상방은 `key`(앞 지역 보스 스킬)로 잠그는 것이
    규격인데(커닝시티·엘리니아), **커닝시티 포획 스킬 5종이 아직 없다**(`drop_skill_id` 공란).
    그래서 `stat 245`를 두 번째 게이트로 썼다 — **스킬이 생기면 한 칸으로 바꾼다.**
  - **전용 타일 4종을 그렸다** (T46 생성기 방식): `Nautilus_sand_1/2`(마른 모래 / 물결 자국) +
    `Nautilus_deck`(가로 널빤지 — 배 갑판) + `Nautilus_water`(바닷물).
    이제 세 지역이 같은 형식이다 — 엘리니아 이끼 · 커닝시티 콘크리트+선로 · **노틸러스 모래+갑판**.
  - ⚠⚠ **경계는 가로 직선으로 간다.** 물가를 구불거리게 냈더니 미리보기에서 **계단처럼 각졌다** —
    부드럽게 이으려면 룰타일 경계 16종이 필요한데(T46) 그건 업로드가 48번이다.
    그리고 레퍼런스(리엔드·인플레이션 RPG)의 바닥은 원래 단순하다. **직선이 맞다** —
    계단이 사라지고 방마다 물가 높이만 다르면 결이 갈린다.
  - ⚠ **자국은 2x2 덩어리다.** 4x2로 했더니 **400px짜리 가로 띠**가 되어 방 전체가
    줄무늬로 보였다 (T26-4의 *"한 칸씩 무작위면 잡티, 뭉쳐야 지형"*의 반대편 함정 —
    **너무 뭉치면 띠가 된다**). 얼룩은 작아야 얼룩이다.
  - ⚠⚠ **카메라가 보는 것은 y −2 ~ 4다** (T45 재확인). 처음에 선착장 물가를 y=5,
    모래톱 아래 물을 y=−3에 뒀더니 **둘 다 화면 밖**이었다. 미리보기를 카메라 창(13x7칸)으로
    잘라 보고서야 잡았다 — **방 전체(28x16)를 축소해 보면 판단이 틀린다.**
  - **방마다 결을 갈랐다**: 리본돼지 해안가는 가장 깨끗한 마른 모래 / 파란리본 해안가는 자국이
    흩어져 있고 / 선착장은 **부두 널빤지가 물까지 뻗는다** / 높은 파도 바다는 **널빤지 통로
    하나가 바다를 건넌다**(물 308칸) / 갑판은 위아래로 바다 / 얕은 여울은 젖은 모래가 가장 넓고 /
    조개 모래톱은 **위아래가 다 물**이다.
  - ⚠⚠⚠ **`.map`을 통째로 재직렬화하지 않았다** (T43-1 교훈). `"tileIndex": N`의 숫자만
    순서대로 치환했고 **줄 수 변화 0**을 검사에 넣었다. 그 검사가 실제로 버그를 잡았다 —
    `coast()`가 함수를 돌려주는데 세 방에서 호출을 빼먹어 **함수 소스가 통째로 파일에
    들어갈 뻔했다**(줄 수가 바뀐다). **검사를 안 넣었으면 조용히 깨졌다.**
  - ⚠⚠ **`RectTileMapComponent`는 맵 본체가 아니라 `RectTileMap` 자식에 있다.**
    `map:GetComponent(...)`로 재다가 **타일 0칸**이 나와 타일셋이 깨진 줄 알았다 —
    기존 맵(map21)으로 대조하니 거기서도 0이라 **측정 코드가 틀린 것**이었다.
    `GetFirstChildComponentByTypeName("RectTileMapComponent", true)`를 쓸 것.
    **의심스러우면 이미 도는 맵을 대조군으로 같이 잰다.**
  - ⚠ **`RoomTable.csv`의 8줄이 한 번 통째로 사라졌다.** 원인은 line-ending만 바뀐 diff를
    `git checkout --`으로 되돌리면서 같이 날아간 것으로 보인다.
    **Maker가 재저장한 CSV를 되돌릴 때는 내가 방금 넣은 줄이 그 안에 있는지 먼저 볼 것.**
  - **몬스터는 원작 노틸러스 계열이다**: 리본 돼지 41 / 파란 리본돼지 43 / 불가사리 45 /
    쿨 젤리피쉬 48 / **주니어 발록 50(보스)**. 팩 id 대역을 다시 확인했다(T37-1b 교훈) —
    `9xxxxxx` 이벤트 · `24~26xxxxx` 그란디스가 아니다.
  - **클립은 `rel_path`로 골랐다** (T41에서 찾은 방법). **주니어 발록만 전용 공격 클립이
    있다**(attack1/2/3) — 나머지 넷은 원작에도 없어 `stand`(리본 돼지는 `jump`)를 물렸다.
    ⚠ 발록 팩의 `attack1/info/effect` 같은 원소는 **몸 동작이 아니다**(투사체·타격 이펙트) —
    세지 말 것.
  - **히트박스는 stand 클립 크기에서 유도했다**: 리본 돼지 0.69x0.49 / 파란 리본돼지 0.69x0.55 /
    불가사리 0.40x0.37 / 쿨 젤리피쉬 0.52x0.68 / **주니어 발록 1.60x1.56**(지금까지 가장 크다).
  - **포탈은 새로 안 놓았다** (T26-1). 필요한 조합을 가진 맵을 복제했다 —
    `map01`(E·W) → r_051·r_054 / `map14`(E·S·W) → r_052 / `map13`(사거리) → r_053 /
    `map1a`(S) → r_05a / `map05`(W·Return) → r_055 / `map16` → r_056 / `map17` → r_057.
  - **남은 것**: 노틸러스 포획 스킬 5종(`drop_skill_id` 다섯 칸이 비어 있다 — 지금
    **포획이 안 되는 지역**이다) · 커닝시티 포획 스킬 5종 · area_06~20 (15개 지역).
- **T40-1 랜드마크 소급 지급 ✅ (버그 수정)** — 대표 신고: *"페리온에서 엘리니아로 안 이동하는데?"*
  **맞았다. 지역이 안 열려 있어 선택 창에 안 떴다** (실측: Lv37인데 해금된 지역이 `area_02` 하나).
  실측 수정 후: `해금된 지역: area_02 area_03` · `지역 선택 창에 뜰 것: 페리온 엘리니아`
  - ⚠⚠⚠ **원인은 두 겹이다.**
    ① `GrantLandmarks`를 부르는 곳이 **`Rebirth.Execute` 하나뿐**이었다 —
       즉 지역이 "레벨에 도달하면"이 아니라 **"그 레벨에서 환생해야"** 열렸다.
    ② 게다가 **정확히 그 레벨의 줄만** 본다. 표에 걸린 레벨이 10·20·30뿐이라
       **Lv37에서 환생하면 아무것도 안 준다.**
  - **왜 여태 안 드러났나**: 레벨 캡이 30이고 **환생이 캡에서만 가능하던 시절**에는
    언제나 Lv30에서 환생했으므로 `GetLandmarksAtLevel(30)`이 곧 전부였다.
    **2026-08-21에 환생 조건이 없어지고 캡이 200이 되면서 그 전제가 깨졌는데** 아무도 안 봤다.
  - 고친 방법: **`GrantLandmarksUpTo(level)`** — 지금 레벨 **이하**의 안 받은 것을 전부 준다.
    원장(`LandmarkClaimed`)이 레벨별이라 여러 번 불러도 안전하다. 부르는 곳을 셋으로 늘렸다:
    **레벨업**(`PlayerStats.RefreshTierRewards`) · **로드**(`PlayerDBManager.Load`) · 환생.
  - ⚠ **로드에서 부르는 것이 핵심이다.** 그게 없으면 **기존 세이브가 영영 안 고쳐진다** —
    이미 Lv10·Lv20을 지나온 사람은 다시 그 레벨이 될 일이 없다.
    반드시 `ApplyRun` 뒤여야 한다 (레벨 복원 전이면 Lv1로 읽혀 아무것도 안 준다).
  - ⚠ **표를 나중에 고치면 같은 일이 또 난다.** 2026-08-29에 엘리니아를 Lv20 → Lv10으로
    당긴 것이 정확히 그 경우였다. 소급 지급이 이제 그것도 덮는다.
- **T40-2 커닝시티 구조 재설계 ✅** — 대표 지적: *"맵 다양성이 없잖아 일자맵이고"*.
  맞았다. **일자 5방 → 갈림길 8방**으로 다시 짰다 (페리온과 같은 규격).
  실측: 8방 전부 양방향 OK · 깨진 연결 0 · 통과 불가 타일 0칸 · 미니맵에 갈림길이 그려진다
  ```
                        r_04a 비어있는 지하철  ← 막다른 보상방 [key 어둠의 손아귀]
                           ↑ 북
  r_041 → r_042 → r_043 지하철 매표소 ─동→ r_044 ─동→ r_045 (보스)
                          (사거리)   [stat 165]
                             ↓ 남                ↓ 남
                          r_046 ─동→ r_047 ─북→ r_044
  ```
  - **맵은 페리온 것을 복제했다.** 필요한 포탈 조합을 이미 갖고 있다 —
    사거리는 `map13`(E·N·S·W) · 막다른 방은 `map1a`(S) · 합류점은 `map14`(E·S·W) ·
    우회로는 `map16`(E·N)/`map17`(N·W). **포탈을 새로 놓지 않는 규칙**(T26-1)을 지키는 길이다.
  - **`gate_type=key`를 되살렸다.** 2026-08-16에 *"쓰는 방이 없어졌을 뿐"*이라고 남겨 둔
    게이트 종류다. 막다른 보상방을 **어둠의 손아귀(엘리니아 보스 스킬)**로 잠갔다 —
    앞 지역을 실제로 플레이했는지를 묻는 문이 된다. `s_mon_faust.is_key_skill=true`.
  - **몬스터 5종으로 8방을 채운다.** 스티지·주니어 레이스·셰이드를 다른 레벨로 두 번씩 쓴다
    (페리온이 돌 정령을 Lv10 → Lv36으로 다시 쓰는 것과 같은 방식).
  - 게이트 셋이 한 줄로 선다: 해금 Lv30(미등록) → 진입 `boss r_15` → 지름길 `stat 165`,
    그리고 곁길에 `key`. **일직선 성장 + 곁가지**가 같이 성립한다.
- **T40-3 커닝시티 철근 데코 ✅** — 대표 지적: *"커닝시티는 철근 스타일로 타일 맞춰줘야 하고"*.
  타일로는 회색 조약돌이 한계라(T39-2), **바닥 위에 원작 오브젝트를 얹어** 지하철을 만들었다.
  8방에 **311개** — 선로·승강장 가장자리·철골 기둥·배관·라바콘.
  실측: 8방 전부 **스프라이트 없음 0 · 콜라이더 0**(이동을 안 막는다) · 빌드 에러 0 ·
  화면으로 선로/승강장/공사장 3종 확인
  - ⚠⚠ **띠로 이어 깔아야 지하철이 된다.** 처음엔 방마다 7~9개를 흩뿌렸는데,
    방이 **28x16인데 카메라는 13x7밖에 안 보여** 대부분의 화면이 맨 조약돌이었다.
    가로로 **이어 붙는 스프라이트**를 찾아 방 전체에 깔고서야 결이 생겼다.
    | 쓴 것 | 크기 | 정체 |
    |---|---|---|
    | `2c662ec2` | 316x64 | **승강장 가장자리** (어두운 판 + 노란 안전선) — 이어진다 |
    | `56ebbf0f` | 476x96 | **침목 + 자갈 선로** — 이어진다 |
    | `79f61e0e` | 80x168 | 철골 I빔 기둥 |
    | `64c03c64` / `b2ab4ddb` / `347d7394` / `868ef7c1` | | 세로 배관 / 쇠사슬 / 라바콘 / 모래더미 |
  - ⚠ **이어 깔 때 폭 그대로 띄우면 이음매에 1px 틈**이 밝은 세로 실선으로 보인다 (실측).
    **2% 겹쳐** 깔면 사라진다 — 무늬가 반복이라 겹쳐도 안 어긋난다.
  - **방마다 결을 다르게 줬다** (여섯이 전부 같으면 "한 방을 여섯 번 본 것"이 된다):
    공사장은 선로 없이 철골+라바콘+모래더미 / 추락주의는 안전선 두 줄 /
    매표소(사거리)는 가운데를 비워 길이 보이게 / 구간들은 선로 1줄 / 보스방은 **선로 2줄**.
  - ⚠ **스프라이트에는 이름이 안 붙는다** (T19-3). 10개 검색어로 55개를 받아
    **회색 바닥색 위에 얹은 대조 시트**로 눈으로 골랐다. 바닥색을 배경으로 깔아야
    "이 바닥 위에서 보이는가"가 판단된다.
  - ⚠ **`05e516c4`(880x184)는 뺐다.** 승강장처럼 생겼지만 **파란 미래풍(에오스탑)**이라
    커닝시티가 아니다. `78448c22`(H빔 48x28)도 뺐다 — 게임 크기에서 흰 파편으로 읽힌다.
  - **order 1(띠) / 2(오브젝트).** T21-1 실측에서 6은 캐릭터 뒤였으므로 둘 다 캐릭터 아래다.
  - **`sprite()`는 SpriteRenderer만 붙인다** — 콜라이더가 없어 밟고 지나간다 (실측 0개).
- **T42 데코가 몬스터를 가리던 것 수정 ✅** — 대표 지적: *"몬스터가 움직일 때 사물 뒤에 있던데
  위에 있도록 해야지"*. 맞았다. **`MapBuilder.sprite()`가 `SortingLayer`를 안 넣는다** —
  T40-3에서 깐 데코 311개가 엔진 기본 레이어에 남아 몬스터의 `MapLayer0`보다 위로 그려졌다.
  실측: 311개 전부 `MapLayer0`로 내려갔고(띠 107 / 오브젝트 204), 런타임 데코 order 0·1 /
  몬스터 order 2 · 화면에서 셰이드가 철골 기둥 앞에 그려진다 · 빌드 에러 0
  - **`platform.md` §6이 답이다** — 그리는 순서는 3단계이고 **1순위가 `SortingLayer`**다:
    `SortingLayer` → `OrderInLayer` → `Position.z`. 같은 문서의 기본 규약이
    **Object 0 / Tile 1 / Monster·NPC 2 / 내 아바타 4**라, 데코를 0·1로 두면 몬스터가 위에 온다.
  - ⚠ **order만 낮추면 안 고쳐진다.** 레이어가 1순위라 레이어가 다르면 order를 안 본다.
    **두 값을 같이** 넣어야 한다.
  - ⚠ **앞으로 데코를 놓을 때마다 `SortingLayer`를 같이 줘야 한다** — `sprite()`는 안 넣어 준다.
- **T43-1 엘리니아 구조 재설계 ✅** — 대표 지적: *"엘리니아 다시 만들어 맵 디자인 복잡하게
  일직선이 뭐야 저게"* + *"맵 디자인 좀더 복잡하게 할 수 없어?"*.
  **일자 5방(게이트 0개) → 갈림 3회 8방**으로 다시 짰다.
  실측: 8방 전부 스폰 · `MissingModel` 0 · 깨진 연결 0 · 새 맵 6개 **448칸씩 통과불가 0** ·
  미니맵 `엘리니아 방 8개` · 게이트 **양방향**(통과/차단 둘 다) · 빌드 에러 0
  ```
                    r_29 요정과 나무의 숲 [key 바위 던지기]  ← 막다른 보상방
                       ↑N
   r_21 ─E→ r_22 ─E→ r_23 ─E→ r_24 [stat 65] ─E→ r_25 (보스)
    ↓S                              ↓S
   r_26 ─E→ r_27 ───────N───────────┘
  ```
  - **"복잡하게"의 실체는 갈림 횟수다.** 페리온·커닝시티는 갈림이 **1회**(사거리 하나)인데
    엘리니아는 **진입 직후(r_21) · 사거리(r_23) · 게이트 방(r_24)** 세 곳에서 갈린다.
    순환 1개 + 막다른 보상방 1개는 같다.
  - ⚠⚠ **진입 방에 boss 게이트를 안 걸었다** (설계 §9 공식은 "진입 방 = 직전 지역 보스").
    엘리니아는 **두 번째 지역**이라 여기서 막히면 게임이 아예 안 굴러가고,
    **이미 열려 있던 지역**이라 진입 문을 새로 다는 것은 되돌리는 변경이다.
    커닝시티(4번째)는 앞에 지역이 셋이라 사정이 다르다. `stat`·`key`만 세웠다.
  - **`stat 65`는 공식값이다** — 설계 §9의 `5 × (10k+3)`, 엘리니아는 k=1이라 `5 × 13`.
    페리온 170(Lv35) · 커닝시티 165(Lv34)와 같은 규칙이고 여기서는 **Lv14**다.
  - **`key`는 헤네시스 보스 스킬(바위 던지기)이다.** `s_mon_stone.is_key_skill=true`.
    커닝시티가 엘리니아 보스 스킬을 쓴 것과 같은 구조 — **앞 지역을 실제로 플레이했는가**를 묻는다.
  - **몬스터는 기존 5종을 다른 레벨로 재사용했다** (커닝시티가 스티지·주니어 레이스·셰이드를
    두 번씩 쓴 것과 같은 방식). 새 팩 검색도 모델 제작도 없다.
  - **포탈은 새로 안 놓았다** (T26-1). 필요한 조합을 가진 페리온 맵을 복제하고 **남는 것만 지웠다**:
    `map13`(E·N·S·W)에서 W를 지워 r_21 · S를 지워 r_23 / `map14` → r_24 / `map16` → r_26 /
    `map17` → r_27 / `map1a` → r_29. `map22`·`map25`는 조합이 맞아 그대로 뒀다.
  - ⚠⚠⚠ **`.map`을 통째로 재직렬화하면 바닥이 통째로 안 그려진다** (실측으로 겪었다).
    타일을 `JSON.parse` → 수정 → `JSON.stringify`로 썼더니 들여쓰기가 바뀌며
    **4028줄이 484줄로** 재작성됐고, 게임 화면에서 **타일이 하나도 안 나왔다**
    (런타임 `GetTile`은 448칸을 돌려주는데 옛 인덱스였다).
    T33-5가 *"`tileIndex` 숫자만 범위를 좁혀 고쳤다 / diff가 정확히 10줄"*이라고 적어 둔 것이
    이 뜻이다. **`"tileIndex": N`의 숫자만 순서대로 치환**하고 나머지 바이트는 손대지 않는다
    (줄 수 변화 0). 검산은 `MapBuilder.getTiles()`로 한다.
  - ⚠ **`RectTileMapComponent:GetTile`은 `Vector2`가 아니라 `Vector2Int`를 받는다.**
    `Vector2`를 넘기면 `[LEA-3005]`가 난다 — 정수 두 인자 형태(`GetTile(x, y)`)가 안전하다.
  - ⚠ **클라에서는 자기가 선 맵만 잡힌다.** 여러 맵의 타일을 훑으려면 **서버에서 텔레포트와 같이**
    돌려야 한다 (`_TeleportService`는 클라 스크립트에서 안 먹었다).
  - **잔디는 기존 맵이 실제로 쓰는 0·2·3·4만 썼다.** `1`은 어느 맵도 안 쓴다 —
    무엇인지 모르는 타일을 바닥에 깔지 않는다 (T26-4에서 `stone_4/5`에 데인 것과 같은 함정).
  - **방마다 결을 다르게 줬다**: 오솔길(사거리)은 발길이 닿아 잡초가 많고, 어두운 숲속이 가장
    무성하며(민무늬 70%), 보상방인 요정과 나무의 숲이 가장 깨끗하다(93%).
  - ~~다음: 엘리니아 숲 데코~~ → **T43-2에서 했다 (오브젝트가 아니라 바닥 타일로).**
- **T49 커닝시티도 타일만으로 ✅ — 오브젝트 311개를 걷어내고 전용 타일셋을 그렸다.**
  엘리니아(T47)와 같은 형식으로 맞췄다. T40-3에서 얹은 선로·철골·배관 **311개를 전부 제거**하고
  지하철 전용 타일 **3종**을 직접 그려 넣었다.
  실측: 8방 **데코 0개** · 몬스터 67마리 정상 · 타일 448칸씩 **통과불가 0** ·
  선로 칸 수가 설계와 일치(보스방 112칸) · 빌드 에러 0
  - **선로를 오브젝트가 아니라 타일로 만들었다**: `Kerning_concrete_1/2`(회색 콘크리트·바닥판) +
    `Kerning_rail`(침목 + 레일 두 줄 — **가로로 이어 깔면 선로**가 된다).
  - ⚠ **선로에는 룰타일(경계 16종)이 필요 없다** — 선로는 원래 직선이라 특정 y줄에만 깔면 된다.
    엘리니아의 흙길은 곡선이라 경계가 필요했지만 여기는 아니다.
  - **방마다 선로 줄을 다르게** 줬다: 방치된 공사장은 **선로 없음**(아직 안 깔린 공사장) /
    구간들은 두 줄 / **1호선 3구간(보스)은 네 줄**로 가장 촘촘하다.
  - **길(y=1)에는 선로를 안 깐다** — 다니는 자리라 걸려 보이면 답답하다.
  - **이제 두 지역이 같은 형식이다**: 엘리니아 = 어두운 이끼(정글) · 커닝시티 = 회색 콘크리트 + 선로.
    둘 다 오브젝트 0이고 **바닥이 지역을 말한다**.
- **T48 엘리니아 진입 방이 세 갈래였던 것 수정 ✅** — 대표 신고: *"포탈 좀 잘못 되어 있는거
  같아 보스 전방이랑 처음 방이랑 연결되어 있네 마을로 가야 하는거 아닌가"*.
  ⚠ **데이터는 정상이었다** — 실측에서 `r_21(map21) --north--> r_town(maptown)`으로 마을에
  제대로 갔고 8방의 포탈·목적지가 전부 CSV와 일치했다. **틀린 것은 구조**다.
  실측: 8방 포탈 전부 일치 · 빈 연결 0 · **깨진 연결 0개** · 타일 448칸씩 통과불가 0 ·
  미니맵 격자 **겹침 0** · 빌드 에러 0
  - **원인은 T43-1에서 내가 넣은 "진입 직후 갈림"이다.**
    `r_21(처음 방) ─S→ r_26 ─E→ r_27 ─N→ r_24(보스 전방)` — 처음 방이 **세 갈래**
    (마을 N / 본선 E / 우회로 S)가 됐고 그 우회로가 **세 방 만에 보스 앞**에 닿았다.
    ⚠ **페리온 `r_11`·커닝시티 `r_041`은 진입 방의 포탈이 둘(마을·앞)뿐**이다 —
    엘리니아만 규격에서 어긋나 있었다.
  - **고친 것: 우회로 입구를 `r_21` → `r_22`로.** 진입 방은 마을과 본선 둘뿐이고,
    **`r_23`이 진짜 사거리(N/E/S/W 넷)**가 됐다 — 위는 보상방, 아래는 우회로.
    순환은 그대로다(`r_22 → r_26 → r_27 → r_23 → r_22`).
    갈림이 3회 → 2회로 줄었지만 **줄어든 하나가 애초에 잘못 놓인 것**이었다.
  - **덤: T44의 아쉬움이 해소됐다.** *"우회로가 본선으로 돌아오는 연결선은 안 그려진다"*고
    남겨 뒀는데, 새 배치는 **격자가 완벽히 닫혀** 순환이 지도에 다 보인다.
  - **포탈은 새로 안 놓았다**(T26-1): `map21`·`map24`는 `Portal_S`를 지우고,
    `map22`는 `map14`(E·S·W) · `map23`은 `map13`(사거리)을 복제했다.
    복제본은 페리온 흙이 딸려 와 **정글 타일셋으로 다시 칠했다**.
  - ⚠ **교훈: 진입 방은 포탈이 둘이어야 한다** — 마을과 앞. 갈림은 두 번째 방부터다.
    앞으로 지역을 짤 때 이 규격을 먼저 본다.
- **T47 엘리니아 꾸민 오브젝트를 걷어냈다 ✅ — 타일만 남긴다.**
  대표 지시: *"꾸민 오브젝트도 빼주면 좋을거 같아보이네 이런 형식으로 타일 만들어달라는거였어
  레퍼런스 게임인 리엔드랑 인플레이션 rpg 보면 그렇게 되어 있었잖아"*.
  T45의 정글 오브젝트 **679개를 8방에서 전부 제거**했다. 남은 것은 T46에서 직접 그린
  정글 타일(어두운 이끼) 바닥뿐이다.
  실측: 8방 **데코 0개** · 몬스터 67마리 정상 · 타일 448칸씩 **통과불가 0** · 빌드 에러 0
  - **왜 이게 맞는가**: Re:END와 인플레이션 RPG는 바닥이 단순하고 오브젝트가 거의 없다 —
    **화면의 주인공이 몬스터와 숫자**이기 때문이다. CLAUDE.md도 엔진을
    *"숫자가 오르는 성장의 손맛 (Re:END식)"*으로 적어 두었다.
    오브젝트를 층으로 쌓으면(T45) 화면이 꽉 차는데, 그건 이 게임이 보여주려는 것을 가린다.
  - ⚠ **T40-3 → T45 → T47이 한 줄기다.** 커닝시티에 오브젝트를 얹고(311개), 엘리니아에도
    얹고(679개), 결국 **둘 다 걷어내는 방향**으로 왔다. 배운 것: **레퍼런스 게임의 화면 밀도를
    먼저 보고 시작해야 한다** — "지역 콘셉트를 살린다"를 오브젝트로 푸는 것이 기본값이 아니다.
  - 경계 타일 3종(SE/SW/NE)을 더 올려 타일셋이 **7종**이 됐다. 길을 깔려면 **16가지가 다
    있어야** 모서리가 이어지므로(없으면 구멍이 난다) 이번에도 길은 안 깔았다.
  - **남은 것**: 경계 11종을 마저 올리면 길까지 타일로 낼 수 있다(그림은 이미 다 있다).
    그리고 **커닝시티의 오브젝트 311개**도 같은 형식으로 가려면 걷어내고
    커닝시티 전용 타일셋(회색 콘크리트 + 선로)을 만들어야 한다.
- **T46 엘리니아 전용 타일을 직접 그렸다 ✅ — 지역 타일셋을 만들 수 있게 됐다.**
  대표 지시: *"타일 있잖아 이거 답답하네 그냥 색 타일 새로 그거에 맞게 만들어서 해주면 안되려나?"*.
  맞다 — 20개 지역을 전부 `RectTileData_Henesys` 하나로 칠하고 있어서 **어느 지역이든
  헤네시스의 밝은 잔디**였다.
  실측: 8방 448칸씩 **통과불가 0칸** · 빌드 에러 0 · 화면으로 확인
  - ⚠⚠⚠ **`.tileset`을 새로 만들 수 있다.** T39-2에 *"빌더가 없는 파일 형식이라 못 한다"*고
    적어 뒀는데 **실제로 해 보니 된다**: `.tileset`의 `EntryKey`(`tileset://UUID`)와
    `.map`의 `RectTileMapComponent.TileSetRUID`가 문자열로 짝을 이루고,
    **`datas[].Id`는 그냥 스프라이트 RUID**다. 내가 그린 PNG를 올려 RUID를 받아 넣으면 된다.
    **64x64**가 정상으로 들어간다(엔진이 1 world unit에 맞춰 늘린다).
    ⚠ 시험 타일 **한 장으로 먼저 확인**하고 진행했다 — 검증 안 된 파일 형식이라 그게 맞았다.
  - **지역마다 색만 바꿔 찍어내는 생성기를 뒀다** (`docs/art/tiles/gen_tileset.py`).
    룰타일 16종은 결국 *"네 모서리가 각각 바닥인가 길인가"*의 16가지이고, 픽셀마다
    **네 코너 값을 이중선형 보간**해 0.5를 넘으면 길로 칠하면 경계가 저절로 둥글게 이어진다 —
    원작 헤네시스 타일셋과 **같은 구조**다(T43-2에서 그 61종의 모서리 잔디 비율을 재서 알아냈다).
    **프리셋에 색 한 벌만 더하면 지역이 하나 는다.**
  - **엘리니아 = 정글**: 어두운 이끼 바닥 `(58,102,44)` + 축축한 진흙길 `(92,72,48)`.
    4종을 올려(`Jungle_floor_1/2/3` + `Jungle_path`) 8맵에 적용했다.
  - **바닥 변주가 셋인 이유**: 한 종만 쓰면 같은 얼룩이 반복돼 **격자 무늬처럼** 보인다
    (타일을 이어 붙여 보고 알았다).
  - ⚠ **경계 14종은 아직 안 올렸다.** 18종을 다 그려 뒀지만(`docs/art/tiles/jungle/`)
    업로드가 4종까지다. 길을 깔려면 경계가 있어야 모서리가 둥글어지므로 **이번엔 길을 안 깔고
    바닥만** 깔았다 — 정글의 결은 덩굴 오브젝트(T45)가 만든다.
    **색이 이 방향이 맞으면 나머지 14종을 올려 길까지 넣는다.**
- **T45 엘리니아를 정글로 ✅** — 대표 지적: *"풀숲이 아니라 덩굴 숲 느낌인데 정글처럼"*.
  맞았다. T43-2의 바닥 타일만으로는 **들판에 난 길**이 됐다.
  실측: 8방 **데코 679개 전부 `MapLayer0` order 0·1** · 스프라이트 없음 0 · **콜라이더 0** ·
  몬스터 8~10마리 정상 스폰(보스방 1) · 빌드 에러 0 · 화면으로 정글 확인
  - ⚠⚠ **타일만으로는 정글을 만들 수 없다.** 타일셋에 있는 것은 **잔디·흙·돌·물**뿐이고
    **덩굴이 없다.** 그래서 오브젝트를 다시 썼다 — 직전 지시("오브젝트 넣지 말고")와
    표면상 어긋나지만, *"덩굴 숲"*을 요구한 이상 다른 길이 없다.
    **대신 커닝시티처럼 흩뿌리지 않고 층으로 쌓았다.**
  - ⚠⚠⚠ **카메라는 세로 7유닛만 본다 — 이걸 놓쳐서 한 번 헛돌았다.**
    처음엔 층을 `y -6.6 ~ 7.4`(방 전체 높이)에 깔았는데 **대부분이 화면 밖**이라
    화면에는 "잎이 좀 있는 들판"만 보였다. 길(y=1)에 섰을 때 보이는 것은 **y -2.5 ~ 4.5**다.
    층을 그 안으로 끌어내리고서야 정글이 됐다.
    | 층 | y | 무엇 |
    |---|---|---|
    | 맨 위 | 6.8 | 어두운 덤불 |
    | | 5.2 | **덩굴 감긴 나무 기둥** (몸통이 화면에 보인다) |
    | | 3.7 | **늘어진 덩굴 커튼** (가로로 이어 깐다) |
    | | 2.7 | 수풀 |
    | **길** | **-1.4 ~ 2.4** | 여기만 비운다 — 플레이어·몬스터가 노는 곳 |
    | | -1.9 / -3.4 / -4.6 | 수풀 두 겹 + 덤불 |
    | | -5.6 | 나무 기둥 |
  - **덩굴이 주인공이다.** 길 양옆(y 2.5~4.6 / -3.2~-1.6)에 늘어진 덩굴을 흩뿌려
    **화면 안에서 덩굴이 드리운다**. `bbb1f234`(832x372 덩굴 커튼) ·
    `16435c51`/`3099d769`/`04658855`(덩굴 감긴 기둥 3종) · `20a93edb`(늘어진 조각)이 뼈대다.
  - ⚠ **큰 덩굴 벽 3종은 뺐다** (`26767d25` · `fda3e6cc` · `496b71d9`). 잔디색 대조 시트에서
    **회색 배경 조각이 같이 들어 있는 것**이 보였다 — 잔디 위에 놓으면 회색 네모가 뜬다.
    불투명 비율(63~72%)만 봤으면 못 걸렀다. **시트를 봐야 걸린다.**
  - ⚠ **보라 잎이 한쪽에 뭉쳤다** (실측 화면). 후보 배열에서 비중을 낮추고 초록 계열을
    늘려 고쳤다 — 무작위 배치는 **같은 것을 여러 번 넣어 확률을 조절**한다.
  - **바닥도 같이 손봤다.** T43-2의 꽃밭을 거의 걷어냈다(요정의 숲 40%만 남기고
    사악한 기운의 숲·어두운 숲속은 **0%**) — 정글 바닥은 꽃밭이 아니고, 이제 결은
    **덩굴 오브젝트**가 만든다.
  - ⚠ **커닝시티의 오브젝트 데코 311개는 그대로 뒀다.** 지하철 선로·철골은 타일로 못 만든다.
- **T43-2 엘리니아 숲 — 바닥 타일만으로 ✅** — 대표 지시: *"그 오브젝트를 넣지 말고
  그냥 바닥 색으로만 맞춰서 해주면 안될까?"*. 커닝시티식 **오브젝트 데코를 안 얹고**
  타일만으로 숲을 만들었다.
  실측: 8방 전부 **448칸 · 통과불가 0칸** · 빌드 에러 0 · 화면으로 오솔길과 꽃밭 확인
  - ⚠⚠⚠ **타일셋이 룰타일(블롭) 세트라는 것을 풀었다.** `Henesys_soil` 61종의 썸네일을
    받아 **네 모서리의 잔디 비율**을 재니 16가지 조합이 **전부 갖춰져 있었다**:
    | 흙인 모서리 | 타일 | | 흙인 모서리 | 타일 |
    |---|---|---|---|---|
    | 없음 | 0~4 (잔디) | | 전부 | 58~60 (흙) |
    | SE / SW / NE / NW | 39 / 40 / 41 / 42 | | 아래·위·오른·왼 절반 | 28 / 29 / 30 / 31 |
    | NW+SE / NE+SW (대각) | 52 / 51 | | 한 모서리만 잔디 | 44 / 43 / 46 / 45 |
    → **잔디 위에 흙길을 자유롭게 그릴 수 있다.** 경계가 저절로 둥글게 이어진다.
    T26-4가 *"104종이 잔디↔길 경계 조각"*이라고만 적어 둔 것의 정체다.
  - **길은 포탈을 잇는다.** 좌우 포탈이 y=1, 상하가 x=0에 있으므로(`RoomPortal` 기본값)
    길을 거기 맞췄다 — **길을 따라가면 다음 방**이라 바닥이 장식이 아니라 안내가 된다.
    포탈 근처에서는 흔들림을 죽여 길이 문에 정확히 닿게 한다.
  - **직선은 들판이지 숲이 아니다.** 중심선을 씨앗 고정 사인 합으로 흔들어 구불구불하게 냈고,
    폭은 **3칸**(반폭 1)이다. 처음엔 5칸 직선이었는데 미리보기에서 들판으로 보였다.
  - **잔디 2/3/4는 꽃 밀도다** (썸네일로 확인 — 0·1은 민무늬, 2→3→4로 꽃이 는다).
    한 칸씩 뿌리면 잡티라(T26-4) **4x4 블록 해시로 덩어리**를 만들어 꽃밭이 되게 했다.
    방 성격이 바닥으로 읽힌다: 요정과 나무의 숲 62% ↔ **사악한 기운의 숲 0% · 어두운 숲속 6%**.
  - **막다른 방·보스방은 둥근 공터**다(타원 마스크). 길이 들어와 넓어지는 꼴이라
    "여기가 끝"이 바닥으로 보인다.
  - ⚠ **`1`(soil_2)을 쓰기 시작했다.** T43-1에 *"어느 맵도 안 쓴다 — 무엇인지 모르는 타일을
    바닥에 깔지 않는다"*고 적었는데, **썸네일로 보니 민무늬 잔디**(0과 거의 같다)이고
    `IsCollidable=false`다. 이제 안전하다고 확인됐다.
  - **게임 없이 미리보기를 그려 판단했다** (T26-4 방법). 타일 썸네일을 격자에 붙이면
    방 그림이 나온다 — 길 폭과 구불거림을 플레이 없이 두 번 고쳤다.
  - ⚠ **파일을 통째로 재직렬화하지 않았다** (T43-1 교훈). `"tileIndex": N`의 숫자만
    순서대로 치환했다 — **줄 수 변화 0**.
  - ⚠ **커닝시티의 오브젝트 데코 311개는 그대로 뒀다** — 대표 지시가 "그 오브젝트"(= 지금
    넣으려던 엘리니아 것)를 가리키는 것으로 읽었다. **커닝시티도 걷어낼지는 대표 판단이 필요하다.**
- **T44 미니맵이 방을 겹쳐 그려 보스방이 사라지던 것 수정 ✅** — 대표 신고:
  *"왜 파우스트 지역이 왜 나오지 않냐? 이상한데?"*. **맞았다.** 방·포탈·게이트는 전부
  정상인데(8방 전부 통과 가능·포탈 방향 정상·등록 정상) **미니맵 격자에서 두 방이 같은 칸에
  떨어져** 파우스트 방이 다른 방 밑에 깔려 있었다.
  실측(고치기 전): `r_24`가 `r_22`와 같은 칸 · **`r_25`(사악한 기운의 숲)가 `r_23`과 같은 칸`.
  실측(고친 뒤): **네 지역 전부 겹침 0** · 화면에서 8방과 `◆사악한 기운의 숲 파우스트x1` 확인 ·
  빌드 에러·경고 0
  - **원인은 `LayoutRooms`가 점유 검사를 안 하는 것**이었다 — `gx[nextId] = gx[id] + dx[i]`가
    **그 칸이 이미 쓰였는지 안 본다.** 순환이 있는 지도에서는 **두 경로의 변위가 달라서**
    같은 방이 서로 다른 좌표를 요구한다: 엘리니아 `r_24`는 본선으로 오면 x=3인데
    우회로로 오면 x=1이다.
  - **고친 것 둘.** ① **동서를 먼저 훑는다** (`east west north south`). 지역의 본선은 대개
    동서로 뻗고 우회로가 남북으로 돌아 나가는데, 남북을 먼저 훑으면 **우회로 쪽 좌표가
    먼저 매겨져** 본선 방과 어긋난다. ② 그래도 겹치면 **빈 칸으로 밀어내고 경고를 남긴다** —
    조용히 사라지는 것이 최악이다 (T35에서 "조용히 잘리는 것"을 고친 것과 같은 정신).
  - **실제로는 ①만으로 해결됐다** (밀어낸 경고 0건). ②는 앞으로 area_05~20이 붙을 때를
    위한 안전망이다.
  - ⚠ **우회로가 본선으로 돌아오는 연결선은 여전히 안 그려진다.** `DrawLinks`가
    **격자에서 실제로 붙어 있는지**로 긋는데(T26-3) `r_27`(1,-1)과 `r_24`(3,0)는 안 붙는다.
    거짓 선이 그려지지는 않는다 — **방은 전부 보이고 선 하나가 없는 것**이다.
  - ⚠ **`.mlua`에서 값을 둘 돌려주면 빌드 경고가 난다** (`LWA 1111`). 선언이 `any` 하나라
    안 맞는다 — 테이블(`{x=, y=}`)로 돌려줬다.
  - ⚠ **이 결함은 방이 늘수록 커진다.** 지금까지 안 드러난 이유는 페리온·커닝시티가
    **우회로와 본선의 길이가 같아** 우연히 격자가 닫혔기 때문이다. 앞으로 구조를 짤 때
    **격자에 담기는지**를 같이 봐야 한다 — 안 담기면 이제 경고가 뜬다.
- **T41 커닝시티 몬스터 모델 5종 ✅ — area_04가 실제로 열렸다.**
  `Snail.model`을 틀로 복제해 옥토퍼스·스티지·주니어 레이스·셰이드·레이스를 세웠고,
  `LandmarkTable`에 **`30,area,area_04`**를 넣어 문을 열었다 (T39-1부터 일부러 비워 둔 자리).
  실측: `구역 개방 [커닝시티]` · **8방 전부 스폰 · `MissingModel` 0건** · 미니맵 `커닝시티 방 8개` ·
  5종 전부 화면으로 확인 · 보스 배율 정확(HP 1478→**4434** ×3 / DEF **821** ×1 / ATK **909** ×1.5) ·
  빌드 에러 0
  - ⚠⚠⚠ **랜드마크 원장이 레벨 단위라 Lv30 줄이 통째로 가려졌다** (실측으로 잡았다).
    `30,area,area_04`를 넣었는데 Lv37 세이브가 **안 받았다** — Lv30에는 이미 `slot,1`이
    있어서 원장에 `"30"`이 찍혀 있었고, `GrantLandmarksUpTo`가 그 레벨을 통째로 건너뛴 것이다.
    **원장 키를 줄 단위(`레벨:종류:값`)로 바꿨다.** 이제 표에 줄을 더해도 그 줄만 새로 지급된다.
    - T40-1이 *"표를 나중에 고치면 같은 일이 또 난다"*고 적어 둔 함정의 **두 번째 얼굴**이다.
      그때는 "레벨을 지나쳐서" 못 받는 것을 고쳤고, 이번은 "한 레벨에 줄이 둘이라" 못 받는 것이다.
    - 옛 세이브의 `"30"` 같은 레벨 키는 그냥 무시된다. 다시 지급돼도 안전하다 —
      `area`는 이미 열렸으면 건너뛰고 `slot`은 원래 아무것도 안 한다.
  - ⚠⚠⚠ **발견: 리소스 팩 원소의 `rel_path`에 클립 이름이 그대로 있다** (`stand`/`move`/`hit1`/`die1`).
    T19-3 이후로 *"클립엔 이름이 안 붙는다"*고 알고 **썸네일 GIF를 대조 시트로 만들어 눈으로**
    골라 왔는데(T36-2·T37-1b), 그건 `searchResources`로 클립을 **낱개로** 찾았을 때 이야기다.
    **팩으로 받으면**(`getResource(packId).payload.elements`) 이름이 온다.
    달팽이(정답을 아는 대조군)로 **5개 전부 일치**를 확인했다. 이 작업의 가장 큰 관문이 없어졌다.
  - **전용 공격 클립이 없는 종이 셋이다.** 원작에도 없으므로 다른 클립을 물렸다 —
    옥토퍼스·셰이드는 `stand`, 주니어 레이스·레이스는 `jump`, 스티지는 `fly`.
    (주황버섯이 `attack`에 `jump`를, 리프 퍼펫트리가 `stand`를 넣은 것과 같은 처리다.)
  - **히트박스는 stand 클립 크기에서 유도했다** (파우스트 136x212 → 1.36x2.12과 같은 규칙):
    옥토퍼스 0.66x0.72 / 스티지 0.40x0.38 / 주니어 레이스 0.44x0.36 /
    **셰이드 1.03x0.95**(가장 크다) / 레이스 0.72x0.60.
  - **공속·추격 범위는 모델에 안 넣었다** — T33-3에서 CSV로 옮겼고 모델 값이 0이어야 CSV가 쓰인다.
  - ⚠ **팩 id를 다시 검색해 확인했다** (T37-1b 교훈). 5종 전부 실존하고 대역도 안전하다
    (`9xxxxxx` 이벤트 · `24~26xxxxx` 그란디스 아님): 옥토퍼스 `mob/1120100` / 스티지 `mob/2300100` /
    주니어 레이스 `mob/3230101` / 셰이드 `mob/5090000` / 레이스 `mob/4230102`.
  - **곁들여 고친 것: 미니맵이 맵 전환 중 널 참조로 터졌다** (실측 1건, 프레임이 `GetComponent`).
    `WorldMapPanel.Refresh`가 `me == nil`만 보는데 **전환 중에는 nil이 아니면서 무효**다.
    `isvalid(me)`로 바꿨다 — 0.5초 타이머라 한 프레임 건너뛰어도 다음 틱에 다시 그린다.
  - ⚠ **마릿수가 설계(4/4/5/5/1)보다 많은 것은 환생 보너스다** (상한 +5). 실측 8~10마리,
    **보스방만 1마리** — 보스방은 보너스 제외라 설계대로다.
  - ⚠ **`%d`에 실수값을 넣으면 `[LEA-2005]`가 난다** — 검증 스크립트에서 또 겪었다.
    `rm.Atk`가 909.0이라 터졌다. 로그는 `tostring`으로 찍을 것.
- **다음 (대표 지적 1건, 미착수)**
  - ⚠ **엘리니아도 같은 규격으로** 다시 짤 것 (지금 일자 5방).
  - ⚠ **커닝시티 포획 스킬 5종이 아직 없다** (`drop_skill_id` 다섯 칸이 비어 있다).
    지금 커닝시티는 **포획이 안 되는 지역**이다 — T32(페리온)·T37(엘리니아)과 같은 3단계
    (데이터 → 아이콘 → 이펙트·소리)가 남았다. 대표 지시대로 **액티브 3 + 패시브 2**로 나눈다.
- 진행 중: **T38 포획 패시브 · 지역 20칸 선행 작업** (2026-08-29 대표 지시).
  **T38-1 포획 패시브 ✅** (`skill_kind=passive` + `passive_stat`/`passive_value` 컬럼.
  **슬롯을 안 먹고 보유만 해도 켜진다.** 장당 첫 장 1P + 추가 0.25P.
  기존 17종 중 3종을 패시브로 — 뿔 들이받기 STR / 물방울 터뜨리기 INT / 강철 가죽 DEX.
  실측: 0장→1장→만작에서 보정 0 → 1.00 → 2.00, 물공 170 → 177 → 184 · 게이트 `STAT_TOTAL=0` 유지) /
  **T38-2 마을 출입 정리 + 해금 레벨 정정 ✅** (`r_town` conn 네 칸을 비우고
  `VerifyRooms`가 마을을 향하는 링크의 역방향을 안 묻는다. 엘리니아 Lv20 → **Lv10**,
  페리온 Lv30 → **Lv20**. 실측: 역방향 면제 3건 · 깨진 연결 0개 · `Lv10 area_03` / `Lv20 area_02`) /
  **T39-1 area_04 데이터 계층 ✅** (몬스터 5종 + 방 5개 `r_041`~`r_045` + `area_04`.
  **게이트 3종을 설계대로 세웠다** — 진입 방 `boss r_15`(페리온 보스) · 3번째 방
  `stat STAT_TOTAL 165`. 실측: 5종 전부 `[공식 일치]` · 방 25개 / 연결 44개 / 깨진 연결 0개) /
  **T39-2 맵 5개 ✅** (`map041`~`map045`. `map01`·`map05`를 `fromTemplate`으로 복제하고
  **커닝시티 = 회색 조약돌**로 칠했다 — 헤네시스·엘리니아가 잔디, 페리온이 갈색 흙이라
  **아직 안 쓴 팔레트**다. 실측: 다섯 방 448칸씩 · **통과 불가 0칸** · 빌드 에러 0) /
  다음: **T39-3 몬스터 모델 5종**
  - ⚠⚠⚠ **원작 맵 가져오기는 못 쓴다 — 실제로 넣어 보고 알았다.**
    `maker_import_maplestory_map`은 배경 레이어를 얹는 것이 아니라 **맵을 통째로 갈아치운다.**
    `map042`에 `103020100`(선로를 따라서)을 넣었더니:
    **타일 448 → 0칸**(걸을 바닥이 사라진다) · 우리 `Portal_E`/`Portal_W`가 원작 `Portal` 2개로
    바뀌고 · **원작 몬스터 23마리 + NPC 4명**이 딸려 들어오고(우리 `RoomSpawner`가 모르는 것들) ·
    사이드뷰 개념인 **foothold 9개 + 사다리 30개**가 RectTile 맵에 박힌다.
  - **그리고 섞는 안(바닥은 우리 것, 배경만 원작)도 성립하지 않는다.** 탑다운 방에서는
    **타일 바닥이 곧 그림**이라, 우리 448칸을 되살리면 가져온 그림을 덮는다.
    설계서 §10의 안 B·C는 이걸로 닫혔다 — **안 A(우리 타일셋)로 간다.**
  - ⚠⚠ **`stone_4`(64)·`stone_5`(65)를 바닥에 깔았다가 초록 파편이 박혔다.**
    T26-4에 *"`_4`·`_5`는 잔디가 붙은 테두리라 바닥에 흩뿌리면 초록 파편이 박힌다"*고
    **적어 두고도 또 썼다.** 단색 회색은 `stone_1`(61)·`stone_2`(62) **둘뿐**이다.
    (단색 전체: 갈색 흙 58·59·60 / 회색 조약돌 61·62 / 잔디 0~4)
  - **방마다 결을 다르게 줬다** (T26-4 규칙): 조약돌 두 종의 섞는 비율 5~45%와
    세로 선로 줄무늬(3칸·6칸 간격)로 갈랐다. ⚠ 두 종이 비슷해 **차이가 은은하다** —
    T26-4가 *"자갈 지형은 회색 바위만큼 눈에 띄지 않는다"*고 적은 그대로다.
  - ⚠ **맵을 복제할 때는 `MapBuilder.fromTemplate`을 쓴다.** `read`+`rename`은
    엔티티 이름만 바꾸고 **`.map`의 `EntryKey`를 안 바꿔서** Maker가 못 읽는다
    (`[LEA-3015] Entry key가 중복되어 파일 로드에 실패`). `fromTemplate`은 EntryKey·
    루트 경로·엔티티 UUID를 전부 다시 쓴다.
  - **포탈은 새로 안 놓았다** (T26-1). 필요한 조합을 가진 맵을 골랐다 —
    사냥방 넷은 `map01`(Portal_E+W), 보스방은 `map05`(Portal_W+Return).
  - **몬스터는 원작 커닝시티 지하철 계열이다**: 옥토퍼스 31 / 스티지 33 / 주니어 레이스 35 /
    셰이드 38 / **레이스 40(보스)**. 원작 Lv43~45를 우리 대역에 맞췄다.
    ⚠ 원래 보스로 잡았던 **크림슨 락은 이벤트 팩(`9800009`)뿐이라 뺐다.**
  - **게이트 값 165는 지어낸 것이 아니다.** 설계 §9의 식 `5 × (10k+3)`이 주는 값이고,
    기존 `r_14`의 `STAT_TOTAL 170`(Lv35)과 맞아떨어진다 — 이미 쓰던 감각에서 뽑았다.
  - ⚠⚠ **랜드마크를 일부러 안 넣었다** (T36-1과 같은 이유). `LandmarkTable`에
    `30,area,area_04`를 넣으면 Lv30인 사람이 **맵이 없는 지역으로 들어간다.**
    한 번 넣었다가 뺐다 — T39-2에서 맵과 함께 넣는다.
  - ⚠ **로그 필터의 부분문자열 충돌을 또 겪었다** (T36-1과 같은 실수). `r_04`로 거르면
    `r_041`~`r_045`가 전부 걸려서 **다른 방의 연결 줄이 엉뚱한 블록 아래 붙어 보인다** —
    `r_045.east = r_04`처럼 읽혔지만 실제 CSV는 비어 있었다. **로그로 의심되면 CSV를 직접 볼 것.**
  - 방 이름·원작 맵 id는 `docs/지역설계_area04-area20.md` §3 #4에 있다
    (방치된 공사장 `103010100` / 선로를 따라서 `103020100` / 환승구역 `103020200` /
    2호선 3구간 `103020420` / 1호선 3구간 `103020320`).
  - **설계는 `docs/지역설계_area04-area20.md`에 다 있다** (17개 지역 · 85종 몬스터 ·
    팩 id 검증 완료 · 게이트 배치 공식 · 맵 방식 셋).
  - ⚠ **`VerifySkills`가 모르는 `skill_kind`에 에러를 낸다.** `passive`를 더할 때
    거기도 같이 고쳐야 했다 — 안 고쳤으면 실행할 때마다 빨간 줄이 셋 떴다.
  - ⚠ **CSV 노트에 큰따옴표를 쓰면 안 된다.** 이스케이프를 안 해 줄이 깨졌고,
    Maker가 다시 저장하며 따옴표를 덧씌워 내용이 밀렸다. 홑따옴표로 바꿨다.
- 완료: T0 ~ T9 (수직 슬라이스 전 구간) + T10-1 / T10-1b / T10-2 / T10-3 / T10-4
  — **작업지시서의 코드 태스크는 전부 끝났다.** 단, 기획서에 있으나 구현하지 않은 것이
  **7건**(+ 문서 근거 없이 구현된 것 2건) 있다 (`docs/인수인계_현재상태.md` 3절)
- 완료: **T11 연출·UI 보강** (지시서 밖 추가 작업).
  T11-1 몬스터 공격 모션 ✅ / T11-1b 공격 효과음 ✅ / T11-2 상시 HUD(HP·EXP·레벨) ✅
- 진행 중: **T12 공간 확장** (지시서 밖 추가 작업).
  T12-1 지역(area) 데이터 계층 ✅ / T12-2 마을 맵 + 시작방 이전 ✅ /
  T12-3 마을 게이트(지역 선택 이동) ✅
- 진행 중: **T13 아이템·회복** (지시서 밖). T13-1 마을 전회복 ✅ /
  T13-1b 게이트 STAT_TOTAL ✅ / T13-1c 마을 진입 버그 ✅ /
  T13-1d 신규 계정 마을 시작 ✅ / T13-2 아이템 데이터 계층 ✅ (`ItemTable.csv` 6종) /
  T13-3 인벤토리·장착 저장 ✅ (`Inventory/PlayerInventory.mlua` + `Perm.inventory/equipped`) /
  **T13-4 장착 스탯 반영 ✅** (`PlayerStats.GetEquipBonus` — 전투는 장비 포함,
  게이트는 `GetStatWithoutEquip`으로 장비 제외) /
  T13-5a 가방 창 ✅ (`ui/Inventory.ui` + `UI/InventoryPanel.mlua` — 줄을 눌러 장착·해제) /
  **T13-6 몬스터 드랍 ✅** (`Inventory/ItemDrop.mlua` 판정 + `Inventory/DropItem.mlua`
  바닥 아이템 — 밟으면 줍고 30초 뒤 사라진다. LUK이 드랍률에 비례 보정) /
  **T13-7 포션 사용 ✅** (`PlayerHit.Heal` + `PlayerInventory.UseItem` —
  가방 창에서 물약 줄을 눌러 마신다. 만피면 소모하지 않는다) /
  **T13-5b 아이템 퀵슬롯 ✅** (`ui/ItemQuickSlot.ui` + `UI/ItemQuickSlotBar.mlua` —
  화면 오른쪽 아래 2칸, 키 `1`·`2`. 스킬 바와 분리) /
  ~~T13-5c(퀵슬롯 키 재설정 — 키 설정 창에 "아이템 슬롯" 줄)~~ → **2026-08-24 대표 지시로 취소**
  (*"키 설정은 없어도 될거 같아"*). 물약 키는 `1`·`2` 고정으로 간다. **T13 아이템 묶음은 여기서 닫힌다**
- 진행 중: **T15 중복 획득 강화** (지시서 밖). 같은 것을 5개까지 모으면 수치가 쌓인다.
  **T15-1 장비 중복 합산 ✅** (`ItemTable.stack_max` 1→5, `GameData.GetItemStackedStatBonus`,
  `PlayerStats.GetEquipBonus`가 보유 개수만큼 곱한다. 상한에 닿은 아이템은 드랍을 굴리지
  않는다 — 밟아도 버려지면 고장처럼 보인다) /
  **T15-2 스킬 중복 강화 ✅** (`skill_stack_max` 5 · `skill_stack_bonus` 0.2,
  `CombatFormula.StackedCoefficient`, `PlayerAttack.CalcDamage`가 보유 장수로 계수를 만든다.
  만작이면 `PlayerCollection.TryCapture`가 굴리지 않는다. **직업 스킬은 중복이 없으므로
  제외** — `source == "monster"`만 쌓인다)
- 진행 중: **T17 몬스터·스킬 확장** (지시서 밖, 2026-08-16 대표 지시).
  **T17-1 헤네시스 몬스터 5종 ✅** (방마다 다른 몬스터 — 달팽이/파란 달팽이/주황 버섯/뿔버섯/돌 정령) /
  **T17-2 스킬 메타 ✅** (`skill_kind` 공격·방어 분류 + 설명 6컬럼. **방어도 같은 슬롯을 먹는다**) /
  **T17-3a 스킬 판정 ✅** (스킬별 반경 `range` + 대상 수 `max_targets`. 엔진에 최대 타격
  수가 없어 `IsAttackTarget` 훅에서 예산을 깎는다. 실측 단일 1마리 / 범위 5마리) /
  **T17-3b 실드 ✅** (`PlayerHit.ApplyShield`/`GetDamageReduction` + `MonsterAttack.CalcDamage`
  감쇄. 실측 44 → 26 → 만료 후 44. 감쇄율 상한 0.9 — 1.0이면 무적이 된다) /
  **T17-4 스킬 설명 UI ✅** (장착 창에서 줄을 누르면 설명, 한 번 더 누르면 장착.
  `[공격]`/`[방어]` 태그. 계수는 공격 스킬만 — 방어는 계수 0이라 오해를 부른다) /
  **T17-5 방별 수집 진척도 HUD ✅** (`ui/RoomProgress.ui` — 지금 방에서 뭘 얼마나 더
  모아야 하는지. 분모는 데이터에서 유도한다. 마을에서는 숨긴다) /
  **T17-4b 계수 스탯 표기 ✅** (장착 창 줄에 `LUK 계수 1.20`, 설명 줄에 `LUK 0 → 피해 없음`.
  대표 실플레이에서 LUK 0으로 LUK 스킬을 끼운 상태가 나왔다 — 데미지 공식이
  `계수 × 스탯² / (스탯 + DEF)`라 **계수 스탯이 0이면 계수와 무관하게 피해가 0**인데
  그 이유가 화면 어디에도 없었다. `.ui`는 안 고쳤다 — 글자만 늘렸다) /
  **T17-4c 아이템 아이콘 칸 이탈 수정 ✅** (퀵슬롯·가방 아이콘 11곳
  `PreserveSprite` AspectOnly → None. AspectOnly는 비율은 지키지만 **위치를 스프라이트
  자신의 pivot으로 잡아서** 원작 아이템 그림처럼 pivot이 그림 밖인 경우 칸 밖으로 밀려
  그려진다 — RectTransform은 멀쩡해서 좌표로는 안 잡힌다)
  → **T17 묶음은 여기까지 끝.** 남은 것은 T16-3 모험가 NPC(회복 스킬 포함)와
  T16-4 차수·슬롯·레벨 캡 정정이다.
  - 회복 스킬은 **배선만** 돼 있다(`effect_type=heal`). 데이터가 없어서인데,
    **모험가 NPC 몫**이라 T16-3에서 스킬이 생기면 CSV 한 줄로 붙는다.
- 진행 중: **T16 보스·직업 재설계** (지시서 밖, 2026-08-14 대표 지시).
  **T16-1 보스 리젠 + 강화 ✅** (재입장마다 리젠 — `RoomSpawner.CheckBossEntry`가
  방에 있는 사람 목록의 **상승 엣지**를 본다. `UserEnterEvent`는 텔레포트 이동에서
  오지 않아 못 쓴다. 수치는 `boss_*_multiplier` ×3) /
  **T16-2 직업 시험 맵 ✅** (`map/mapjob.map` — map05를 복제해 **16×9=144칸**(원본 448의 약 1/3)로
  줄이고 **돌 계열 타일**(`Henesys_stone`, index 61~117)로 바꿨다. 원본은 흙 계열 0~4.
  `RoomTable.r_job_01`, `SectorConfig`에 `map://mapjob` 등록) /
  **T16-4a 스킬 차수 잠금 ✅** (`SkillTable.tier` + `PlayerSkillSlots.GetTier`(= 환생 횟수 + 1)
  / `CanUseTier`. `EquipMonsterSkill`이 차수 미달을 거절하고, 장착 창은 `[잠김] 2차` ·
  `잠김 — 환생 1회 더`로 이유를 적는다. **보유는 되지만 장착이 안 되는 것**이 핵심 —
  상위 차수를 미리 뺏어 두고 환생으로 여는 것이 환생의 보상이 된다.
  기존 9종은 전부 1차. 실측: 1차에서 2차 장착 거절 → 환생 1회 뒤 장착 성공) /
  **T16-3a 히어로 데이터 ✅** (`MonsterTable`에 **히어로 Lv20**(HP 154 / DEF 21.6 / EXP 31) 1명,
  `SkillTable`에 **히어로 계보 스킬 5종** — 슬래시 블러스트·아이언 바디(1차) / 브랜디쉬(2차) /
  오라 블레이드(3차) / 레이징 블로우(4차). **한 명을 이기면 5개가 한꺼번에 넘어오고,
  차수가 오를 때마다 하나씩 풀린다** — T16-4a 차수 잠금이 여기서 실제로 일한다.
  **`npc_skill_ids` 컬럼 신설** — 격파 시 확정 지급이고 파이프로 여러 개를 준다.
  포획(`drop_skill_id`)과 **칸부터 갈라** 뒀다: 포획은 확률 + 5장 중복, 격파는 확정 + 1장 고정.
  `icon_ruid`에 **원작 스킬 아이콘 RUID**를 확정했다) /
  **T16-3b 히어로 아바타 모델 ✅** (`Models/Monsters/Hero.model` — 몬스터 스프라이트 대신
  **아바타 3종**(`AvatarRenderer`/`CostumeManager`/`AvatarStateAnimation`)을 쓴다.
  모험가 히어로 아머·소드·부츠·헤어를 입혔고 `UseCustomEquipOnly=true`로 계정 코스튬을 막았다.
  `r_job_01`에 배치 — 실측 HP 462 / DEF 64.8 / ATK 312 (보스 ×3)) /
  **T16-3c 격파 지급 ✅** (`PlayerCollection.GrantNpcSkills` — **확정 100% · 1장 고정**.
  `RoomMonster.Dead`가 포획 바로 뒤에 부른다. 이미 가진 스킬은 건너뛰므로 보스가
  재입장마다 다시 서도 두 번째부터는 안 들어온다. 실측: 격파 1회에 5개 전부 →
  재격파 0개 → 차수 1에서 1차만 사용 가능) /
  **T16-4b 슬롯 5칸·차수 축 ✅** (`monster_slot_max` 3 → **5**,
  `RecomputeSlotCount` = `min(차수, 5)`. **보스 클리어 축 제거** — `ClearBoss`는
  원장과 누적 카운터만 적는다(환생 조건·보스 게이트가 그걸 읽는다).
  장착 창 슬롯 3 → 5칸. 실측: 차수 1→슬롯 1 / 보스 클리어해도 그대로 /
  환생 1~4회 → 2·3·4·5칸 / 차수 6에서도 5칸에서 멈춤) /
  **T16-3d 숨은 NPC ✅** (`Models/Npc/HeroNpc.model` + `Room/HeroNpc.mlua` —
  r_04 북서쪽 구석(-11,6)에 히어로가 **아바타로** 서 있고, 두 번 누르면 시험의 방으로
  간다. 첫 터치는 경고만(8초 창) — 네 대면 죽는 방이라 스쳐서 끌려가면 사고다.
  목적지는 `RoomTable`에서 읽는다(맵 이름·좌표를 스크립트에 안 박는다).
  **이걸로 T16 흐름이 닫혔다**: r_04에서 만남 → 시험장 → 격파 → 계보 5종 → 차수로 해금) /
- 진행 중: **T20 스킬 창 재설계** (지시서 밖, 2026-08-23 대표 지시).
  **왜**: 목록이 **5줄뿐**인데 보유 스킬이 9~10개다. 후보를 id 순으로 훑는데
  `s_hero_*`가 `s_mon_*`보다 알파벳 앞이라 **5줄이 전부 히어로로 채워지고
  몬스터 스킬이 밀려난다**(대표 발견: *"모험가의 스킬이 보여지고 몬스터 스킬은 안보이는거 같아"*).
  스크롤도 없다. 팔라딘·다크나이트가 붙으면 25종이 되므로 구조를 바꿔야 한다.
  대표 설계: **아이콘 + 이름만** 보여 주고, 출처를 **버튼 외곽 색**으로 가르고,
  누르면 **상세**를 띄운다 (원작 메이플식).
  **T20-1 몬스터 스킬 아이콘 ✅** (`s_mon_*` 5종의 `icon_ruid`가 비어 있어 아이콘
  격자로 가면 5칸이 빈칸이 된다. 이펙트를 빌려온 것과 **같은 방식**으로 비슷한
  원작 스킬의 아이콘을 넣었다 — 썸네일을 받아 눈으로 골랐다) /
  **T20-2 출처 탭 ✅** (대표 정정: *"몬스터랑 전직 캐릭터의 스킬만 분리해서 볼 수
  있도록... 일단은"* — 아이콘 격자 대신 **탭**까지만. 그것만으로 안 보이던 문제가
  풀린다: 출처로 가르면 각 5개씩이라 지금 줄 수에 들어간다) /
  **T20-3 아이콘 격자 + 스크롤 ✅** (대표 지시: *"스킬 개수가 넘칠때 스크롤로
  내려갈 수 있게 하고 메뉴 좀 개편... 최근 원작과 스타일을 같게"*.
  글자 5줄 → **4열 아이콘 격자**. 한 화면에 12칸, 넘치면 스크롤.
  칸에는 **아이콘과 이름만**, 나머지는 눌렀을 때 아래 상세로) /
  **T20-6 스킬 아이콘을 직접 그린다 ✅** (대표 판정: *"저런 형식으로 가자 기존
  메이플스토리의 스킬과 같이 별다를게 없네"*. 원작에는 **몬스터 스킬 아이콘이라는
  것 자체가 없어** 검색으로는 더 못 올린다 — 몬스터는 스킬을 쓰지만 아이콘을 갖지 않는다.
  그래서 5종을 **직접 그렸다**: 껍질+속도선+충격 / 껍질+방어막 / 버섯+포자 구름 /
  뿔+충격 / 바위+포물선 자취. 원작 스킬 아이콘과 같은 **밝은 판(plate)**을 깔아
  "재료 아이템"이 아니라 "스킬"로 읽힌다. 원본은 `docs/art/skill-icons/`에 있다) /
  **T20-5 아이콘을 "스킬이 쓰는 부위"로 ✅** (대표 지시: *"스킬이 개성있게 아이콘이
  있어야되긴 할거 같은데 카드 말고 좋은 대체재가 있을까?"*. T20-4의 몬스터 카드는
  **넷이 전부 같은 빨간 액자**라 아이콘 크기로 줄이면 구분이 안 됐다 — 출처는 나르지만
  **무슨 스킬인지는 못 나른다.** 원작 몬스터 드랍품(ETC)에서 **그 스킬이 실제로 쓰는
  부위**를 찾아 입혔다: 몸통 박치기=달팽이의 껍질(청록) / 푸른 껍질=파란 달팽이의
  껍질(파랑) / 포자 살포=버섯의 포자(크림) / 뿔 들이받기=뿔(주황) / 바위 던지기=바위(회색).
  다섯이 **모양도 색도 전부 다르고**, 각각이 스킬이 쓰는 물건이다) /
  **T20-4 몬스터 스킬 아이콘 교체 ✅** (대표 지시: *"보스 몬스터는 사용하는 스킬을
  아이콘으로 넣어주고 일반 몬스터의 스킬은 좀 기존 스킬이 아닌거로 느낌 가져가면"*.
  T20-1에서 **플레이어 직업 스킬 아이콘을 빌려 입혔던 것**을 벗겼다 — 글자 목록일
  때는 안 보였는데 격자로 바꾸자 아이콘이 칸의 주인공이 되면서 몬스터 스킬이
  사람의 기술처럼 보였고, 진짜 원작 아이콘을 입은 히어로 5종과 같은 계열로 읽혔다.
  일반 몬스터 4종은 **그 종의 몬스터 카드**(달팽이/파란 달팽이/주황버섯/뿔버섯),
  바위 던지기는 **실제로 날아가는 그 바위 그림**을 쓴다) /
  다음: 대표 플레이 판정
  - **갈림선은 "보스냐"가 아니라 "원작에 실존하는 스킬이냐"다.** 히어로 5종이 원작
    아이콘을 입는 것은 보스라서가 아니라 원작에 실제로 있는 스킬이기 때문이다.
    보스로 가르면 보스가 늘 때마다 "이 보스 스킬은 원작에 없는데 어디서 빌리지"가 다시 생긴다.
  - ⚠ **아이콘 비율은 데이터다** (`SkillTable.icon_ratio`). 몬스터 5종은 직접 그려
    56x56(1.0)이고 원작 스킬 아이콘은 32x32(1.0)다. 다른 비율의 그림을 한 크기에
    밀어 넣으면 찌그러진다 — 아이콘을 갈면 비율도 같이 갈 것.
  - **28x28 격자로 그려 56x56으로 뽑는다.** 게임의 아이콘 칸이 정확히 56px이라
    **확대·축소가 한 번도 안 일어난다** (실측 `rect=56.0x56.0`).
  - ⚠ **`asset_update_resource_storage_info`는 생략한 필드를 지운다.** `properties`만
    넘겼더니 name·description·subcategory가 전부 빈 문자열이 됐다 (실측). 넷을 항상 같이 넘길 것.
  - ⚠ **`msw-painter`의 `render.cjs`를 못 쓴다.** 스킬 폴더에 `package-lock.json`이 없어
    `npm ci`가 실패한다. 200MB짜리 Chromium을 깔지 않고 `<rect>`만 읽어 직접 찍는
    `rasterize.py`를 뒀다 (`docs/art/skill-icons/`). **아이콘을 갈면 비율도 같이 갈 것** —
    `GameDataVerify`가 아이콘은 있는데 비율이 없는 줄을 잡는다.
  - ⚠ **런타임에 크기를 재려다 실패했다.** `SetNativeSize()`는 **다음 프레임에야**
    RectSize에 들어오고(실측: 직후 999x999 → 0.1초 뒤 27x38), 자원이 아직 안 올라온
    첫 회에는 **18x18**을 문다. 그걸 캐시했더니 그 뒤로 계속 정사각으로 그려졌다.
    입력과 출력이 RectSize 한 칸을 같이 쓰는 것이 원인이라 데이터로 옮겼다.
  - ⚠ **`PreserveSprite=AspectOnly`는 여기서도 못 쓴다** (T17-4c 재확인). 비율은
    지키지만 위치를 스프라이트 자신의 pivot으로 잡아 그림이 칸 위로 반쯤 튀어나오고,
    pivot을 가운데로 옮기면 아예 사라진다 (실측).
  - ⚠ **이펙트는 안 건드렸다.** 몬스터 스킬의 시전 이펙트는 여전히 비슷한 원작 스킬에서
    빌려온 것이다(돌진/워터실드/포이즌 미스트/아머 피어싱). 아이콘과 달리 이펙트는
    T19-2에서 대표가 플레이로 확인한 뒤 그대로 둔 것이라 지시 없이 바꾸지 않았다.
  - ⚠ **이름 없는 카드가 있다.** 뿔버섯 카드는 이름이 `몬스터리 4/뿔버섯 카드`로 붙어
    있어 "뿔버섯 카드"로는 안 걸렸다. 카드 규격(28x40)으로 좁혀 훑어 찾았다.
  - ⚠ **탭을 바꿀 때 스크롤을 위로 되돌려야 한다.** 아래까지 내린 상태에서 항목이
    적은 탭으로 옮기면 짧아진 목록의 **빈 아래쪽**을 계속 봐서 화면이 텅 빈다 (실측).
    창을 열 때도 같은 이유로 되돌린다.
  - ⚠ **0이 위다** — `VerticalScrollBarDirection`을 TopToBottom(3)으로 뒀기 때문.
    엔진 기본(BottomToTop)에서는 반대라 `runtime-patterns` 4의 "0.0 = 맨 아래"와 어긋난다.
  - ⚠ **`scrollLayout()`은 스프라이트를 안 만든다.** Mask는 자를 그림이 있어야
    동작하므로 `SpriteGUIRendererComponent`를 따로 붙여야 한다.
  - ⚠ **격자 칸에 `ui_lint` L023이 190건 뜨는 것은 오탐이다.** 칸의 `.ui` 좌표가
    전부 (0,0)이고 **런타임에 레이아웃이 배치**하는데 오프라인 lint는 그걸 모른다.
  - ~~⚠ 한 출처가 5개를 넘으면 여전히 잘린다.~~ → **2026-08-28 확인: 낡은 메모다.**
    T20-3이 글자 5줄을 **20칸 격자 + 스크롤**로 바꿔 놓았다(`.ui`에 Cell1~20,
    `EquipPanel.cellCount = 20`, 실측 일치). 지금 몬스터 12종 · 모험가 5종이고,
    **팔라딘·다크나이트를 붙여 15종이 돼도 들어간다.** 잘리는 문턱은 5가 아니라 20이다
    (`EquipPanel.Refresh`가 20을 넘을 때 경고 로그를 남긴다).
  - ⚠ **새 UI 요소는 기존과 pivot을 맞춰야 한다.** `EquipWindow`의 자식은 전부
    top-center(pivot y=1)인데 빌더 기본값은 0.5라, 같은 y값이 **창 중앙 기준**으로
    읽혀 탭이 Row4/Row5 자리에 떨어졌다 (`ui_lint` L023이 잡았다).
  - ⚠ **그라데이션 테두리는 엔진에 없다.** `SpriteGUIRendererComponent`에는 단색
    `Color`뿐이고 그라데이션 속성이 없다. 내려면 **그라데이션이 그려진 스프라이트**를
    찾아 테두리로 깔아야 한다 — T20-2에서 확인한다.
- 진행 중: **T36 area_03 엘리니아** (지역 20칸의 2번째 · Lv11~20).
  **T36-1 데이터 계층 ✅** (몬스터 3종 + `area_03` + 방 3개 `r_21`~`r_23`) /
  **T36-2 맵·모델 ✅ — 엘리니아가 실제로 열렸다** (맵 3개 + 몬스터 모델 3종 +
  `LandmarkTable` Lv20. 실측: **세 방 전부 진입 OK · 슬라임 7 / 리프 퍼펫트리 7 / 페어리 8마리**
  스폰 · 전투·EXP 정상 · 미니맵 `엘리니아 방 3개`) /
  **T36-3 5방 완성 ✅** (`r_24` 동쪽 숲(참새 Lv18) + `r_25` 요정의 정원(로얄 페어리 보스).
  **이걸로 area_03 엘리니아가 닫혔다** — 5방 = area_01과 같은 규격이다.
  실측: 참새 8마리 · **보스 배율 정확**(HP 794→2382 ×3 / DEF 441 ×1 / ATK 376→564 ×1.5 /
  EXP 31→93 ×3) · 보스 재등장 작동 · 미니맵 `엘리니아 방 5개`) /
  다음: **T37-2 아이콘 5종** → T37-3 이펙트·소리
- **T37-3 이펙트 5종 + 소리 ✅** (T21-1/T32-3과 **같은 파이프라인** — 48x48 격자 →
  96x96 출력, 한 장을 `SkillCastEffect`가 움직인다. **스타일은 새로 안 만들었다**:
  기존 넷(burst/ring/cloud/pierce)에 물렸다. **이걸로 T37 엘리니아 묶음이 닫힌다.**
  실측: **파우스트 보스가 어둠의 손아귀를 실제로 쓰고 이펙트가 떴다**
  (`[이펙트] 어둠의 손아귀 (burst)` + 1명 명중) · 5종 전부 월드에 그려지는 것을
  화면으로 확인 · `[스킬이펙트] 22/22종에 시전 이펙트가 있다` · 빌드 에러 0) /
  다음: 대표 플레이 판정
  - ⚠⚠ **같은 스타일을 쓰면 실루엣을 갈라야 한다.** burst가 이미 넷인데 다섯째를 또
    8방향 방사형으로 그렸더니 슬라임·물방울·손아귀가 서로도, 기존 뼈 무덤과도
    **색만 다른 같은 그림**이 됐다 (실측). 성게 / 눈꽃 / 강철 가죽과 같은 고리로 보였다.
    각각 **치우친 덩어리 + 흘러내리는 가닥** / **이어지지 않은 방울** /
    **열두 조각으로 자른 통나무 방책** / **넷으로 줄인 굵은 갈고리**로 다시 그렸다.
  - ⚠⚠⚠ **눈으로 "보인다"고 판단한 것이 틀렸다 — 재야 한다.** 화면으로 보고 넘겼는데
    실측하니 **세 종의 주된 색이 배경과 명도차 10~17**이었다(끈적한 몸통 10.6 /
    물방울 9.8 / 요정의 인분 17.0). 테두리(차 125)만으로 버티고 있었다.
    기준은 **고쳐진 뒤의 포자 살포**(47.6 / 70.4)이고, **면적 12%가 넘는 색은 잔디·흙
    양쪽 모두에 대해 명도차 30 이상**이어야 한다. 셋을 고쳐 46~56으로 올렸다.
    배경색은 게임 화면에서 뽑았다 — 잔디 `(132,174,27)` lum 154.5 / 흙 `(168,128,62)` lum 131.7.
  - ⚠ **덩어리를 겹칠 때는 테두리를 전부 먼저 깔고 본체를 전부 나중에 칠한다.**
    덩어리마다 테두리→본체를 짝으로 그리면 뒤 것의 테두리가 앞 것 위에 그어져
    **한 덩어리가 아니라 금이 간 것처럼** 보인다 (실측).
  - **그림만 고칠 때는 `asset_update_resource_storage_data`를 쓴다** — RUID가 유지되므로
    `SkillTable`을 안 고쳐도 되고 pivot·filter_mode 같은 속성도 남는다 (실측으로 확인).
  - ⚠ **소리는 이번에도 새로 안 찾았다** (T32-3과 같은 이유 — 들어볼 수가 없다).
    이미 쓰이던 소리를 성격이 같은 것끼리 물렸다: 끈적한 몸통←돌진 / 단단한 밑동←로얄 가드 /
    물방울←바위 던지기 / 요정의 인분←포이즌 미스트 / 어둠의 손아귀←레이징 블로우.
    **바꾸려면 CSV 한 칸이다.**
  - **`SkillCastEffect`는 한 줄도 안 고쳤다** — 스타일을 안 늘렸으므로 CSV만으로 끝났다.
- **T37-2 아이콘 5종 ✅** (T20-6/T32-2와 **같은 파이프라인** — 28x28 논리 격자 →
  56x56 출력, 공유 판(plate), chunky 규칙. `draw-icons.cjs`에 이어 그리고 `rasterize.py`로
  찍어 `sprite/skill`로 올렸다. **이걸로 스킬 22종 전부 아이콘이 있다.**
  실측: 장착 창 격자에 5종이 제 비율로 뜨고, `[스킬아이콘] 22종 전부 아이콘이 있다`) /
  다음: **T37-3 이펙트·소리**
  - ⚠⚠ **또 여기서 걸렸다 — 5종 중 4종이 다른 물건으로 보였다.** T32-2에서 7종 중 4종이
    안 읽혔다고 적어 뒀는데 이번에도 같은 비율이다. **단단한 밑동 → 솥**(두 번) ·
    **물방울 터뜨리기 → 보석** · **어둠의 손아귀 → 벌레 → 촛불 케이크 → 빨간 보석 박은
    덩어리**(세 번). 기존 12종과 **나란히 놓은 대조 시트를 4배**로 뽑아 보고 다시 그렸고,
    세부는 **8배**로 한 번 더 봤다. **그리고 나서 보는 것이 이 작업의 절반이다.**
  - **28칸에서 손·그루터기는 실루엣으로만 읽힌다.** 굵기·마디·기운을 더할수록 덩어리가 된다.
    손아귀는 손가락을 **셋으로 줄이고 손목을 화면 밖으로 빼서야** 손이 됐다 — 손목이 없으면
    아래 덩어리가 몸통이 되어 거미로 읽힌다. 그루터기는 **비스듬한 각도를 버리고 단면을
    정면으로** 놓아야 나무가 됐다 (어두운 몸통 + 밝은 윗면 타원 + 아래 뿌리는 어떻게 그려도 솥이다).
  - ⚠ **동심원은 폭만 깎으면 안쪽이 네모가 된다.** 가로세로를 같은 비율로 줄여야 한다
    (반지름을 정수로 빼다가 가운데 고리가 막대가 됐다).
  - **색을 미리 갈라 놓고 그렸다** (T32-2와 같은 방식). 헤네시스 5종이 청록/파랑/주황+초록/
    베이지/회색, 페리온 7종이 호박+은/보라/적갈/철청+금/아이보리+자주/주황빨강/황토라
    엘리니아는 **연두 · 밤색+나이테 · 밝은 청록 · 분홍 · 검정+진홍**으로 갈랐다.
    특히 **끈적한 몸통(연두 젤리)과 몸통 박치기(청록 껍질)가 격자에서 나란히 서는데**
    몸도 색도 달라 안 헷갈린다 (실측 화면으로 확인).
  - ⚠ **`asset_update_resource_storage_info`에 네 필드를 항상 같이 넘겼다** (T20-6의 경고 —
    생략한 필드는 지워진다). name·description·subcategory·properties.
  - ⚠ **`contentLength`는 실제 파일 크기여야 한다.** presigned URL이 그 값을 서명에 넣기
    때문에 어림짐작으로 넣으면 PUT이 거절된다 (첫 회에 465로 넣었다가 실제 618이었다).
  - ⚠ **`EquipPanel`은 `@Component`가 아니라 `@Logic`이다** — `_EquipPanel:SetOpen(true)`로
    연다. 엔티티에서 `GetComponent("EquipPanel")`을 찾으면 nil이다 (검증하다 한 번 헛짚었다).
    `StatPanel`·`SkillBar`도 같다 — UI 엔티티의 `@Component`는 Server 메서드를 못 갖는다.
- **T37-1b 몬스터 3종 정정 ✅** (대표 지적: *"엘리니아를 다시 검색해봐 몬스터 다른 애들이야"*.
  맞았다 — **리프 퍼펫트리·참새·로얄 페어리가 엘리니아 몬스터가 아니었다.**
  다크 스텀프·버블링·파우스트로 갈고 방 이름도 원작 맵으로 고쳤다.
  실측: 다섯 방 전부 정상, 파우스트가 [어둠의 손아귀]를 실제로 썼다) /
  다음: **T37-3 이펙트·소리**
  - ⚠⚠⚠ **나무위키 요약본을 교차 확인 없이 믿은 것이 원인이다.** 이 세션에서 같은 요약본이
    이미 *"111~120에 예티" · "141~150에 고양이"* 같은 오류를 냈는데도 몬스터 목록은 안 걸렀다.
    **id 대역만 봤어도 잡혔다**: 리프 퍼펫트리 `mob/9300423`(93xxxxx = 이벤트) ·
    참새 `mob/2400202`(24xxxxx = 그란디스). **앞으로 몬스터를 고르면 팩 id 대역을 같이 볼 것.**
  - **정본은 「엘리니아/빅뱅 이전」이다** (지역 문서). 맵별 몬스터가 레벨과 함께 적혀 있다:
    스텀프 4 / 슬라임 6 / **다크 스텀프 10** / 초록버섯·**버블링** 15 / 엑스텀프 17 /
    파란버섯 20 / 뿔버섯 22 / 이블아이 27 / **페어리 30** / 커즈아이 35 / 루팡 37 /
    콜드아이 40 / **파우스트 50**.
  - **방 이름도 내가 지어낸 것이었다.** 요정의 숲·동쪽 숲·요정의 정원은 원작에 없다 →
    엘리니아 남쪽 숲 · 솟아오른 나무 · 남쪽 숲 나무던전 · 북쪽 숲 나무던전 · 사악한 기운의 숲.
  - **파우스트 팩에는 몸 동작이 아닌 자산이 섞여 있다** — [7] 바나나(투사체) · [8] 노란 폭발
    (타격 이펙트). 대조 시트로 골라낼 때 **몸 동작만 세야 한다**(9개 중 7개).
- 진행 중: **T37 엘리니아 포획 스킬** (T32와 같은 3단계).
  **T37-1 스킬 5종 데이터 + 포획 연결 ✅** (`SkillTable` 5줄 + `MonsterTable`의 빈
  `drop_skill_id` 5칸. **이걸로 엘리니아가 포획되는 지역이 됐다.**
  실측: 스킬 22개(몬스터 17/NPC 5) 로드 · 보스가 **요정의 빛을 실제로 썼다**(계수 1.55
  반경 2.67 → 1명 명중, 데미지 298) · **포획 성공 로얄 페어리 → 신규 스킬 [요정의 빛]**) /
  다음: **T37-2 아이콘 5종**
  - **계수를 사다리로 놨다**: area_01(1.0~1.35) < **엘리니아(1.15~1.55)** < 페리온(1.3~2.0).
    엘리니아가 Lv11~20이라 두 지역 **사이**에 들어간다.
    | 스킬 | 몬스터 | 계수 | 성격 | 쿨 |
    |---|---|---|---|---|
    | 끈적한 몸통 | 슬라임 11 | ATK 1.15 | 단일 근접 | 4 |
    | 나무껍질 | 리프 퍼펫트리 14 | — | **실드 45% / 5초** | 8 |
    | 요정의 인분 | 페어리 16 | **INT** 1.35 | 범위 3.5 | 5 |
    | 날개치기 | 참새 18 | ATK 1.4 | 범위 3.0 **최대 3명** | 5 |
    | 요정의 빛 | 로얄 페어리 20 | **INT** 1.55 | 범위 4.0 | 6 |
  - **INT 스킬을 둘 넣었다.** 요정 계보라 결이 맞고, T27이 "INT 빌드가 얇다"고
    남긴 자리를 메운다 — 지금까지 INT 계수 스킬은 포자 살포·어둠의 뿌리 둘뿐이었다.
  - **`max_targets` 제한을 두 번째로 썼다** (날개치기 3명). 뼈 무덤(2명/계수 2.0)보다
    **많이 맞히되 약하게** — 같은 축의 반대편이라 둘이 서로를 설명한다.
  - **투사체는 이번에도 안 만들었다** (T32-1과 같은 이유). `projectile_ruid`가 비면
    판정 방식 자체가 갈리고(T19-4), 스프라이트는 이름이 안 붙어 눈으로 골라야 한다.
    반쯤 만든 기계를 남기지 않는다.
  - **회복 스킬은 이번에도 안 넣었다** — `effect_type=heal`은 **모험가 NPC 몫**이라는
    기록된 설계를 지킨다.
  - ⚠ **아이콘이 없어 장착 창에서 빈 칸이 된다** (`[스킬아이콘] 아이콘 없음: 5종`).
    T37-2에서 직접 그린다 — T20-6/T32-2와 같은 파이프라인(28x28 → 56x56).
  - **대조 시트 방법이 두 번째로 통했다.** 참새·로얄 페어리 둘 다 클립이 **4개**로
    리프 퍼펫트리와 같은 꼴(die[0] / hit[1] / move[2] / stand[3])이었다.
    **셋 다 전용 공격 클립이 없다** — `attack`에 `stand`를 넣었다.
  - ⚠ **map23을 다시 복제했다.** T36-2에서 `Portal_E`를 지웠는데 `r_24`가 붙으면서
    되살려야 했다. **포탈을 새로 놓으면 RUID가 비어 사라지므로**(T26-1) `map01`에서
    다시 복제하는 편이 안전하다. 맵에 UUID를 참조하는 곳이 없어 안전하다.
  - **보스방은 `map05`를 복제했다.** `Portal_Return`이 필요한데 그게 있는 맵이 보스방뿐이다.
    `room_type=boss` 한 칸으로 배율·재등장·귀환 포탈이 전부 따라온다.
  - **원작 레벨을 우리 사다리에 맞췄다.** 참새 원작 Lv20 → 우리 18, 로얄 페어리 원작
    Lv15 → 우리 20(보스라 위여야 한다). 지역 대역(Lv11~20)이 우선이다.
  - ⚠⚠ **클립에 이름이 안 붙는 문제를 푸는 방법을 찾았다.** 리소스 팩의 `animationclip`은
    이름 없이 나오는데(T19-3), `getResource(ruid).payload.thumbnail`이 **GIF 주소**를 준다.
    받아서 프레임을 가로로 편 **대조 시트**를 만들면 눈으로 읽힌다. **정답을 아는 몬스터를
    대조군으로 같이 넣는 것**이 핵심이다 — 달팽이로 검증하고 나머지를 읽었다.
    시각 신호: **die는 프레임이 많고 몸이 무너지거나 사라진다 / move는 걷기·비행 순환 /
    hit은 1프레임 찡그림 / stand는 맨 그림이고 팩의 마지막**이다.
  - **클립 순서는 알파벳순이다** (attack < die < hit < jump < move < stand). 네 팩에서 성립했다.
    다만 팩마다 클립 수가 달라(9/6/4/5) 순서만으로는 확정할 수 없다 — **시트와 같이 봐야 한다.**
  - **전용 공격 클립이 없는 몬스터가 있다.** 리프 퍼펫트리는 4개(die/hit/move/stand)뿐이라
    `attack`에 `stand`를 넣었다. 주황버섯이 `attack`에 `jump`를 넣은 것과 같은 처리다 —
    원작에도 공격 동작이 없으므로 이게 맞다.
  - **모델은 `Snail.model`을 복제해 RUID만 갈았다.** 이 프로젝트는 Pattern B(AIChase +
    ActionSheet)이고, 새로 조립하는 것보다 **이미 도는 구성**을 물려받는 편이 안전하다.
    컴포넌트 13개가 그대로 따라온다.
  - **엘리니아는 타일을 다시 안 칠했다.** 숲이라 area_01의 잔디가 그대로 맞는다 —
    페리온(T26-4)이 돌로 다시 칠해야 했던 것과 다르다. `map01`·`map02`를 복제했다.
  - ⚠ **포탈은 지우기만 했다.** 새로 놓으면 `openRUID`/`lockedRUID`가 비어 **화면에서
    사라진다**(T26-1). map21은 map02에서 `Portal_W`만, map23은 map01에서 `Portal_E`만 지웠다.
  - ⚠ **`RectTileMapComponent를 찾지 못함` 경고는 복제 탓이 아니다.** 대조로 확인했다 —
    **기존 `map01`·`map12`에서도 똑같이 난다.** `OnMapEnter`가 클라의 타일맵보다 먼저 도는
    경합이고, 곧이어 `OnBeginPlay`가 448칸을 제대로 재서 카메라를 적용한다.
  - ⚠ `LWA-3019` 포탈 경고도 마찬가지로 기존 맵에서 상속됐다. 새 맵 3개만큼 늘었다.
  - **원작 레벨이 우리 대역과 거의 그대로 맞는다.** 엘리니아 몬스터가 슬라임 7 ·
    리프 퍼펫트리 10 · 페어리 13 · 로얄 페어리 15 · 참새 20이라 Lv11~20에 자연스럽게 얹힌다 —
    미나르숲처럼 **올릴 필요가 없었다**. 방 이름도 원작 맵에서 그대로 가져왔다
    (숲으로 가는 길 · 요정의 숲 · 솟아오른 나무) — T26-5의 "방과 몬스터는 같은 출처에서" 규칙.
  - ⚠⚠ **랜드마크를 일부러 안 넣었다.** `LandmarkTable`에 `20,area,area_03`을 넣으면
    Lv20에 환생한 사람이 **맵이 없는 지역으로 들어간다.** `default_unlocked=false`이고
    주는 곳이 없으므로 지금 area_03은 **닫혀 있다**. T36-2에서 맵과 함께 연다.
  - ⚠⚠ **몬스터 EXP는 내림이 아니라 반올림이다.** 내림으로 넣었더니 `GameDataVerify`가
    Lv11(12 vs 13)·Lv16(20 vs 21)을 **[공식 불일치!]**로 잡았다. Lv14는 우연히 같아서
    안 걸렸다 — **셋 중 하나만 맞아도 공식이 맞는 줄 착각한다.** 검산: 스텀피 Lv42가
    248.9 → 표에 **249**로 들어가 있다(반올림).
  - ⚠⚠⚠ **마을은 방향이 넷뿐이라 지역 20칸이 area_06에서 막힌다.**
    `GameDataVerify.VerifyRooms`가 **양방향을 강제**하는데, 지역 진입 방이 마을로
    걸어 나가려면 `r_town.conn_{방향}`에 한 칸을 써야 한다. 지금 N=페리온 · E=area_01 ·
    **S=엘리니아**(이번에 씀)라 **W 하나만 남았다.** area_05까지는 되고 **area_06이 벽**이다.
    풀려면 (a) 마을(`room_type=town`)을 향하는 링크는 역방향 검사를 면제하거나
    (b) 조건 없는 귀환 포탈을 두는 것이다 — `ReturnPortal`은 **그 방의 보스를 잡아야**
    열려서 진입 방에는 그대로 못 쓴다. **지시 없이 안 건드렸다.**
  - ⚠ **애니메이션 클립 식별이 T36-2의 관문이다.** 리소스 팩의 클립에는 **이름이 안 붙는다**
    (T19-3과 같은 문제). 대조군(달팽이, 정답을 아는 모델)으로 확인한 것:
    **순서는 알파벳순이고 `stand`가 마지막**이다(달팽이 [8]/9개, 슬라임 `50faf654`도 [5]/6개).
    다만 팩마다 클립 수가 달라(슬라임 6 · 리프 퍼펫트리 4 · 페어리 5) `attack`/`die`/`hit`
    대응은 **프레임 수만으로 확정할 수 없다** — 틀리면 "공격했는데 죽는 동작이 나오는"
    조용한 실패가 된다. 썸네일을 받아 눈으로 확인해야 한다.
  - **모델은 `Snail.model` 구성을 그대로 따른다** (컴포넌트 13개 · `ActionSheet`에
    stand/move/attack/hit/die 5키 · `SpriteRUID` = stand 클립 · `MonsterId`).
- 진행 중: **T35 지역 선택 창 20칸 스크롤** (T34의 지역 20칸을 담을 그릇).
  **✅ 칸 4개 → 20칸 + 스크롤** (`ui/AreaSelect.ui`의 `Slot0~3`을 걷어내고
  `Box/List` 스크롤 목록 + `Slot1~20`. `AreaSelectPanel`은 슬롯 프로퍼티 4개 대신
  **뿌리 하나**(`listRoot`)만 잡고 이름으로 찾는다.
  실측: 칸 20/20 이름으로 잡힘 · 지역 2곳→2켜짐/18꺼짐 · 20곳→20켜짐 ·
  **21곳→경고 뜨고 20개만**) /
  다음: 지역당 방 수를 정하고 area_03(엘리니아 Lv11~20)부터 제작
  - ⚠⚠ **이걸 안 고치면 5번째 지역부터 조용히 안 뜬다.** `Slot0`~`Slot3` 넷뿐이었고
    `FillSlot`이 목록에 있는 만큼만 켜는 구조라 **에러도 로그도 없이** 사라졌다.
    T34에서 지역 20칸을 확정한 순간 이게 선행 조건이 됐다.
  - **T20-3 스킬 격자와 같은 방식으로 맞췄다** — `ScrollLayoutGroup`(Grid, 1열) + `Mask`,
    칸을 `.ui`에 미리 20개 만들어 두고 스크립트가 켜고 끈다. **GridView(가상화)를 안 쓴다**:
    칸이 재사용되면 스크롤할 때마다 클릭 핸들러를 다시 걸어야 해서 새기 쉽다.
    지역은 20개로 확정이라 실제 엔티티가 싸고 안전하다.
  - **칸을 프로퍼티로 늘어놓지 않는다.** 20개를 박으면 `.ui`를 고칠 때마다 UUID 20개를
    다시 맞춰야 한다. `listRoot` 하나만 바인딩하고 `GetChildByName("Slot"..i)`로 찾는다.
  - ⚠ **`scrollLayout()`은 스프라이트를 안 만든다** (T20-3 재확인). `Mask`는 자를 그림이
    있어야 동작하므로 `SpriteGUIRendererComponent`를 따로 붙였다.
  - ⚠ **`Constraint`·`ConstraintCount`·`GridSpacing`·`ScrollBarVisible`은 빌더 시그니처
    밖이다** — `patchComponent`로 넣었다 (builder-protocol-ui §3.8).
  - ⚠ **pivot을 기존 자식과 맞췄다** (T24-1 교훈). `Box`의 자식이 전부 top-center(pivot y=1)라
    빌더 기본값 0.5로 두면 같은 y가 **창 중앙 기준**으로 읽혀 엉뚱한 자리에 떨어진다.
  - **넘치면 경고를 남긴다.** 21곳을 밀어 넣으면 `지역이 21곳인데 칸은 20개다 — 뒤가 잘린다`가
    뜬다. 조용히 잘리는 것이 이 태스크가 고친 결함이라 같은 함정을 다시 만들지 않는다.
  - ⚠ **`ui_lint` L023 190건은 오탐이다** (T20-3과 같은 원인·같은 건수). 칸의 `.ui` 좌표가
    전부 (0,0)이고 **런타임에 레이아웃이 배치**하는데 오프라인 lint는 그걸 모른다.
    L011 3건은 INFO이고 기존 `Title`·`BtnClose`에도 붙어 있다.
  - ⚠ **클릭은 실측하지 못했다** (T23-8: 합성 클릭이 UI에 안 먹는다). `Show`/`FillSlot`/
    `Hide`와 칸 조회까지는 실측했고, **실제로 눌러서 이동되는지는 대표 확인이 필요하다.**
- 진행 중: **T34 EXP 곡선 재설계 + 지역 20칸 확정** (2026-08-28 대표 지시).
  **T34-1 곡선 ✅** (`exp_base` 20 → **60**, `exp_ratio` 1.12 → **1.105**. CSV 두 칸이고
  **코드 변경 0줄**이다 — 네 값을 전부 `GameBalance`에서 읽고 있었다.
  실측: Lv47 필요 5927 → 레벨업 → Lv48 다음 필요 6549, 저장 정상) /
  다음: 지역당 방 수 → area_03부터 제작
  - ⚠⚠⚠ **대표 확정: 지역 20칸, 각 10레벨, Lv1~200.** 이게 이 태스크의 근거다.
    | # | 레벨 | 지역 | | # | 레벨 | 지역 |
    |---|---|---|---|---|---|---|
    | 1 | 1~10 | **헤네시스** (있음) | | 11 | 101~110 | 루더스 호수 |
    | 2 | 11~20 | 엘리니아 | | 12 | 111~120 | 루디브리엄 |
    | 3 | 21~30 | **페리온** (있음) | | 13 | 121~130 | 니할 사막 |
    | 4 | 31~40 | 커닝시티 | | 14 | 131~140 | 마가티아 |
    | 5 | 41~50 | 노틸러스 | | 15 | 141~150 | 무릉도원 |
    | 6 | 51~60 | 지하 하수도 | | 16 | 151~160 | 미나르숲 |
    | 7 | 61~70 | 슬리피우드 | | 17 | 161~170 | 시간의 신전 |
    | 8 | 71~80 | 오르비스 | | 18 | 171~180 | 지구방위본부 |
    | 9 | 81~90 | 엘나스 산맥 | | 19 | 181~190 | 미래의 문 |
    | 10 | 91~100 | 아쿠아로드 | | 20 | 191~200 | 기계무덤 |
    Lv1~60이 전부 빅토리아(메이플월드)이고 61부터 슬리피우드로 나간다.
    테마던전(원작 Lv31~50)은 **커닝시티·노틸러스로 바꿨다** — 인스턴스 스토리 던전이라
    방이 포탈로 이어지는 우리 구조에 안 맞는다.
  - ⚠⚠ **곡선을 안 고치면 20칸이 성립하지 않는다.** 옛 곡선(20/1.12)을 20칸에 얹으면
    #1 헤네시스 **4분** / #20 기계무덤 **111분**으로 **30.7배** 편차다. 원인은 필요EXP가
    ×1.12로 오르는데 몬스터EXP는 ×1.10이라 **한 레벨에 잡을 마릿수가 레벨마다 1.8%씩
    복리로 늘어나는 것**이다(Lv1 4마리 → Lv200 144마리). 1.105면 그 복리가 0.45%로 줄어
    **10분 → 24분(2.4배)**이 된다.
  - **완전 평탄(안1)을 안 고른 이유**: `monster_exp_ratio`를 1.12로 맞추면 20지역이 전부
    17분으로 똑같아지는데, **고레벨이 무거워지는 감각이 사라진다** — 숫자가 오르는 성장이
    엔진인 게임에서 그건 잃는 것이 있다. 그리고 그 안만 `MonsterTable` 13종의 `exp`를
    전부 다시 생성해야 한다(T28에서 한 작업).
  - ⚠⚠ **`integer`는 64비트다 — 실측으로 확인했다.** `LevelCurve.RequiredExp`가
    `integer`를 돌려주는데 Lv175부터 필요 EXP가 int32 상한(21억)을 넘는다.
    실측 Lv180 **34.6억** / Lv199 **231억**이 음수 없이 정상이었다. int32였으면
    **Lv175 이후 레벨업이 조용히 깨졌을 것**이다.
  - **저장은 안 넘친다.** `PlayerStats.Exp`는 `number`(double)이고 레벨업 때
    `Exp = Exp - need`로 **깎아서 회차 잔여만 들고 있다** — 누적 2,432억을 저장하지 않는다.
  - ⚠ **게이트는 안 건드려도 된다.** `STAT_TOTAL`은 레벨당 5P라 곡선과 무관하다 —
    170P가 열리는 **레벨**은 그대로고, 거기 **도달하는 시간**만 바뀐다.
  - ⚠ **기존 세이브는 레벨이 그대로이고 다음 레벨만 멀어진다.** Lv47 기준 옛 곡선 3,980 →
    새 곡선 5,927이다. 깨지지는 않는다.
- 진행 중: **T33 정리** (2026-08-28 대표 선택 — 한 회차 플레이 판정 중이라 **밸런스·콘텐츠를**
  **안 건드리는 일**만 골랐다).
  **T33-5 바닥에 깔린 보이지 않는 벽 10칸 ✅** (대표 선택. T26-4에서 발견하고 "지시 밖이라
  안 건드렸다"고 남겨 둔 것. 마을 4칸 · 시험의 방 6칸이 **돌 포장으로 보이는데 못 지나갔다** —
  마을 (-6,1)·(1,1)은 환생 NPC에서 동쪽 문으로 가는 **길 위**였다.
  `Henesys_stone_3`(63) → `Henesys_stone_4`(64).
  실측: 런타임 전수 스윕 **마을 448칸 · 시험의 방 144칸 모두 통과불가 0칸**) /
  다음: 대표 플레이 판정
  - **`stone_3`만 `IsCollidable=true`인 예외였다.** 같은 결의 stone_1·2·4·5는 전부
    통과 가능하다. 두 맵 모두 **63과 64의 칸 수가 같아서**(4/4 · 6/6) 잔무늬 흩뿌림으로
    한 묶음에 뿌린 것이 드러났다 — 64로 바꾸면 결이 안 바뀐다.
  - ⚠⚠ **고칠 곳은 타일셋이 아니라 맵이다.** `.tileset`은 편집 대상이 아니고(AGENTS.md의
    편집 가능 목록은 `.mlua`/`.model`/`.ui`/`.map`뿐), `stone_3`이 원작에서 장애물로
    의도된 그림일 수도 있다. **그걸 바닥으로 깐 쪽**이 잘못이다.
  - ⚠ **런타임 `RectTileInfo.Index`는 JSON보다 1 크다** — `.d.mlua`에 *"It starts at 1"*로
    적혀 있다(실측 JSON 64 → 런타임 idx 65). T26-4에 적어 둔 관찰이 문서로 확인됐다.
  - **타일 칠하기는 빌더 커버리지 밖이라**(builder-protocol-map §1.6) `tileIndex` 숫자만
    범위를 좁혀 직접 고쳤다. diff가 **정확히 10줄**이고, 읽기·검산은 `MapBuilder.getTiles()`/
    `getTileAt()`으로 했다.
  **T33-4 죽은 `Slime.model` 삭제 ✅** (대표 지시. T33-3에서 발견한 것.
  `MonsterId = m_slime`이 `MonsterTable`에 없고 어떤 맵도 안 쓴다 — T33-1에서 지운
  `Skeleton.model`과 같은 경우다. `RoomMonster.MonsterId` 기본값도 `"m_slime"` → **빈 값**.
  실측: 엔진이 `[LEA-3028] MissingModel: model://slime가 없습니다` · 몬스터 13종 정상 스폰 ·
  빌드 에러 0) /
  다음: 대표 플레이 판정
  - **이걸로 헤네시스 재테마(2026-08-16) 잔재가 다 걷혔다.** 그때 `MonsterTable`에서
    빠진 두 종(`m_skeleton` · `m_slime`)의 모델이 각각 T33-1과 여기서 사라졌다.
  - **기본값도 같이 고쳤다.** `RoomMonster`가 `MonsterId = "m_slime"`을 기본으로 들고 있어
    값을 안 준 몬스터가 생기면 **실재하지 않는 종 이름을 대며** 에러를 냈다.
    `OnBeginPlay`가 어차피 거절하지만, 로그가 거짓말을 하면 원인 찾는 데 시간을 먹는다.
  - ⚠ **삭제 전 전수 조사는 빌더로 했다.** 맵 15개(`MapBuilder`) · `.model` 26개
    (`ModelBuilder`) · `.mlua` · `.csv` · `.config`/`.directory` 전부 참조 0건.
    T33-2에서 `.model`을 Grep으로 봤다가 오탐을 낸 적이 있어 원문 검색을 안 썼다.
  - ⚠ **`SpawnByModelId`는 없는 모델에 예외를 던지지 않는다.** `pcall`로 감싸면 "성공"이
    돌아온다 — 실제 증거는 로그의 `[LEA-3028] MissingModel`이다. 살아 있는 모델
    (`model://snail`)을 대조군으로 같이 돌려 그 줄이 안 뜨는 것까지 봤다.
  **T33-3 몬스터 공속·추격 범위를 CSV로 ✅** (대표 지시. `monster_attack_interval` 0.5 ·
  `monster_detect_range` 6.0 신설, 14개 `.model`에서 두 값을 걷어냈다.
  실측: 실제 주기 0.5 · **서버 `DetectionRange` 6.0** · 전투 정상) /
  다음: **T33-4에서 지웠다** → 이어서 T33-5
  - **"CSV가 기본값, 모델이 덮어쓴다" 꼴로 넣었다.** 프로퍼티가 **0이면 CSV**를 쓰고,
    값이 있으면 그것이 이긴다(`ResolvedAttackInterval` / `ResolvedDetectRange`).
    그냥 CSV 한 값으로 합치면 **종별로 다르게 둘 여지가 사라지는데**, T28에서
    "난이도는 방이 만든다"고 정한 이상 종별 공속은 그 방을 짜는 지렛대다.
  - ⚠⚠ **`AIChaseComponent`는 네이티브라 "CSV를 읽어라"라고 시킬 수 없다.**
    `MonsterAttack.OnBeginPlay`가 값을 **컴포넌트에 써 넣는다.** `.model`에서 값을 지웠으므로
    **이 줄을 지우면 추격 거리가 엔진 기본값 5.0으로 조용히 바뀐다.**
  - ⚠⚠⚠ **네이티브 컴포넌트 값을 클라에서 읽어 검증하면 틀린 답이 나온다.**
    클라에서 `DetectionRange`를 읽었더니 **5.0**(엔진 기본값)이라 안 먹은 줄 알았는데,
    서버에서 읽으니 **6.0**이었다. `DetectionRange`는 클라로 동기화되지 않고
    **추격 판정은 서버가 한다** — 실제로 도는 값은 6.0이다.
    `maker_execute_script`의 `context: server_main`으로 확인할 것.
  - ~~⚠ `Slime.model`도 죽은 모델이다~~ → **T33-4에서 지웠다.**
  **T33-2 템플릿 잔재 일괄 정리 ✅** (대표 지시 *"템플릿 맵까지 묶어서 다 정리하자"*.
  **27개 파일 삭제** — 스크립트 11쌍(`.mlua`+`.codeblock`) · `.stateset` 2 · 템플릿 맵 3.
  `Global/` 모델 2개는 삭제 대신 **편집**했다.
  실측: **게임 맵 15/15 정상 진입** · 몬스터 13종 정상 · 빌드 에러 0) /
  다음: 대표 판단 — 몬스터 공속·추격 범위를 CSV로 옮길지
  - **`Global/`은 지울 수 없어 고쳤다.** AGENTS.md가 `Global/` **삭제는 금지, 편집은 허용**으로
    갈라 두었다. `ChaseMonster.model`·`MoveMonster.model`에서 사라질 스크립트를 참조하던
    `script.Monster`·`script.State*Monster`만 `ModelBuilder`로 떼어냈다 —
    `StateSetId` 값도 같이 빠졌다. **`script.MonsterAttack`은 우리 게임이 쓰므로 남겼다.**
  - ⚠ **`ModelBuilder.write()`는 기본으로 `SpriteRUID` 자리표시자를 주입한다.** 그냥 썼더니
    `Global/` 모델에 **없던 값이 생겼다.** 되돌리고 `{ ensure_sprite_ruid: false }`로 다시 했다.
    `Global/`을 고칠 때는 **의도한 변경만** 들어가야 한다.
  - **템플릿 맵 3개는 `SectorConfig`에 등록조차 안 돼 있었다** (게임 맵 15개만). 그래서
    지워도 끊어진 섹터 항목이 안 남는다 — 지우기 전에 이걸 먼저 확인했다.
  - ⚠⚠ **첫 검증이 틀렸었다.** 맵 15개를 밟는 시험에서 `5/15`가 나와 깨진 줄 알았는데,
    실패 목록이 전부 **직전 맵**을 가리켰다(`map12 실패 → 지금 map11`) — 대기 0.8초가
    맵 로드보다 짧아 한 박자 늦게 읽힌 것이다. **2.5초로 늘리니 15/15**였다.
    **맵 전환을 검증할 때는 로드 시간을 넉넉히 줄 것.**
  - ⚠ **`.model`은 Grep으로 보면 안 된다** (builder-protocol §2.0). 이번 조사 초반에 썼다가
    `script\.Monster` 패턴이 **`script.MonsterAttack`에 걸리는 오탐**을 냈다.
    `ModelBuilder.snapshot()`/`listComponents()`로 봐야 정확하다.
  **T33-1 규칙 위반 · 죽은 자산 · i-frame ✅** (`PlayerAttack.mlua`에 실행공간 주석 /
  죽은 `Skeleton.model` 삭제 / i-frame을 `GameBalance.player_iframe_seconds`로.
  실측: `player_iframe_seconds=1.0` · `GetImmuneCooldown()=1.0`(값 그대로) · 몬스터 13종 정상) /
  다음: 아래 두 가지를 대표가 정해 주실 것
  - ⚠⚠ **인수인계 문서의 정리 후보 두 항목이 낡아 있었다.** 이번에 전수 재조사했다:
    - **"`PlayerAttack`과 `MonsterAttack`에 실행공간 주석이 없다"** → `MonsterAttack`에는
      **원래 있었다.** 빠진 것은 `PlayerAttack` 하나였고 붙였다.
    - **"미사용 잔존 파일 11개가 어떤 모델·맵에도 안 붙어 있다"** → **우리 게임 기준으로만 맞다.**
      **템플릿 잔재 5개가 참조한다**(`Global/ChaseMonster·MoveMonster.model` + 템플릿 맵 3개).
      지금 지우면 거기 끊어진 참조가 남는다.
  - **i-frame을 CSV로 옮긴 이유는 규칙 3만이 아니다.** T28이 "동레벨 12대 맞으면 죽는다"로
    곡선을 세울 때 **i-frame 1초를 전제로 "대수 = 초"라고 읽었다.** 그 전제가 코드에만 있으면
    밸런스를 CSV로 못 굴린다.
  - ~~⚠ 몬스터 공격 주기·추격 범위는 안 옮겼다~~ → **T33-3에서 해소.** "CSV가 기본값,
    모델이 덮어쓴다" 꼴로 넣어 종별 조정 여지를 남겼다.
  - ~~⚠ 잔존 파일 11개도 대표 판단 대기~~ → **T33-2에서 해소.** 27개를 한꺼번에 걷어냈다.
- 진행 중: **T32 페리온 몬스터 스킬** (2026-08-27 대표 지시).
  **T32-3 이펙트 7종 + 소리 ✅** (T21-1과 **같은 파이프라인** — 48x48 격자 → 96x96 출력,
  한 장을 `SkillCastEffect`가 움직인다. **스타일은 새로 안 만들었다**: 기존 넷
  (burst/ring/cloud/pierce)에 물렸다.
  실측: **스텀피 보스가 대지 가르기를 실제로 쓰고 이펙트가 떴다** —
  `CastSkill 계수=1.9 반경=3.33 → 1명 명중` + `SpawnOwnEffect 대지 가르기 (burst)`) /
  다음: 대표 플레이 판정. **이걸로 T32 페리온 묶음이 닫힌다**
  - ⚠⚠ **소리는 새로 안 찾았다 — 들어볼 수가 없어서다.** 스프라이트는 그려서 눈으로
    보고 고쳤지만 오디오는 검색 결과에 이름도 안 붙고 재생해 볼 방법도 없다.
    T19-2의 "아이콘 RUID로 원본 팩 역추적"도 **직접 그린 아이콘에는 안 통한다**
    (역추적할 팩이 없다). 그래서 **이미 쓰이던 소리를 성격이 같은 것끼리 물렸다** —
    실드는 푸른 껍질 소리, 돌진은 몸통 박치기 소리 식이다. 바꾸려면 CSV 한 칸이다.
  - **스타일을 안 늘린 이유**: 넷이 움직임을 충분히 덮는다. 늘리면 `SkillCastEffect.Apply`에
    분기가 하나씩 붙는데, 같은 움직임에 **다른 그림**을 물리는 편이 싸다.
  - **그린 뒤 다시 본 것 둘**: 도끼 호를 왼쪽 아래 중심으로 그렸다가 **가운데로 옮겼다** —
    `SkillCastEffect`는 **가운데(24,24) 기준으로 확대**하므로 쏠린 그림은 커질수록
    시전자에게서 멀어진다. 대지 가르기는 파편을 위아래 대칭으로 흩었다가 **육각 테두리
    장식**처럼 보여 위로만, 크기도 제각각으로 다시 뿌렸다.
  - ⚠ **`SkillEffect.ShowCast`는 클라 스크립트에서 못 부른다** (실측: 호출은 되는데
    이펙트가 안 뜬다). 확인은 **실제로 스킬을 쓰는 경로**로 할 것 — 보스가 자기 스킬을
    쓰는 것(T19-1)이 가장 확실하다.
  **T32-2 아이콘 7종 ✅** (T20-6과 **같은 파이프라인**으로 직접 그렸다 —
  28x28 논리 격자 → 56x56 출력, 공유 판(plate), chunky 규칙. `draw-icons.cjs`에 이어 그리고
  `rasterize.py`로 찍어 `sprite/skill`로 올렸다. 실측: **스킬 17종 중 아이콘 없는 것 0개**) /
  다음: **T32-3 이펙트·소리**
  - ⚠⚠ **그려 놓고 반드시 눈으로 볼 것.** 7종 중 **4종이 안 읽혔다**: 어둠의 뿌리는 가닥이
    붙어 **보라 성문**처럼, 뼈 무덤은 옆 뼈가 **탁자 다리**처럼, 대지 가르기는 땅 덩어리가
    **모자**처럼 보였고 저돌 맹진은 그냥 갈색 덩어리였다. **기존 5종과 나란히 놓고 4배로**
    뽑아 보고 다시 그렸다 — 56px 원본만 보면 판단이 안 된다.
  - **색을 미리 갈라 놓고 그렸다.** 헤네시스 5종이 청록/파랑/주황+초록/베이지/회색이므로
    페리온은 호박+은 / 보라 / 적갈 / 철청+금 / 아이보리+자주 / 주황빨강 / 황토+노랑.
    격자에서 이름을 안 읽어도 갈린다.
  - **`msw-painter`의 `render.cjs`는 이번에도 못 썼다** (T20-6과 같은 이유 — `package-lock.json`이
    없어 `npm ci`가 실패한다). 프로젝트가 가진 `rasterize.py`가 `<rect>`만 읽어 찍는다.
  - ⚠ **`asset_update_resource_storage_info`에 네 필드를 항상 같이 넘겼다** (T20-6의 경고 —
    생략한 필드는 지워진다). name·description·subcategory·properties.
  **T32-1 스킬 7종 데이터 + 포획 연결 ✅** (`SkillTable` 7줄 + `MonsterTable`의 빈
  `drop_skill_id` 7칸. **이걸로 페리온이 포획되는 지역이 됐다** — T26-1이 *"포획이 되는
  지역이 되려면 이 칸을 채워야 한다"*고 남겨 둔 자리다.
  실측: 7종 전부 정확한 수치로 로드, 남쪽 능선에서 `포획 성공 엑스텀프 → 신규 스킬
  [도끼 휘두르기]`) /
  다음: **T32-2 아이콘 7종** → T32-3 이펙트·소리
  - **대표 확인: 지금 콘텐츠는 Lv1~35다** (레벨 캡 200은 자리만 잡아 둔 것이다).
    몬스터 레벨 Lv1~36, 마지막 게이트 `r_14`가 분배 170P = **환생 없으면 Lv35**,
    도달 차수는 **2차까지**(1차 Lv20 · 2차 Lv30), **슬롯 최대 3칸**.
    그래서 이 7종은 **Lv18~42 대역**에만 맞췄다 — 200레벨은 고려하지 않았다.
  - **전부 차수 0이다.** 기존 몬스터 스킬과 같다. 차수를 올렸으면 3차(Lv60) 이후에나
    쓸 수 있어 **콘텐츠가 없는 구간에 묶였을 것**이다.
  - **계수는 사다리로 놨다**: area_01 몬스터 스킬(1.0~1.35) **위**, 히어로 최상위(2.3) **아래**.
    | 스킬 | 몬스터 | 계수 | 성격 | 쿨 |
    |---|---|---|---|---|
    | 도끼 휘두르기 | 엑스텀프 18 | ATK 1.3 | 범위 3.0 | 4 |
    | 어둠의 뿌리 | 다크 엑스텀프 22 | **INT** 1.5 | 범위 4.0 | 5 |
    | 저돌 맹진 | 와일드보어 26 | ATK 1.5 | **돌진 5.0** 단일 | 5 |
    | 강철 가죽 | 아이언 호그 28 | — | **실드 55% / 4초** | 9 |
    | 뼈 무덤 | 스켈레톤 지휘관 30 | ATK **2.0** | 범위 5.0 **최대 2명** | 6 |
    | 화염 돌풍 | 파이어보어 32 | ATK 1.7 | 범위 4.5 | 6 |
    | 대지 가르기 | 스텀피 42 | ATK 1.9 | 범위 5.0 | 7 |
  - **`max_targets` 제한을 처음 썼다** (뼈 무덤). T17-3a에서 만들어 놓고 **아무도 안 쓰던**
    기능이다 — "많이 맞히는 대신 세게"라는 축이 하나 생긴다(그래서 계수만 2.0으로 높다).
  - ⚠⚠ **성격이 겹치는 것은 피할 수 없었다.** 엔진이 지원하는 `effect_type`은
    **damage / shield / heal** 셋뿐이고, damage의 변주(단일·범위·투사체·돌진)를
    기존 5종이 이미 다 쓰고 있다. 7종을 더하면 "더 센 버전"이 될 수밖에 없다 —
    대표가 이걸 알고 **7종 전부**를 골랐다(포획·진척도 HUD가 지역 전체에서 정상화되는 값어치).
  - **회복 스킬은 안 넣었다** (대표 판정). `effect_type=heal`은 배선만 돼 있고 데이터가
    없는 상태 그대로이며, **모험가 NPC 몫**이라는 기록된 설계를 지킨다.
  - ~~⚠ 아이콘·이펙트·소리를 비워 뒀다~~ → **T32-2(아이콘)·T32-3(이펙트·소리)에서 전부 해소.**
  - ⚠ **투사체 스킬은 안 만들었다.** 스켈레톤을 투사체로 두려 했으나 `projectile_ruid`가
    비면 **판정 방식 자체가 갈린다**(T19-4: 투사체가 있으면 착탄 시점 판정이라 비켜설 수
    있고, 없으면 즉시 명중). 반쯤 만든 기계를 남기지 않으려고 `max_targets` 축으로 바꿨다.
    스프라이트는 이름이 안 붙어 나와 눈으로 골라야 한다(T19-3과 같다).
  - ⚠ **발견: 오라 블레이드(3차)·레이징 블로우(4차)는 지금 얻어도 못 낀다.** 3차가 Lv60,
    4차가 Lv100인데 콘텐츠가 Lv35에서 끝난다. 갈아서 올리면 되지만 설계된 경로가 아니다.
- 진행 중: **T31 스탯 이름 변경** (T27 재설계의 마지막 조각. 2026-08-27 대표 지시).
  **✅ 분배 스탯이 STR/DEX/INT/LUK으로 불린다** (`StatAtk`→`StatStr`, `StatDef`→`StatDex`,
  저장 키 `stat_atk`→`stat_str`·`stat_def`→`stat_dex`(하위 호환), `Allocate("ATK")`→`"STR"`,
  스탯 창 라벨 ATK/DEF → STR/DEX).
  실측: **옛 세이브에서 STR 157 / DEX 80이 그대로 살아났고**, 새 키로 쓴 뒤 재접속에도
  STR 158 / DEX 81이 유지된다) /
  다음: 대표 플레이 판정 — **스탯 창이 STR/DEX로 뜨는지** 봐 주실 것
  - ⚠⚠⚠ **이름 체계가 둘이라는 것이 이 태스크의 핵심이다.** 이제 갈라 뒀다:
    | | 무엇 | 값 | 쓰는 곳 |
    |---|---|---|---|
    | **분배 스탯** | STR / DEX / INT / LUK | 분배한 **포인트 수** | `StatStr` 등, `Allocate`, 스탯 창 |
    | **파생 수치** | ATK / INT / DEF / LUK | 포인트가 만드는 **수치** | `GetStatWithoutEquip`, `GetEquipBonus`, `ItemTable` 스탯 컬럼, `SkillTable.scaling_stat`, `RoomGate.gate_key` |
    T31 전에는 1)이 2)의 이름을 써서 **"ATK"가 두 가지를 뜻했다.** 이제 안 겹친다
    (INT·LUK은 스탯과 수치가 같은 이름이지만 그건 원래 1:1이라 문제가 아니다).
  - **파생 수치 이름(체계 2)은 안 바꿨다.** 겹침이 사라졌으므로 더 바꿀 이유가 없고,
    바꾸면 `ItemTable` 컬럼 + `SkillTable` + 그걸 읽는 5개 파일이 더 걸린다.
    바꿀 일이 생기면 `PATK`/`MATK`처럼 수치임이 드러나는 이름이 좋다.
  - **저장은 하위 호환으로 넘긴다.** `body["stat_str"] or body["stat_atk"] or 0` —
    새 키를 먼저 보고 없으면 옛 키를 읽는다. **쓸 때는 새 키만 쓰므로** 한 번 저장되면
    옛 키가 사라진다. ⚠ **되돌아갈 수는 없다** — 새 빌드로 저장한 뒤 옛 빌드로 로드하면
    STR/DEX가 0이 된다.
  - ⚠ **`.mlua`를 python으로 고치면 안 된다** (T27-3에서 겪었다). Windows에서 text 모드
    쓰기가 LF를 CRLF로 바꿔 **파일 전체가 바뀐 diff**가 된다. node `fs.writeFileSync`는
    개행을 번역하지 않는다 — 이번엔 전부 node로 썼고 diff가 실제 변경만 담았다.
  - **`.ui` 라벨은 UIBuilder로 고쳤다** (`patchComponent`). Row3 → STR, Row5 → DEX.
    Row4 INT · Row6 LUK은 그대로다.
- 진행 중: **T27-5 회피 폐기** (2026-08-27 대표 판정: *"회피는 없애는 방향으로 너무 크다"*).
  **✅ LUK 회피를 걷어냈다** (`GetEvadeRate`/`RollEvade`/`ShowEvade` 삭제,
  `luk_evade_per_point`·`player_evade_max` 키 삭제. `IsHitTarget`은 i-frame만 본다.
  실측: 두 키가 0으로 사라졌고, 피격 `-243 HP 4140 → 3897`이 정상이며 `회피!` 로그 없음) /
  다음: 대표 플레이 판정
  - **T28 설계와 부딪히는 것이 진짜 이유다.** T28은 몬스터 곡선을 "동레벨 3대에 잡고
    12대 맞으면 죽는다"로 다시 세웠고, 그 값어치가 **난이도가 읽힌다**는 것이었다
    (방의 `monster_level`·`monster_count`가 난이도 전부를 정한다).
    회피는 그 "몇 대"에 정확히 난수를 꽂는다 — 상한 0.6에서 12대가 30대가 되고,
    **매번 다르다.** 방을 세울 때 몇 대짜리인지 말할 수 없게 된다.
  - **덜 맞게 하고 싶으면 실드가 그 자리다** (`GetDamageReduction`). 피해를 깎을 뿐이라
    "몇 대"가 그대로 계산되고 상한 0.9도 이미 걸려 있다.
  - ⚠ **`0.002`는 내가 T27-1에서 올린 값이었다** (원안 0.0001은 죽은 수치였다).
    개념이 안 맞는 것을 계수로 계속 맞추느니 뺐다.
  - **LUK이 하는 일은 이제 다섯이다**: 물리 공격력 +2 / 공속 +0.002 / 이속 +0.001 /
    포획률 +0.1%p / 드랍률 비례 보정. 앞 셋은 민첩, 뒤 둘은 운이라 **이름이 2:2로 갈린다** —
    `AGI`로 바꾸는 안이 나왔고, 어긋남이 사라지는 게 아니라 반대편으로 옮겨갈 뿐이라
    **이름 변경 태스크(StatAtk/StatDef → STR/DEX)에 얹어 한 번에 정하기로 미뤘다.**
- ~~진행 중: **T27-4 회피 판정**~~ → **T27-5에서 폐기됐다** (아래).
  **✅ LUK이 공격을 아예 피하게 한다** (`PlayerStats.GetEvadeRate` +
  `PlayerHit.RollEvade`/`ShowEvade`). 회피율 = LUK x 0.002, 상한 0.6.
  실측: LUK 73 → 회피율 **14.6%**, 실전 전투에서 `회피!` 발생) /
  다음: 대표 플레이 판정 — **피할 때 흙먼지가 보이는지** 봐 주실 것
  - ⚠⚠ **`IsHitTarget` 오버라이드에 `@ExecSpace`를 붙이면 안 된다.** 부모
    `HitComponent.IsHitTarget`이 ExecSpace=All이라 자식이 다르면
    **[LEA-3014] SignatureMismatch**가 난다. 안 붙여도 호출 경로가 서버 히트
    파이프라인이라 판정은 서버에서 돈다(실측 로그가 서버에서 나왔다).
  - ⚠⚠ **회피는 i-frame을 소모하지 않는다.** `LastHitTime`을 갱신하면 피한 것만으로
    1초짜리 무적을 공짜로 얻어 **회피율 60%가 실제로는 60%보다 훨씬 단단해진다.**
    피했으면 바로 다음 공격에 다시 노출되는 것이 맞다.
  - **순서가 중요하다: i-frame 먼저, 회피 나중.** 이미 무적인 동안 굴리면 실제 피격
    횟수보다 훨씬 많이 굴려져 "회피율 14.6%"라는 말이 뜻을 잃는다.
  - **회피와 실드는 갈라 뒀다.** 실드(`GetDamageReduction`)는 피해를 **깎고**,
    회피는 판정 자체를 **없앤다**. 한 곳에 합치면 둘이 곱해져 감쇄 상한 0.9가 무의미해진다.
  - **화면에 알려 준다** (`ShowEvade`, `BasicParticleType.DustExplosion`).
    데미지 숫자가 아예 안 뜨는 판정이라, 안 알려 주면 "몬스터가 때렸는데 아무 일도
    없다"가 되어 고장으로 읽힌다. 서버에서만 띄운다 — `IsHitTarget`이 ExecSpace=All이라
    양쪽에서 돌 수 있고 그대로 두면 흙먼지가 두 번 터진다.
  - ⚠ **큰 표본으로 비율을 검증하지 못했다.** 1000번 굴리는 클라 스크립트가
    `maker_execute_script`에서 계속 끊겼다(에러 로그도 안 남는다). 확인한 것은
    **회피율 값(0.146 = 73 x 0.002)과 실전에서 실제로 발동하는 것**까지다.
  - **이걸로 T27 스탯 재설계의 계수 12개가 전부 배선됐다.** 남은 것은 이름 변경
    (`StatAtk`/`StatDef` → STR/DEX 표기 + 저장 마이그레이션)뿐이다.
- 진행 중: **T27-3 공속·이속 배선** (T27 스탯 재설계의 남은 계수).
  **✅ LUK이 공격 주기와 이동 속도를 실제로 움직인다** (`PlayerStats.GetAttackInterval`/
  `GetMoveSpeed`/`ApplySpeeds` + `PlayerAttack.RestartAutoAttack`).
  실측: LUK 3→23→73에서 주기 `0.994 → 0.954 → 0.854`, InputSpeed `2.4072 → 2.4552`) /
  다음: 대표 플레이 판정 — **걷는 속도와 때리는 속도가 달라진 게 느껴지는지**
  - ⚠⚠ **주기를 바꾸려면 타이머를 다시 걸어야 한다.** `SetTimerRepeat`은 걸 때의 주기를
    그대로 들고 있어서, 값만 고쳐 놓으면 **그 회차 내내 옛 주기로 돈다** — 에러가 없어
    "LUK을 찍었는데 공속이 그대로"로 보인다. `RestartAutoAttack`이 그 자리다.
  - ⚠⚠ **기본 이동 속도는 1.0이 아니라 2.4였다** (`DefaultPlayer.model`의 `speed`).
    1.0으로 하드코딩했으면 이동이 **42%로 느려질** 뻔했다. `player_base_move_speed`로
    데이터에 옮겼다 — ⚠ `.model`과 CSV **두 곳이 같은 숫자**를 들고 있다.
    런타임에는 `ApplySpeeds`가 덮어쓰므로 실질 정본은 CSV다.
  - **이속을 배수로 둔 이유**: `InputSpeed`는 월드 유닛/초가 아니라 맵 종류마다 환산이
    다른 계수다(RectTile은 1.2로 나눈다). 정액으로 더하면 맵마다 체감이 달라진다.
  - **돌진이 스냅샷을 안 쓰게 고쳤다.** `PlayerDash.Finish`가 저장해 둔 속도로 되돌리는데,
    이속이 LUK에서 나오게 되면서 그 스냅샷이 낡을 수 있다 — 돌진 도중 레벨업으로 LUK이
    오르면 옛 속도로 돌아가고 **그 뒤로는 아무도 안 고쳐 준다**(`ApplySpeeds`는 스탯이
    바뀔 때만 돈다). 이제 `GetMoveSpeed()`를 다시 물어본다.
  - **재 봤는데 LUK은 과하지 않다.** 공속은 DPS에 **곱해지므로** 몰빵하면 세질까 싶어
    확인했다 — STR 몰빵 대비 LUK 몰빵 DPS는 Lv20 **0.41배** / Lv30 0.43배 / Lv50 0.55배 /
    Lv76 **1.07배**(주기 하한 0.25 도달) / Lv150 1.02배다. **주기 하한이 천장 역할을 해서**
    끝까지 가도 겨우 동률이고, STR은 그 위에 HP까지 준다. LUK은 늦게 피는 특화 스탯이다.
  - ⚠ **회피(`luk_evade_per_point`)는 아직 아무도 안 읽는다.** `PlayerHit.IsHitTarget`에
    붙이는 별도 단계다 — 판정을 건드리는 것이라 성격이 다르다.
- 진행 중: **T30 장비 수치** (지시서 밖, 2026-08-27 대표 지시: *"장비 수치부터 잡자"*).
  **T30-1 아이템 스탯 컬럼을 "스탯 포인트 상당"으로 통일 ✅** (`ItemTable`의 다섯 스탯
  컬럼이 이제 값이 아니라 **포인트**다. 변환은 `GameData.GetStatPointValue` 한 곳에서만 한다.
  실측: 무쇠 검 만작 +25 → **+125** / 가죽 갑옷 만작 +25 → **+250**) /
  다음: 대표 플레이 판정 — **가방 창 한 줄이 새 값으로 뜨는지** 봐 주실 것
  - ⚠⚠ **문제는 "장비가 약하다"가 아니라 "아이템마다 단위가 달라졌다"였다.** T27이 스탯마다
    다른 계수를 주면서 **같은 +5가 아이템마다 다른 값**이 됐다:
    | 아이템 | 컬럼 | 얹히는 곳 | 그 수치의 1P 계수 | = 몇 포인트 |
    |---|---|---|---|---|
    | 무쇠 검 | atk +5 | 물리 공격력 | STR 1P = 5 | **1P** |
    | 가죽 갑옷 | def +5 | 방어력 | DEX 1P = 10 | **0.5P** |
    | 행운의 목걸이 | luk +5 | LUK 포인트 | 1P = 1 | **5P** |
    **목걸이가 갑옷의 10배**였다. 값만 곱해 올리면 이 뒤틀림이 그대로 남는다.
  - **그래서 표에는 포인트를 적고 값은 코드가 만든다.** 아이템끼리 세기가 비교되고,
    `str_atk_per_point` 같은 계수를 조정하면 **장비가 저절로 따라온다** —
    T29에서 배운 것과 같다(두 곳이 같은 일을 하면 한쪽만 고쳐진 채 남는다).
  - **CSV의 숫자는 하나도 안 바뀌었다.** 뜻만 값 → 포인트로 바뀌었고, 그 결과
    넷이 전부 **5P**로 같아졌다(반지만 올스탯 3P). 결과 값:
    | | 1개 | 만작(5개) |
    |---|---|---|
    | 무쇠 검 | 물공 +25 | **+125** |
    | 불티 완드 | 마공 +25 | +125 |
    | 가죽 갑옷 | 방어 +50 | **+250** |
    | 행운의 목걸이 | LUK +5 | +25 |
    | 균형의 반지 | 물공+15 마공+15 방어+30 LUK+3 | 물공+75 마공+75 방어+150 LUK+15 |
  - **만작 = 25P = 5레벨치다.** T27 이전과 같은 값어치다(그때는 1P = 1이라 만작 +25가
    곧 25P였다). Lv30 기준 무쇠 검 만작이 물리 공격력의 **16.5%**다 — 전에는 2.5%였다.
  - ⚠ **가방 창은 표의 숫자를 그대로 띄우면 안 된다.** 표가 5인데 실제로는 +25가
    얹히므로, `StatSummary`가 **스탯 창과 같은 창구**(`GetItemStackedStatBonus`)에서
    값을 받도록 고쳤다. 안 그러면 가방은 "+25"라는데 스탯 창은 +125로 늘어난다.
  - ⚠ **올스탯만 포인트로 적는다** (`올스탯+15P`). 네 수치가 전부 달라(물공 +15 / 방어 +30)
    한 줄에 다 쓰면 줄을 넘고, 이 아이템의 값어치는 "모든 스탯 몇 P"가 가장 잘 나른다.
  - **실측**: `ATK 1P = 5 / INT 1P = 5 / DEF 1P = 10 / LUK 1P = 1`,
    살아 있는 세이브에서 장비 몫이 물리 공격력 23 → **115**, 방어력 18 → **180**.
- 진행 중: **T29 보스 스킬 배율** (지시서 밖, 2026-08-27 대표 지시: *"보스 스킬 배율부터 잡자"*).
  **T29-1 `boss_skill_damage_multiplier` 0.25 → 1 ✅** (= 안 깎는다.
  실측: 히어로 평타 274 / **레이징 블로우 526(1.92배)** / **오라 블레이드 434(1.58배)**) /
  다음: 대표 플레이 판정
  - **이건 난이도 knob이 아니다 — 재 보고 알았다.** 스킬은 보스 타격의 **1/3뿐**이다
    (스킬 주기 3초 · i-frame 1초라 3대 중 1대). 그래서 0.25든 1.0이든 **승패 경계가
    똑같이 Lv25**다(시뮬레이션). 이 값이 정하는 것은 **위협이 평타에서 오나 스킬에서 오나**이고,
    난이도는 `boss_atk_multiplier`가 잡는다.
  - **두 knob이 같은 일을 하게 둔 것이 이번 어긋남의 원인이었다.** T25-3의 0.25는
    `boss_atk_multiplier` ×3을 되돌리려고 넣은 값인데, T28에서 그 배율을 1.5로 내리자
    **한쪽만 고쳐진 채로 남아** 보스 스킬이 평타의 48%가 됐다. 값을 1로 두면 `SkillTable`의
    계수가 그대로 말한다 — 손댈 곳이 하나로 줄어든다.
  - **계수는 원래 자동 공격(1.2) 기준으로 적혀 있었다.** 슬래시 1.15 ≈ 평타 / 브랜디쉬 1.6 =
    1.33배 / 오라 1.9 = 1.58배 / 레이징 2.3 = **1.92배**. 이 사다리가 이미 의도된 설계였고,
    배율 1은 그걸 그대로 쓰는 것이다.
  - ⚠ **`boss_skill_range_multiplier` 0.667은 안 건드렸다.** 그 근거는 스탯이 아니라
    **방 크기**다 — 시험의 방 144칸 vs 사냥방 448칸. T28이 바꾼 것이 아니라 그대로다.
  - ⚠ **한 방은 아니다.** 레이징 526에 플레이어 HP 4140이면 7.9대다.
    T25-3에서 대표가 겪은 *"한방 맞고 뻗는다"*는 보스 ATK ×3 시절 이야기이고,
    그 배율은 T28에서 1.5로 내려와 있다.
- 진행 중: **T28 몬스터 곡선 선형화** (지시서 밖, 2026-08-27 대표 지시: *"몬스터 곡선부터 잡자"*).
  **T28-1 곡선을 기하급수 → 선형 ✅** (`monster_hp/def/atk`를 `base + per_level x (lv-1)`로.
  보스 DEF 배율 3 → **1**, ATK 배율 3 → **1.5**. `LevelCurve` 3개 메서드 + `GameDataVerify` +
  `MonsterTable` 13종 전부 + `docs/밸런스_확정수치.md` 1·2·4·5·6절 재생성) /
  다음: 대표 플레이 판정
  - ⚠⚠⚠ **값을 낮추는 문제가 아니었다. 곡선의 모양이 틀렸다.** T27에서 플레이어 스탯이
    **레벨에 선형**(1P당 +5 고정)이 됐는데 몬스터는 기하급수(`1.09^lv`)였다.
    **선형과 지수는 딱 한 번만 교차한다** — 실측 교차점이 Lv55 근처였고, 그 아래는 전부
    한 방(Lv30 동레벨 몬스터 = **0.5대**), 위는 전부 불가능(Lv100 = **400대**, Lv200 = **8억 대**)이었다.
    `monster_hp_base`를 조절하면 **교차점이 좌우로 움직일 뿐** 모양은 안 고쳐진다.
  - **EXP만 기하급수로 남겼다.** 필요 EXP 곡선(`exp_ratio` 1.12)이 기하급수라 몬스터 EXP가
    그쪽을 따라가야 한다. 전투 수치는 플레이어 스탯을 따라가고 EXP는 레벨 곡선을 따라간다 —
    **둘이 서로 다른 것을 쫓기 때문에 모양이 갈리는 것이 맞다.**
  - **DEF 배율을 뺀 것이 보스 문제의 진짜 답이었다.** DEF 배율은 싸움을 **길게** 만들고
    ATK 배율은 **위험하게** 만드는데 **둘이 곱해진다.** T26-6에서 대표가 보스에게 진 것은
    ATK ×3 때문만이 아니라 이 겹침 때문이었다. HP ×3은 그대로 둬서 보스는 여전히 긴 싸움이다.
  - **실측 (테스트 세이브 ATK 978 / DEF 828 / HP 4140)**:
    | | 내 딜 | 잡는 대수 | 맞는 피해 | 버티는 대수 | 결과 |
    |---|---|---|---|---|---|
    | 스톤골렘 Lv36 | 666 | **3대** | 271 | 15대 | 승 |
    | 스텀피 보스 Lv26 | 749 | **4대** | 358 | 11.6대 | **승** (EXP 162) |
    바로 전까지 **둘 다 한 방**이었고, T26-6에서는 스텀피에게 **졌다.**
  - **난이도는 이제 곡선이 아니라 방이 만든다.** 선형이라 "동레벨 3대/12대"가 Lv1부터
    Lv200까지 일정하다 — 방이 세운 `monster_level`과 `monster_count`가 난이도 전부를 정한다.
    **이게 이 변경의 값어치다**: 앞으로 고레벨 방을 만들 때 곡선을 다시 안 건드려도 된다.
  - ~~⚠⚠ `boss_skill_damage_multiplier` 0.25가 거꾸로 일한다~~ → **T29에서 해소.** 1로 되돌렸다.
  - ~~⚠ 장비 수치는 아직 안 고쳤다~~ → **T30에서 해소.**
  - ⚠ **레벨링이 느려졌다 — 의도한 것이다.** 한 마리가 0.5대에서 3대가 됐으니 사냥 시간이
    약 6배다. Lv30 기준 한 레벨에 7마리 x 3초 = 21초로, 전에는 7초였다.
- 진행 중: **T27 스탯 재설계** (지시서 밖, 2026-08-27 대표 지시 + 보정안 승인).
  ATK/INT/DEF/LUK → **STR/DEX/INT/LUK**, 스탯마다 효과 여러 개 (표는 설계 변경 이력 참조).
  **T27-2b STR당 HP 50 → 20 ✅** (대표 지시: *"스텟당 hp를 좀 낮추면 어떨까? 한 20으로"*.
  `str_hp_per_point` 한 칸. 실측: 로드 `1000 → 4120`(STR 156 x 20), 분배 1P에
  `4120 → 4140` + 현재 HP도 +20) /
  다음: 대표 플레이 판정
  - **지금 게임에 있는 것은 하나도 안 깨진다.** T26-6에서 대표를 죽였던 스텀피 보스(Lv26)를
    Lv30 STR 빌드가 **8.5대** 버틴다(50에서는 18.1대). 원래 벽이 **2.1대**였다.
  - **오히려 20이 빌드 균형을 고친다.** 50이면 STR이 **딜 1등이면서 탱 1등**이다 —
    HP가 STR에서만 나오기 때문이다. Lv30에서 스텀피를 버티는 대수가 STR 18.1 vs DEX 10.6으로
    STR이 1.7배 단단했는데, 20에서는 **8.5 vs 8.4로 붙는다.** DEX 빌드는 HP가 1200 근처에
    고정이라 이 변경의 영향을 거의 안 받고, 그제서야 방어력 x10이 값을 한다.
  - ⚠ **잃는 것은 미래 콘텐츠 여유다.** Lv42 스텀피(배율 3) 기준 Lv30 STR 빌드가
    4.6대 → **2.2대**가 된다. 다만 50으로 둬도 Lv60 보스에서는 어차피 한 방(1.1대)이다 —
    몬스터 ATK가 `1.08^lv`로 자라는 것을 **HP 계수로는 못 따라잡는다.**
    **그 천장을 정하는 것은 `boss_atk_multiplier`지 `str_hp_per_point`가 아니다.**
  **T27-2 공격력·방어력 배선 ✅** (물리/마법 공격력과 방어력이 새 계수로 돈다.
  물리 = STR x5 + DEX x2 + LUK x2 / 마법 = INT x5 / 방어 = DEX x10.
  실측: 물리 973(검산 일치) · 방어 828 · 마법 13 · 포획률 3.3%) /
  다음: 대표 판정 — **아래 몬스터 곡선부터 정해 주실 것**
  - ⚠⚠⚠ **플레이어가 몬스터보다 훨씬 세졌다.** 테스트 세이브(STR 156 / DEX 80) 기준:
    | | 전 | 후 |
    |---|---|---|
    | 물리 공격력 | 185 | **973** (x5.3) |
    | 방어력 | 108 | **828** (x7.7) |
    | 최대 HP | 1000 | **4120** (x4.1) |
    실측 전투: 스톤골렘 Lv36을 **4대 → 1대**에 잡고, 맞는 피해는 327 → 128이라
    **4대 버티던 것이 69대**가 됐다. 스텀피 보스도 한 방이다.
  - **이건 승인된 계수의 당연한 귀결이다.** 예전에는 1P = +1이었고 지금은 STR 1P = +5다.
    시뮬레이션에서도 Lv30 올인 STR이 ATK 845로 나왔다 — 숫자는 예상대로다.
    **문제는 몬스터가 예전 플레이어(ATK 185)에 맞춰져 있다는 것이다.**
  - **손댈 곳은 몬스터 곡선이다** (`monster_hp_base` 30 / `monster_hp_ratio` 1.09 /
    `monster_def_base` 5 / `monster_atk_base` 24). 지금 스탯 계수를 다시 낮추면
    T27-1에서 푼 HP 벽이 되살아난다 — HP도 STR에서 나오기 때문이다.
    **지시 없이 안 건드렸다.**
  - ~~⚠ 장비가 상대적으로 무의미해졌다~~ → **T30에서 해소.** 무쇠 검 만작이 +125다.
  - **`scaling_stat`은 이제 스탯이 아니라 수치 이름이다** — "ATK"=물리 공격력,
    "INT"=마법 공격력. 여러 스탯이 한 수치에 모이므로 1:1 대응이 깨졌다.
  - **`GetTotalLuk`만 성격이 다르다.** 셋은 전투 수치인데 이것만 **분배한 포인트 수**다
    — 포획률(3% + LUK x 0.1%p)과 드랍률이 포인트를 묻기 때문이다. LUK이 전투에
    기여하는 몫(+2 물리 공격력)은 `GetTotalAtk`에 이미 들어 있어 두 번 세면 안 된다.
  **T27-1 계수 데이터 + HP 성장 ✅** (`GameBalance.csv`에 계수 12개 + `PlayerStats.GetMaxHp`/
  `ApplyMaxHp`. **최대 HP = 1000 + STR x 20** (처음 50이었다가 T27-2b에서 20).
  실측: 세이브 로드에서 `1000 → 4120`,
  분배 1P에 `4120 → 4140`(현재 HP도 +20), 그리고 **T26-6에서 졌던 스텀피를 484 한 대만
  맞고 잡았다** — EXP 162 · 보스 클리어 기록까지) /
  다음: 대표 플레이 판정 → T27-2 공격력 배선
  - **T26-6의 벽이 여기서 풀린다.** 플레이어 HP가 레벨로 자라지 않는 것이 원인이었고,
    STR 하나로 고레벨 보스가 성립하게 됐다. `boss_atk_multiplier`는 안 건드렸다.
  - **아직 HP만 배선했다.** 나머지 계수 9개는 **데이터만 들어가 있고 아무도 안 읽는다** —
    공격력·방어력은 여전히 구 계수(`atk_per_stat_point` = 1)로 돈다. 단계로 쪼갠 이유는
    스탯 이름 변경이 저장 키·`ItemTable.stat_type`·`SkillTable.scaling_stat`·스탯 창·
    `RoomGate`까지 7개 파일에 걸려 있어서다.
  - ⚠ **프로퍼티 이름은 아직 `StatAtk`/`StatDef`다.** 담는 그릇은 그대로 두고
    **`GetStr()`/`GetDex()`/`GetIntStat()`/`GetLukStat()` 창구**를 새로 뒀다 —
    새 코드는 이것만 쓴다. 이름을 바꿀 때 그 네 줄만 고치면 되고, 저장 마이그레이션은
    그 단계에서 함께 한다.
  - ⚠ **최대 HP는 저장하지 않는다.** STR에서 매번 다시 계산한다(캡 플래그와 같은 판단) —
    저장하면 `str_hp_per_point`를 조정했을 때 세이브가 있는 계정만 다른 HP로 논다.
  - **늘어난 만큼 현재 HP도 같이 올린다.** 안 그러면 STR을 찍은 순간 최대치만 늘고
    체력 비율이 떨어져 "찍었는데 더 위험해졌다"가 된다. 포인트는 유한하고 되돌릴 수
    없으므로 회복 악용이 아니다. 환생으로 STR이 0이 되면 현재 HP를 상한에 맞춰 자른다.
- 진행 중: **T26 area_02 페리온** (지시서 밖, 2026-08-26 대표 지시:
  *"맵 구성을 바꿔보려고 해 좀더 복잡하게 헤네시스였으니 첫 지역이 area2는 어디가 나을까?"*
  → 대표 선택: **페리온** + **갈림길 + 막다른 방**, 이어서 *"좀더 간단하게 … 중간지점으로"*).
  **설계는 8방**: `r_11 남쪽 능선 → r_12 바위 황무지 → r_13 먼지바람 언덕(★사거리)`에서
  셋으로 갈려 **동쪽 지름길**(`gate: stat`) / **남쪽 우회로**(게이트 없음, `r_16 → r_17`) /
  **북쪽 막다른 보상방**(`gate: boss(r_05)`)이고, 둘이 `r_14 험한 바위 지대`에서 합류해
  `r_15 돌풍의 봉우리`(보스)로 간다.
  **T26-6 보스방 — 돌풍의 봉우리(스텀피) ✅** (`r_15` + `map15`. `r_14`에서 동쪽으로 이어지고
  귀환 포탈이 있다. 타일도 페리온 결로 칠했다. **이걸로 area_02 8방이 다 섰다.**
  실측: 방 15개 / 연결 28개 / 깨진 연결 0개, 보스 배율 스폰, 미니맵 `격자 5x3 배율 0.78`) /
  다음: 대표 판정 — **아래 보스 난이도부터 정해 주실 것**
  - ⚠⚠⚠ **지금 보스가 이긴다. 플레이어가 못 이긴다** (실측: 스텀피에게 맞아 죽었다).
    수치로 재면 이렇다 — 플레이어 HP 1000 / DEF 108 / 자동 공격 143 기준:
    | 보스 | 내가 맞는 피해 | 내가 버팀 | 보스 잡는 데 | 결과 |
    |---|---|---|---|---|
    | 스텀피 Lv26 (배율 3) | 484 | 2.1초 | 5.4초 | **보스 승** |
    | 스텀피 Lv26 (배율 2) | 296 | 3.4초 | 3.2초 | 플레이어 승 (아슬아슬) |
    | 스텀피 Lv26 (배율 1.5) | 205 | 4.9초 | 2.2초 | 플레이어 승 |
    | 돌 정령 Lv15 (지금 area_01) | 166 | 6.0초 | 1.7초 | 플레이어 승 |
  - ⚠⚠ **근본 원인은 플레이어 HP가 1000 고정이고 레벨로 안 자란다는 것이다** (T13-1).
    몬스터 ATK는 `24 x 1.08^(lv-1)`로 자라는데 받는 쪽은 그대로라, `boss_atk_multiplier=3`은
    **낮은 레벨에서만 성립한다.** Lv34 이상 보스는 배율 3에서 **무조건 한 방**이다
    (Lv42 스텀피 = 1905 데미지). 이건 이 보스 하나의 문제가 아니라 **앞으로 만들 모든
    고레벨 콘텐츠에 걸리는 천장**이다.
  - **레벨을 낮추는 것으로는 못 푼다.** 배율 3에서 이기려면 Lv20 이하로 내려야 하는데,
    그러면 **바로 앞 방(r_14)의 일반 스톤골렘 Lv36보다 약한 보스**가 된다(ATK 312 vs 355,
    HP 462 vs 612). 지금 Lv26으로 둔 것은 **일반 몬스터보다 확실히 센 것**이 보스의
    최소 조건이기 때문이다 — 대신 이기려면 배율을 손대야 한다.
  - **손댈 곳은 `boss_atk_multiplier` 하나다** (CLAUDE.md가 이미 "가장 먼저 조절될 값"이라
    적어 둔 그 값이다). 다만 **돌 정령과 히어로도 같이 약해진다** — 셋이 한 값을 공유한다.
    보스마다 다르게 하려면 `MonsterTable`에 배율 컬럼을 따로 두어야 한다. **지시 없이
    안 건드렸다.**
  - **종 레벨과 방 레벨을 갈라 뒀다.** `MonsterTable`의 스텀피는 **Lv42**(원작 위계에 맞는
    값, `[공식 일치]`)이고 `RoomTable.monster_level`이 **26**으로 덮는다. r_05가
    돌 정령(종 Lv10)을 Lv15로 세우는 것과 같은 방식이라, 난이도를 바꿀 때 **CSV 한 칸**이면 된다.
  - **보스 재등장이 그대로 작동한다** (T16-1). 로그: `보스 재등장 +1 (입장 시 스폰 —
    주기 리필로는 서지 않는다)`. 방을 나갔다 들어와야 다시 선다.
  - **미니맵 자동 축소가 예측대로 받았다** — 보스방이 붙어 격자가 4x3에서 **5x3**이 되자
    배율이 0.98 → **0.78**(칸 77x43)로 내려가며 그대로 들어갔다. T26-5에서 계산한 값과 같다.
  **T26-5 발굴 몬스터 교체 + 미니맵 자동 맞춤 ✅** (대표 지적 둘.
  *"아이언 뮤테는 나오지 않지 않나?"* → **맞다. 뮤테는 페리온에 아예 안 나온다**(나무위키 확인).
  *"페리온 맵 구조가 커서 좀 삐져 나오는거 같네"* → **맞다. 수치로 확인했다.**
  실측: 스켈레톤 지휘관 스폰·전투·EXP 79 / 미니맵 `격자 4x3 배율 0.98`) /
  다음: 대표 플레이 판정
  - **원작 유적 발굴지는 마스크와 스켈레톤이다**: 초기 발굴지역 우드 마스크 Lv60 /
    발굴 완료지역 스톤 마스크 Lv61 / 발굴 중단지역 스켈독·머미독 Lv62 /
    발굴 위험지역 스켈레톤 사병 Lv63 / **폐쇄지역 스켈레톤 장교 Lv64** /
    **미접근 지역 스켈레톤 지휘관 Lv65**. 우리 방 이름 "발굴 금지 구역"은 원작 맵
    `102040600 Prohibited Area` = **미접근 지역**이라 **스켈레톤 지휘관**이 정확한 짝이다.
    게이트가 걸린 막다른 보상 방이라는 성격과도 맞는다. 수치는 Lv30 그대로 뒀다(밸런스는 미룸).
  - **몬스터를 고를 때 원작을 먼저 확인할 것.** 미스릴 뮤테는 "발굴지 느낌"으로 고른 것이지
    근거가 있어서 넣은 게 아니었다. 방 이름은 원작 맵에서 따 왔으면서 몬스터는 안 따진 것이
    이번 어긋남의 원인이다. **방과 몬스터는 같은 출처에서 뽑을 것.**
  - ⚠ **`Models/Monsters/Skeleton.model`은 죽은 모델이다** — `MonsterId = "m_skeleton"`인데
    그 id는 헤네시스 재테마(2026-08-16) 때 `MonsterTable`에서 빠졌다. 이번에 쓰지 않았다
    (스켈레톤 **사병**이라 지휘관과 다른 종이다). 정리 후보.
  - **미니맵 넘침은 수치로 확인됐다**: 판(Board) 420x150에 필요한 크기가
    헤네시스 근교 430x122 / 페리온 430x188 / 보스방 추가 후 540x188이다.
    **판을 440x370(격자판 420x310)으로 키우고 격자를 판 크기에 맞춰 자동 축소**한다.
    실측 배율 0.98 (칸 97x54). 보스방이 붙으면 0.78로 내려가고 바닥값은 0.55다.
  - ⚠ **칸만 줄이면 글자가 넘친다.** `Name`/`Sub`가 고정 크기(96x26 / 96x24, 글자 15/13)라
    칸과 **같은 비율로 글자 상자와 FontSize까지** 줄여야 한다. 연결선 두께도 같이 줄인다.
  - **판 크기는 런타임에 실제로 잰다** (`board.UITransformComponent.RectSize`).
    `.ui`에서 판을 키웠는데 스크립트 상수를 같이 안 고치면 계산이 조용히 어긋난다 —
    두 곳이 같은 숫자를 들고 있으면 언젠가 갈라진다.
  - **드래그·확대축소는 넣지 않았다** (대표 제안이었으나 논의 후 자동 축소로 결정).
    이유 셋: ① 미니맵은 **레이캐스트를 하나도 안 먹게** 만든 HUD인데(T23-10b) 조작을 넣으면
    우측 상단에 다시 클릭을 먹는 UI가 생긴다(T23-8과 같은 사고의 씨앗) ② 상시 HUD는
    곁눈질용이라 드래그해 놓고 방을 옮기면 엉뚱한 곳을 비춘다 ③ 지금 문제는 38px이라
    자동 축소로 끝난다. **방이 15개쯤 되어 자동 축소가 안 읽히면** 그때는 HUD가 아니라
    **버튼으로 여는 전체 화면 지도**에 드래그·확대를 넣는 것이 맞다(T23-10의 원래 설계).
  **T26-4 페리온 타일 + 지역 이름 정리 ✅** (대표 지적: *"타일 스타일 area 2번째거
  스타일을 바꿔줘야 하지 않나? 페리온 스타일로 가자고 했잖아"* — 맞다. 7방이 전부
  `map04`를 복제한 **헤네시스 잔디**였다. `Henesys_stone` 결로 다시 칠했다.
  같이 `AreaTable.area_01`을 `시작 지역` → **`헤네시스 근교`**로.
  실측: `map14` 448칸 전수 스캔 `돌 448 / 총 448칸`, 경계·칸 수 그대로) /
  다음: 대표 플레이 판정 (**보는 것**이라 화면 확인이 필요하다)
  - ⚠ **타일 칠하기는 원칙상 대표가 Maker에서 하는 일이다** (`tile.md`: "AI guides,
    user paints"). 448칸 x 7방을 손으로 칠할 수 없어 프로그램으로 썼다 —
    T16-2(시험의 방)·T23-11(마을 확장)과 같은 방식이고 그 둘도 이렇게 만들었다.
  - ⚠⚠ **`Henesys_stone_3`(63)과 `stone_27`(87)은 `IsCollidable=true`, 즉 통과 불가다.**
    T23-11에 "`_3/4/5`는 테두리가 아니라 잔무늬 변형"이라고 적어 뒀는데 **`_3`은 벽이었다.**
    그래서 **`mapjob`에 6칸, `maptown`에 4칸의 보이지 않는 벽이 이미 깔려 있다.**
    페리온에는 안 넣었다(실측 통과불가 0). 마을·시험의 방은 지시 밖이라 안 건드렸다.
  - ⚠⚠ **이 타일셋은 룰타일 세트다 — 134종 중 104종이 잔디↔길 경계 조각이다.**
    바닥으로 깔 수 있는 단색은 몇 개뿐이고, T23-11에 적어 둔 *"`_3/4/5`는 테두리가
    아니라 잔무늬 변형"*은 **거꾸로였다**: `stone_4`(64)·`stone_5`(65)는 **잔디가 붙은
    테두리**라 바닥에 흩뿌리면 초록 파편이 박힌다. 처음 칠에서 12~26%를 그렇게 넣었다가
    대표 지적(*"갈색 계통의 색이니까 페리온 바위가 그런 느낌으로"*)으로 다시 칠했다.
  - **타일 그림을 보는 방법을 찾았다.** 타일 `Id`는 공개 리소스 검색에서 404지만
    **썸네일 CDN에는 있다**: `mod-thumbnail.dn.nexoncdn.co.kr/{앞2}/{다음2}/{guid}_64.png`
    (`asset_get_account_thumbnail`이 이 URL을 준다). 134종을 전부 받아 평균색과 **색 분산**을
    재면 단색 채움과 경계 조각이 갈린다 — 경계는 한 칸에 두 재질이 있어 분산이 크다.
  - **쓸 수 있는 단색은 이것뿐이다**: 갈색 흙 `soil_59`(58)·`soil_60`(59)·`soil_61`(60)
    (rgb 211,161,77 / 분산 7.5~8.9 — 잔디가 전혀 없다) · 회색 조약돌 `stone_1`(61)·`stone_2`(62) ·
    잔디 `soil_1`(0)~`soil_5`(4). 그 밖의 갈색 20종은 구석에 잔디 혹이 붙어 있다(분산 20↑).
  - **페리온은 순수 갈색이다** (대표 지시: *"바위가 있는게 맞는거 같고 순수 갈색으로 가자"*).
    회색 조약돌을 한 번 섞어 봤다가 뺐다 — 갈색 흙과 회색 돌 사이에 **경계 타일이 없어**
    (이 세트의 경계 조각은 전부 잔디 기준이다) 덩어리 가장자리가 계단처럼 각졌다.
  - **바위 지형은 자갈 밀도로 만든다.** 갈색 3종이 자갈 개수로 갈린다 —
    `soil_59`(58) 없음 / `soil_60`(59) 2개 / `soil_61`(60) 3~4개. 벌판은 평평하게 깔고
    노두 자리에만 자갈을 촘촘히 몰아 넣는다. 덩어리로 놓는 것은 그대로다 —
    한 칸씩 무작위면 잡티로 보이고 뭉쳐야 지형으로 읽힌다.
  - ⚠ **자갈 지형은 회색 바위만큼 눈에 띄지 않는다.** 게임 실제 크기(한 칸 100px)에서는
    보이지만 "자갈밭과 민흙의 차이"지 뚜렷한 지형지물이 아니다. **순수 갈색의 대가다** —
    강한 대비를 원하면 타일셋을 새로 만들어야 한다.
  - **축소 렌더로 판단하면 틀린다.** 28px로 줄여 보면 자갈이 전혀 안 보여 "밋밋하다"고
    오판하게 된다. **한 칸을 100px로 놓고 카메라 크기(13x7칸)로 잘라** 볼 것.
  - **방 미리보기를 게임 없이 그릴 수 있다.** `.map`의 `tileMap`을 읽어 타일 썸네일을
    격자에 붙이면 방 그림이 나온다. 타일을 갈 때마다 플레이로 확인할 필요가 없다.
  - ⚠⚠ **`RectTileInfo.Index`가 런타임에서 JSON보다 1 크다** (실측: `soil_1` JSON 0 →
    런타임 `idx 1`, `stone_1` JSON 61 → `idx 62`). `tile.md`에는 **`RectTileInfo.Index`는
    0-based**라고 적혀 있는데 이 빌드에서는 아니다. 런타임 값을 JSON에 되쓸 때는 -1 할 것.
  - **페리온 전용 타일셋은 만들 수 없었다.** 원작 페리온 맵은 전부 **사이드뷰(MapleTile)**라
    타일셋 계열이 `MapleTileSetData`인데, 우리는 RectTile이라 `MODTileSetEntry`(`.tileset`)를
    쓴다 — **호환되지 않는다.** 워크스페이스에 타일셋은 `RectTileData_Henesys` 하나뿐이고
    돌 계열이 유일한 대안이었다. 진짜 페리온 바위 타일을 원하면 스프라이트를 찾아
    `.tileset`을 새로 만드는 별도 작업이 된다(빌더가 없는 파일 형식이다).
  - **방마다 결을 다르게 줬다.** 일곱이 전부 같으면 "돌로 바꿨다"가 아니라 "한 방을
    일곱 번 봤다"가 된다: 남쪽 능선·아머피그의 땅은 잔디 얼룩을 남겨 경계로 읽히게,
    발굴 금지 구역은 가장 고르게(인공 굴착지), 험한 바위 지대는 가장 거칠게(잔무늬 26%).
  - **난수에 고정 씨앗을 썼다** (T23-11과 같은 이유). 실행할 때마다 무늬가 달라지면
    지도를 손볼 때 diff로 무엇을 바꿨는지 못 읽는다.
  **T26-3 아랫길 + 시작 마을을 헤네시스로 ✅** (`r_16 아머피그의 땅`(아이언 호그 Lv28)과
  `r_17 불의 땅`(파이어보어 Lv32). 게이트 없는 우회로다 — 이걸로 `r_13 → r_14` 가는 길이
  둘이 되어 **지도에 순환이 생겼다.** 대표 지시로 마을 이름도 `새벽 마을` → **`헤네시스`**.
  실측: 방 14개 / 연결 26개 / 깨진 연결 0개, 미니맵 **방 7개에 연결선 7개**) /
  다음: 대표 플레이 판정
  - **미니맵이 순환을 그린다.** 걱정했던 자리인데 `WorldMapPanel.DrawLinks`가 BFS 트리
    간선이 아니라 **격자에서 실제로 붙어 있는지**(`gx[other] - gx[id] == dx`)를 보고 긋는다.
    격자를 기하학적으로 닫히게 짜 두면 순환 간선도 따라 나온다.
    실측 좌표: 먼지바람 언덕(55,0) / 발굴 금지 구역(55,66) / 아머피그의 땅(55,-66) /
    험한 바위 지대(165,0) / 불의 땅(165,-66) — **방 7개에 선 7개**다(트리면 6개).
  - **우회로가 난이도 계단을 부드럽게 만든다** (설계할 때 노린 것은 아니고 나온 결과다).
    지름길은 와일드보어 평타 120에서 **바로 스톤골렘 329**로 뛰는데,
    우회로는 120 → 아이언 호그 192 → 파이어보어 222 → 329로 이어진다.
    **게이트를 못 넘어 돌아가는 길이 더 안전한 길**이 됐다.
  - **시작 마을 = 헤네시스.** `RoomTable`의 `name` 한 칸이고, 이 이름을 읽는 코드는 없다
    (`room_type == "town"`으로만 가른다). `새벽 마을`은 이 표에만 있었다.
  - ⚠ **`AreaTable.area_01`은 아직 `시작 지역`이다.** 지역 선택 창에 `시작 지역` /
    `페리온`이 나란히 뜨는데, 한쪽만 원작 지명이라 결이 안 맞는다. 마을이 헤네시스가
    됐으므로 area_01은 **`헤네시스 근교`**처럼 마을과 겹치지 않는 이름이 맞다
    (원작도 헤네시스 마을과 그 바깥 사냥터가 따로다). **지시 밖이라 안 고쳤다.**
  **T26-2 윗길 — 막다른 보상방 + 합류점 ✅** (`r_1a 발굴 금지 구역`(미스릴 뮤테 Lv30,
  `gate: boss r_05`)과 `r_14 험한 바위 지대`(스톤골렘 Lv36, `gate: stat 170`).
  `r_13`이 사거리의 절반(서·동·북)을 열었다. 실측: 방 12개 / 연결 20개 / 깨진 연결 0개,
  미니맵 `페리온 방 5개`) /
  다음: 대표 플레이 판정
  - **스톤골렘은 모델을 새로 안 만들었다.** area_01 보스(`m_stone_golem`)를 그대로 쓰고
    `RoomTable.monster_level`로 10 → **36**만 올린다. 헤네시스에서 사건이던 것이
    페리온에서는 길에 서 있다 — 성장이 방 이름이 아니라 **서 있는 몬스터로** 보인다.
  - ⚠⚠ **`RoomPortal.model`의 트리거는 3x3인데 맵의 포탈들은 1.5x1.5다.**
    `arriveInset`(2.5)이 **1.5 기준으로 계산된 값**이라(`0.75 + 몸통 1 + 여유 0.75`),
    모델 그대로 놓으면 도착 지점이 트리거 안에 들어가 **무한 왕복**이 난다.
    `placeModel`할 때 `componentOverrides`로 `BoxSize`를 1.5x1.5로 덮을 것.
  - **도착 좌표가 반대편 트리거와 겹치지 않는 것을 좌표로 확인했다**:
    북→(0,-3.5) vs Portal_S 판정 -6.75~-5.25 / 남→(0,5.5) vs Portal_N 7.25~8.75 /
    동→(-10.5,1) vs Portal_W -13.75~-12.25 / 서→(10.5,1) vs Portal_E 12.25~13.75.
  - **게이트를 양쪽으로 재 봤다.** `stat 170` 통과(184) / 문턱을 9999로 올린 사본은
    `분배한 스탯 9999 필요 — 현재 184`로 차단 / `boss r_05` 통과(격파 기록 있음).
  - ⚠⚠ **스톤골렘 Lv36의 평타가 329다** — DEF 105 / HP 1000 플레이어가 **3대에 죽는다**
    (실측: 577 → 248 → -81 → 마을 복귀). 앞 방과의 낙차가 크다:
    와일드보어 Lv26 = 120(8대) → 미스릴 뮤테 Lv30 = 183(5대) → **스톤골렘 Lv36 = 329(3대)**.
    몬스터 ATK가 `24 x 1.08^(lv-1)`이라 레벨 6칸이 곧 1.6배다. **밸런스는 대표가 미뤄
    두었으므로 손대지 않았다** — 낮추려면 `RoomTable`의 `monster_level` 한 칸이다.
  - ⚠ **`r_14`의 `stat 170`은 초안이다.** Lv30 도착 시점의 분배 포인트가 약 145라
    r_11~r_13에서 조금 더 자라야 열린다는 뜻으로 잡았다. **T26-3에서 남쪽 우회로가
    붙기 전까지는 여기가 유일한 길**이라, 못 넘으면 `r_13`에서 막힌다.
  **T26-1 페리온 개통 + 3방 ✅** (몬스터 3종 + `area_02` 진입 방 + `r_11`~`r_13` + `map11`~`map13`.
  실측: 세 방 전부 스폰·전투·EXP·드랍이 돌고 미니맵이 `페리온 방 3개`로 갱신된다) /
  다음: 대표 플레이 판정
  - **보스는 다크 스톤골렘이 아니라 스텀피다** (대표 정정: *"페리온 보스를 생각한다면"*).
    맞다 — 스텀피(`mob/3220000.img`)가 원작 페리온 필드 보스이고, 엑스텀프 계보의
    우두머리라 이 지역 몬스터 사다리와도 이어진다. 다크 스톤골렘은 발굴지 쪽이다.
    보스방 이름도 따라 **돌풍의 봉우리**(원작 스텀피 서식지)로 옮겼고, 비어난
    막다른 보상방은 **발굴 금지 구역**(미스릴 뮤테의 실제 서식지)이 됐다.
  - ⚠⚠ **마을은 지역 이동의 출입구가 동쪽 문 하나다** (2026-08-10 설계). 그런데
    복귀로가 필요했다 — area_01은 `r_01.conn_west = r_town`으로 걸어 나오는데
    `r_town.conn_east`는 이미 `r_01`이 쓰고 있다. **`r_town.conn_north = r_11`을
    데이터에만 넣고 마을에 북쪽 포탈 엔티티는 놓지 않았다** — `GameDataVerify`의
    양방향 검사는 통과하고, 마을에서 걸어서 페리온에 갈 길은 생기지 않는다.
    **선례가 이미 있다**: `r_town`의 Portal_E에는 `RoomPortal`이 아니라 `TownGate`가
    붙어 있어 `conn_east`도 이동에 안 쓰인다.
  - **`gate_type=rebirth`가 곧 "그 지역이 해금됐는가"다** (`RoomGate.EvaluateRebirth` →
    `UnlockedAreas`). 나중에 마을에 북쪽 문을 실제로 놓고 싶어지면 `r_11`에
    `rebirth / area_02`를 걸면 우회 없이 열린다 — 새 게이트 종류를 만들 필요가 없다.
  - **몬스터 수치는 밸런스 공식에서 뽑는다.** `GameDataVerify.VerifyMonsters`가
    `hp=30x1.09^(lv-1)` · `def=5x1.08^(lv-1)` · `exp=5x1.10^(lv-1)`과 대조해
    어긋나면 경고를 남긴다. 3종 전부 `[공식 일치]`로 들어갔다.
  - ⚠ **`drop_skill_id`를 비워 뒀다.** 페리온 몬스터 스킬은 아이콘·이펙트·소리까지
    딸린 별도 작업이라 T26-1에 넣지 않았다. 빈 값은 안전하다 — `TryCapture`가
    즉시 빠져나가고(천장 카운터도 안 센다) 진척도 HUD도 스킬 줄을 건너뛴다.
    **포획이 되는 지역이 되려면 이 칸을 채워야 한다.**
  - **맵은 rect 템플릿이 아니라 `map04`를 복제했다.** 스킬 카탈로그의 rect 템플릿은
    타일도 포탈도 `RoomSpawner`도 없는 빈 판이라 사냥방 하나에 448칸을 다시 칠해야 한다.
    포탈도 **새로 놓지 않고 이름을 바꿔 옮겼다**(`Portal_W` → `Portal_S`) —
    새로 놓으면 `openRUID`/`lockedRUID`가 비어 **포탈이 화면에서 사라진다**(에러 없이).
  - ⚠ **복제한 맵에 `HeroNpc`가 딸려 온다.** `map04`에는 시험의 방 입구가 서 있다 —
    지우지 않으면 페리온 세 방마다 히어로가 한 명씩 선다.
  - ⚠ **포탈 `TriggerComponent`의 `IsLegacy` 경고가 방 수만큼 늘어난다** (`LWA-3019`).
    원본 `map04`에서 딸려 온 것이라 새 결함은 아니지만, 기존 "정리 후보 5건"이
    이제 9건이다. 방을 더 만들수록 늘어난다.
- 진행 중: **T25 전직 스킬 원작 대조** (지시서 밖, 2026-08-26 대표 지시:
  *"전직 스킬들이 지금 제대로 원작에 맞게 작동하는지 알고 싶어 설명으로는 조금
  다른거 같거든"*).
  **T25-4 히어로 공격 모션 ✅** (T16-3b에서 "별도 태스크"로 남겨 둔 것.
  히어로가 **아무 동작 없이** 때리고 있었다 — 아바타 몬스터라 기존 방식이 안 통했다.
  실측: 때릴 때 `swingO1`, 끝나면 상태에 맞춰 `walk1`/`stand1`로 복귀) /
  다음: 대표 플레이 판정
  - **왜 안 나오고 있었나**: `PlayAttackPose`가 `SpriteRendererComponent`를 갈아끼우는
    방식인데 **아바타 몬스터에는 그 컴포넌트가 아예 없어** 첫 줄에서 물러났다.
  - **`ActionStateChangedEvent`를 몸 엔티티로 직접 보낸다.** 상태머신(ChangeState)은
    스프라이트 몬스터와 같은 이유로 못 쓴다 — AIChase가 매 프레임 IDLE/MOVE를 되쓴다.
    `BodyActionStateChangeEvent`도 안 쓴다: 그건 14개 내장 상태를 거치는데,
    그러면 `AvatarStateAnimationComponent`가 다시 개입해 AIChase와 같이 지워진다.
  - ⚠⚠ **끝나면 직접 되돌려야 한다.** `Onetime`은 한 번 재생하고 **마지막 프레임에서
    멈춘다** — 안 되돌리면 검을 뻗은 채로 굳는다. 그리고 **`AvatarStateAnimationComponent`가
    대신 고쳐 주지 않는다**: 실측에서 공격이 도는 동안에도 셀렉터는 계속 `Walk|MOVE`였다.
    우리 이벤트가 셀렉터를 거치지 않고 몸으로 바로 가기 때문에 **상태가 안 바뀌고,
    저쪽은 다시 보낼 일이 없다.** 되돌리는 것은 우리 몫이다.
  - ⚠ **`GetBodyEntity()`는 ClientOnly다.** 서버에서 부르면 nil이라 재생은 클라에서 한다.
    방에 있는 사람에게만 보낸다 (`SkillEffect.PlayCast`와 같은 방식).
  - ⚠ **`PartsActionName`을 비우면 몸만 움직이고 무기가 그 자리에 멈춘다.**
    `CoreActionName`과 같은 값을 넣는다.
  - **동작 이름은 무기가 정한다**: 한손검 `swingO1/O2/O3`·`stabO1/O2`,
    두손검 `swingT*`, 활 `shoot1`. 히어로는 모험가 히어로 소드(한손)라 `swingO1`이다.
    종별로 갈 수 있게 `.model`에서 바꾸는 프로퍼티(`AvatarAttackAction`)로 뒀다.
  - 실측 로그: `[swingO1] 1회` → 쫓아오는 중이면 `[walk1] 반복`, 붙어 있으면 `[stand1] 반복`.
    화면에서 히어로가 검을 휘두르는 자세로 그려지는 것도 확인했다.
  **T25-3 보스 스킬만 깎는다 ✅** (대표 지시: *"범위 3분의 2, 데미지를 4분의 1로 줄이자
  … 한방 맞고 뻗는데 히어로한테? 이러면 스킬 못얻지"*.
  `boss_skill_range_multiplier` 0.667 · `boss_skill_damage_multiplier` 0.25 신설.
  → **데미지 배율은 2026-08-27 T29에서 1로 되돌렸다** (근거였던 보스 ATK ×3이 T28에서
  1.5가 됐다). 반경 0.667은 근거가 방 크기라 그대로다.
  실측: 레이징 블로우가 **1000 HP를 1.5대에 지우던 것이 6대**가 됐다) /
  다음: 대표 플레이 판정
  - ⚠⚠ **`SkillTable`의 `range`·`coefficient`는 플레이어와 보스가 공유한다.**
    CSV를 그대로 줄이면 대표가 **뺏어서 쓸** 히어로 스킬까지 같이 약해진다 —
    계수 4분의 1이면 0.29~0.58이라 **자동 공격(1.2)보다 약해져** 스킬을 얻을 이유가 사라진다.
    그래서 **쓰는 자리(`MonsterAttack`)에서만** 곱한다. 플레이어 쪽은 안 건드렸다.
  - **평타는 안 깎는다.** 줄여야 하는 것은 *스킬이 평타보다 훨씬 아픈 것*이지
    보스의 기본 화력이 아니다. 보스 ATK는 이미 `boss_atk_multiplier`로 x3이고,
    그 위에 스킬 계수 2.3까지 곱하면 664 — 1000 HP가 **1.5대**에 지워졌다.
  - **왜 범위도 같이 깎나**: 시험의 방(`mapjob`)이 **16x9 = 144칸**뿐이라
    사냥방(448칸) 기준으로 잡은 반경 6.0이 방의 78%를 덮는다. **3배 차이 나는 방에
    같은 반경을 쓰면 한쪽에서는 광역기, 다른 쪽에서는 회피 불가가 된다.**
  - 실측 (플레이어 DEF 25 / HP 1000, **가만히 서서 반격도 물약도 없이**):
    슬래시 83(반경 2.33) · 브랜디쉬 116(2.67) · 오라 137(3.33) · 레이징 166(4.00).
    27초 동안 스킬 8번을 맞고 사망 — 전에는 레이징 두 방이면 끝이었다.
  **T25-2 원작 수치 인터넷 검증 + 범위 확대 ✅** (대표 지시: *"인터넷 검색을 통해서
  검증해 줄 수 있으려나? … 여러마리를 죽이는데 범위가 저게 맞나 싶어서"*.
  나무위키 히어로 스킬에서 원작 수치를 받아 대조했고, 반경을
  2.0/2.2/2.5/3.0 → **3.5/4.0/5.0/6.0**으로 올렸다.
  실측: 브랜디쉬 4발 중 한 발이 **2마리 동시 명중**, 총 3마리/4발) /
  다음: 대표 플레이 판정
  - **원작 수치** (나무위키): 슬래시 블래스트 6명·1타·335% / 브랜디쉬 6명·**2타**·280% /
    오라 블레이드 8명·**5타**·180% / 레이징 블로우 8명·**4타**·320% /
    아이언 바디 방어력 +200 · 최대 HP +20%.
    **차수는 전부 우리와 일치**했다 (1/1/2/3/4).
  - ⚠ **진짜 문제는 범위가 아니라 밀도였다.** 방이 **28x16 = 448칸**인데 몬스터가
    **6마리**(기본 3 + 환생 3)다. 균일 분포 기대 명중 수 = `마릿수 x πr² / 448`:
    반경 2.0 → 0.17마리 / 2.2 → 0.20 / 2.5 → 0.26 / 3.0 → 0.38.
    **평균 한 마리도 안 걸린다** — 예전에 세 발 쏴서 0명이었던 게 우연이 아니었다.
    원작은 좁은 발판에 몬스터가 촘촘해 전방 박스 하나로 6마리가 잡힌다.
  - **이 밀도 모델이 실측과 맞는다.** 반경 4.0 예측 0.67마리 vs 실측 **0.75마리**
    (4발에 3마리). **앞으로 범위를 정할 때 이 식을 쓸 것.**
  - ⚠⚠ **히어로 보스도 이 스킬을 쓴다** (T19-1). 시험의 방(`mapjob`)이 **16x9 = 144칸**이라
    레이징 블로우 반경 6.0은 판정 넓이 113칸 = **방의 78%**다. 사실상 못 피한다.
    세다고 느껴지면 `s_hero_04`의 `range`부터 내릴 것 — 사냥방(448칸)과 시험의 방(144칸)이
    **3배 차이**라 같은 반경이 전혀 다르게 작동한다.
  - **원작 대상 수 상한(6/6/8/8)은 안 넣었다.** 우리는 여전히 **무제한**이라 그쪽은
    우리가 더 후하다. 지금 밀도에서는 의미가 없지만, 밀도를 올리면 그때 의미가 생긴다.
  - **상위 차수가 원작보다 덜 강하다.** 원작 실효 배율(데미지 x 타수) 상대비
    **1 : 1.67 : 2.69 : 3.82** vs 우리 계수 **1 : 1.39 : 1.65 : 2.00**.
    원작 4차는 1차의 3.8배인데 우리는 2.0배다. 손대려면 밸런스 전체가 움직인다.
  **T25-1 브랜디쉬 광역화 + 설명 정정 ✅** (대표 선택: *"설명 거짓말 + 브랜디쉬만"*.
  브랜디쉬 `single/1마리/반경 1.0(기본)` → **`area/제한 없음/반경 2.2`**,
  레이징 블로우 설명에서 **"연속으로"** 삭제) /
  다음: 대표 플레이 판정
  - **계보·차수는 원래 맞았다.** 아이콘 RUID로 원본 팩을 역추적한 결과
    `100`(전사 1차) · `110`(파이터 2차) · `111`(크루세이더 3차) · `112`(히어로 4차)로
    우리 `tier`와 정확히 일치한다. **RUID 역추적이 차수 검증 수단이 된다.**
  - **브랜디쉬가 상위 차수인데 더 약했다.** `range`가 비어 있어 기본 근접값 1.0을
    쓰고 `max_targets=1`이라, **1차 슬래시 블러스트(반경 2.0·무제한)보다 좁고
    대상도 적었다** — 계수만 1.15 → 1.6이라 여러 마리 상황에서는 총 딜이 내려갔다.
    전직해서 얻은 스킬이 더 약하게 느껴지는 구조였다.
  - **이제 반경이 차수 순으로 단조 증가한다**: 2.0 → **2.2** → 2.5 → 3.0.
  - ⚠ **설명이 구현과 어긋난 것은 레이징 블로우 하나뿐이었다** (9종 전수 대조).
    *"연속으로 내리친다"*고 적혀 있었는데 실제로는 **1타**다 — 엔진에 다중 타격
    개념 자체가 없다. 설명을 고쳤고, **다중 타격은 대표가 미룬 것**이다.
  - **각색으로 남긴 것 둘**: 아이언 바디(원작은 방어력 상승 장시간 버프 → 우리는
    4초 피해 35% 감소)와 **모든 스킬이 사방 원형**(원작 전사 스킬은 전방 판정).
    둘 다 대표가 각색으로 인정하고 두기로 했다.
  - **쿨다운은 정당한 각색이다.** 원작은 MP로 억제하는데 이 게임에는 MP가 없다.
  - ⚠ **다중 대상 명중을 실측하지 못했다.** 자동 공격이 달팽이를 한 방(155 vs HP 30)에
    지워서 쿨(5초)이 도는 사이 살아 있는 표적이 사라진다 — 세 번 쐈지만 전부 0명이었다.
    스킬이 `반경=2.20 대상=area 제한 없음`으로 나가는 것까지는 로그로 확인했고,
    범위 판정 자체는 T17-3a에서 **5마리**로 실측돼 있다.
- 진행 중: **T24 UI 전체 리스타일** (지시서 밖, 2026-08-25 대표 지시:
  *"레퍼런스 전체 리스타일로 하는게 좋을거 같아보이네 그리고 배치도 최대한 레퍼런스를
  참고해서 배치 맞춰주면 좋을거 같아보이네"*).
  기준은 `msw-ui-system`의 **`style-1-black`** 번들 — 대표가 보낸 레퍼런스(메이플 월드
  기본 프리셋: 채팅·상태창·HP바·스킬슬롯)와 결이 같은 검은 패널 계열이다.
  판이 커서 셋으로 쪼갠다: **T24-1 왼쪽 버튼 열** / T24-2 창 스킨 / T24-3 HUD.
  **T24-1b 잘못 고른 레퍼런스 되돌림 ✅** (대표 지적: *"저건 지금 메이플스토리 ui
  스타일이 아니잖아 기존 메이플스토리 월드에서 제공하는 ui 스타일도 아니고"*.
  `style-1-black`은 **스킬에 딸려 온 예제 번들**이지 MSW가 제공하는 UI도 원작 UI도
  아니었다. 버튼 아트만 되돌리고 정렬은 남겼다) /
  **T24-11 남은 창 넷을 프리셋 창으로 ✅** (스탯·환생·여신·지역 선택.
  **이걸로 T24 창 스킨이 닫힌다** — 화면에서 열 수 있는 창 여섯이 전부 같은 규격이다.
  실측 4종: 스탯 `레벨 20 / ATK 104(134)` · 환생 `잃는 것/얻는 것/환생 규칙` ·
  여신 `2차 / 3칸 / Lv60(40 남음)` · 지역 선택 `어디로 갈까` + 세 줄) /
  다음: 대표 플레이 판정
  - **넷 다 `.ui`만 고치면 됐다.** 창을 그리는 스크립트에 `Color(`가 있는지 먼저
    훑어(T24-10의 교훈) 넷 다 없음을 확인하고 시작했다. 런타임 색이 있는 것은
    스킬 창(고침) · 퀵슬롯 · 미니맵뿐이다.
  - **금색 강조는 진하게 내렸다** (`1,0.83,0.30` → `0.66,0.42,0.04`). 밝은 판에서
    밝은 금색은 흰 바탕에 묻힌다 — 스탯 값과 환생 창의 절 제목이 그랬다.
  - **채운 버튼은 그대로 뒀다.** 환생의 `환생한다`(금색 바탕 + 검은 글자)와 스탯의
    `+`(파란 바탕 + 흰 글자)는 **바탕이 이미 진해서** 밝은 판에서도 그대로 읽힌다.
    바꿔야 하는 것은 **투명하거나 어두운 바탕에 밝은 글자**를 얹은 것들이다.
  - **창 높이를 34~50 키웠다.** 제목 바(107)가 본문을 아래로 밀기 때문이다.
  - ⚠ **키 설정 창은 안 건드렸다.** T23-8에서 여는 버튼을 없애 **화면에서 열 수 없다** —
    스킬 바가 키 배치를 계속 쓰고 있어 창과 스크립트만 남겨 둔 상태다. 되살리면 그때 스킨한다.
  - ⚠ **미니맵·방 진척도는 어두운 채로 뒀다.** 둘은 창이 아니라 **상시 HUD**이고
    서로 같은 결이다. 창 규격을 HUD에 밀어 넣으면 게임 화면 위에 흰 판 두 개가 뜬다.
  **T24-10 스킬 창을 프리셋 창으로 ✅** (T24-9 레시피를 두 번째 창에 그대로 적용.
  실측: 제목 바 `스킬 장착` · 작은 X · 밝은 본체 · 장착 칸 5개 · 탭 · 격자 · 설명) /
  다음: 대표 플레이 판정 → 통과하면 나머지 창(스탯·환생·여신·지역선택)
  - ⚠⚠ **이 창은 색을 런타임에 칠한다.** `EquipPanel`이 탭의 선택/비선택과 칸의
    선택·잠김·출처를 색으로 가르는데 전부 어두운 값이었다. **`.ui`만 밝게 하면
    밝은 판 위에 검은 탭·검은 칸이 그대로 남는다** — `.mlua`의 상수를 같이 뒤집어야 한다.
    (창을 스킨할 때 **그 창을 그리는 스크립트에 `Color(` 가 있는지 먼저 볼 것.**)
  - **네 상태는 밝기가 아니라 색조로 가른다**: 고름=하늘 `0.72,0.83,0.95` /
    잠김=회색 `0.84,0.83,0.82` / 모험가=크림 `0.96,0.91,0.76` / 몬스터=연녹 `0.88,0.94,0.91`.
    밝은 판에서는 밝기 차만으로는 넷이 안 갈린다.
  - ⚠ **잠긴 아이콘은 어둡게가 아니라 바래게 한다** (`0.45` → `0.72`). 밝은 칸에서
    어둡게 하면 오히려 **강조된 것처럼** 보인다.
  - **창을 980 → 1014로 키웠다.** 제목 바(107)가 본문을 아래로 미는데, 격자를 줄이면
    한 화면에 보이는 스킬 수가 줄어든다. 창을 키우는 쪽을 골랐다.
  - ⚠ **`ui_lint` 경고 191건은 기존 오탐이다** (격자 칸이 런타임 배치라 `.ui` 좌표가
    전부 0,0 — T20-3에 기록됨). 이번에 1건 늘어난 것은 새로 넣은 `XIcon`이다.
  **T24-9b 닫기 X를 줄이되 누름판은 유지 ✅** (대표 판정: *"x크기가 적당하지는 않은거
  같아보여 줄이는게 나을거 같아"*. **그림과 누르는 면적을 갈랐다** — 버튼은 알파 0인
  **88x88 누름판**으로 남기고, X는 **40x40 자식**으로 그린다.
  실측: 버튼 88x88 alpha 0 · 레이캐스트 true / X 40x40 · 레이캐스트 false) /
  다음: 대표 플레이 판정 — **닫기가 눌리는지**를 꼭 봐 주실 것
  - **T24-9에서 88로 키운 것이 틀린 해법이었다.** 터치 목표를 지키려고 **그림째** 키웠는데,
    지켜야 할 것은 **누르는 면적**이지 그림 크기가 아니다. 둘을 가르면 둘 다 만족한다.
  - ⚠ **자식의 `RaycastTarget`을 꺼야 한다.** 안 끄면 X가 클릭을 먼저 먹어
    부모 버튼까지 안 갈 수 있다.
  - ⚠ **알파 0인 그림도 레이캐스트는 받는다** (uGUI 기본). 이 패턴이 성립하는 근거다 —
    다만 **합성 클릭이 UI에 안 먹어 실제 클릭은 못 쟀다.** 만약 닫기가 안 눌리면
    원인은 여기다: 누름판 색의 알파를 0 → 0.004처럼 아주 작은 값으로 올리면 된다.
  **T24-9 가방 창을 MSW 프리셋 창으로 ✅** (창 스킨의 **첫 사례**. 짙은 남색 판 +
  흰 글자 → **밝은 본체 + 짙은 제목 바 + 오른쪽 위 X**. 실측: 제목 바에 `가방`,
  줄이 띠로 갈리고, 글자가 어두워져 읽힌다) /
  다음: 대표 플레이 판정 → 통과하면 **같은 레시피를 나머지 창에** (스탯·스킬·환생·여신·지역선택)
  - ⚠⚠ **프리셋 스프라이트는 전부 흰 그림이고 색은 `Color`가 만든다.** RUID만 옮겼더니
    **흰 판 위에 흰 제목 바·흰 X**가 그려져 아무것도 안 보였다 (엔티티는 Enable·Visible
    전부 true라 로그로는 멀쩡해 보인다 — 실측으로 한 번 헛짚었다).
    프리셋에서 **색까지 같이 읽어 올 것**: 창 `0.98,0.97,0.96` / 제목 바 `0.11` /
    스크롤 영역 `0.91,0.90,0.88`.
  - ⚠⚠ **프리셋 RUID는 공개 리소스 검색에 없다** (`getResource` 404). Maker 편집기
    자산이라 썸네일을 못 받는다. **`ui/WorldMap.ui`의 프리셋을 런타임에 잠깐 켜서**
    화면으로 보는 것이 유일한 확인법이다 (플레이를 멈추면 원래대로 돌아간다).
  - **본체가 밝아지면 글자를 전부 뒤집어야 한다.** T24-2b 상태창과 같은 규칙이다 —
    색은 글씨와 바탕이 한 쌍이라 한쪽만 바꾸면 사라진다.
  - **줄 목록은 그대로 뒀다.** 프리셋은 154x154 칸 격자인데 우리는 줄 목록이고,
    격자로 바꾸면 `InventoryPanel`을 다시 써야 한다 — 지시 밖이다. 껍데기만 옮겼다.
  - ⚠ **닫기 버튼은 프리셋(38x38)을 안 따랐다.** `ui_lint` L007이 모바일 터치 목표
    88을 못 넘는다고 잡았고, 대표가 모바일을 우선한다고 했으므로 **88x88**로 키웠다.
    제목 바가 107이라 들어간다. 커 보이면 여기를 줄이면 된다.
  - ⚠ **줄 높이 82는 L007을 안 지킨 채로 뒀다** (648 폭이라 실제 터치 면적은 충분하다).
    88로 올리면 여섯 줄 + 안내가 1040 안에 안 들어가고 창이 화면 높이를 넘는다.
  **T24-8 물약 칸 가운데 정렬 + 글자 읽히게 ✅** (대표 지적: *"물약 칸 체크해줄래?
  중앙으로 되어 있지 않은 느낌인데 이거도 개수가 흰색이라 잘 안보이네"*.
  둘 다 맞았고 **둘 다 T24-7에서 내가 만든 것**이다) /
  다음: 대표 플레이 판정
  - **가운데가 아니었던 이유**: 칸을 96 → 122로 키우면서 아이콘을 **위로 8** 올려 뒀다.
    스킬 칸은 아래에 이름표가 있어 올릴 이유가 있었는데 **물약 칸에는 이름표가 없다.**
    규격을 베낄 때 딸려 온 값이다 — `pos (0, 8)` → **`(0, 0)`**.
  - **개수가 안 보였던 이유**: 외곽선 굵기가 **0.25**였다(원래부터). 96 칸에서는 개수가
    어두운 테두리 위에 앉아 버텼는데, 122로 커지며 **밝은 물약 그림 위로 올라와** 묻혔다.
    흰 글자 + **검은 외곽선 3** + 굵게 + 22로 고쳤다 (T24-2b와 같은 규칙 —
    색은 글씨와 바탕이 한 쌍이다).
  - **키 힌트도 같이 고쳤다.** 같은 결함(외곽선 0.25)이고 흐린 청회색이라, 스킬 칸의
    키(연노랑 + 검은 외곽선)와 나란히 놓이면 한 묶음으로 안 읽힌다.
  - ⚠⚠ **`OutlineWidth`는 런타임 조회로 못 잰다.** `.ui`에 3이 들어 있어도 실행 중에
    읽으면 **0.0**이다 — 외곽선이 눈으로 확인된 HUD 레벨 글자까지 0.0으로 나온다(실측 3종).
    `OutlineColor`는 제대로 온다. **글자 외곽선은 화면으로 확인할 것.**
  - ⚠ **UI 자식의 앵커는 부모 사각형 기준이다.** 자식 좌표를 화면 크기(1920x1080)로
    환산하면 칸 밖 수백 px짜리 엉뚱한 값이 나온다 (이번에 한 번 헛짚었다).
    자식은 **부모 rect 기준**으로 풀 것.
  **T24-7 스킬 바를 아이콘 슬롯으로 ✅** (글자 버튼(128x88) → **아이콘 슬롯(122x122)**.
  규격은 MSW 프리셋 "스킬퀵슬롯"에서 읽었다 — 칸 122 · 테두리 `5413ab88…`.
  포션 2칸도 **같은 규격**으로 맞췄다. 실측: `몸통 박치기` 아이콘 72x72 · 키 `Z` ·
  쿨 6초에 숫자 `6` + 아이콘 어두워짐) /
  다음: 대표 플레이 판정
  - **왜 지금인가**: T20-5/T20-6에서 스킬 아이콘 10종을 **직접 그려 놓고도** 스킬 창에서만
    쓰고 있었다. 정작 판마다 보는 스킬 바가 글자뿐이라, 우측 아래 조작 묶음에서
    스킬 칸만 혼자 다른 물건처럼 보였다.
  - **한 칸을 자식 넷으로 갈랐다** — `Icon` / `Name` / `Key` / `Cool`. 예전에는 넷을
    글자 한 줄에 이어 붙였는데(`이름 [Z]\n3초`) 아이콘을 넣을 자리가 없었다.
  - **이름은 남겼다.** 원작 퀵슬롯에는 없지만 아이콘 10종이 아직 손에 안 익었고,
    이름을 빼는 것은 지시 밖이다. 빼고 싶으면 `Name` 자식 하나만 지우면 된다.
  - **쿨다운은 숫자 + 아이콘 어둡게** 둘 다 쓴다. 숫자만이면 판이 바쁠 때 안 보인다
    (EquipPanel이 잠긴 스킬을 죽이는 것과 같은 방식).
  - ⚠ **아이콘 비율은 데이터에서 읽는다** (`SkillTable.icon_ratio`). `FitIcon`을
    EquipPanel에서 그대로 가져왔다 — `PreserveSprite=AspectOnly`도 `SetNativeSize()`도
    여기서 못 쓴다(T17-4c · T20-4 · T20-6에 이유가 적혀 있다).
  - ⚠ **칸이 88 → 122로 커져 포션 줄을 밀어 올려야 했다** (y 120 → **154**).
    안 올리면 스킬 줄(20~142)과 겹친다. 두 줄은 오른쪽 끝(1900)을 공유한다.
  - ⚠ **`slotStepX`를 같이 고쳐야 한다** (140 → **132**). 자리는 `.ui`가 아니라
    `SkillBar.Refresh`가 정하므로(T24-3), 칸 크기만 바꾸면 간격이 어긋난 채 남는다.
  - ⚠ **`maker_execute_script`의 `execution_space: server`가 클라에서 돌 때가 있다**
    (실측: `"context":"client"`). `StartCooldown`은 ServerOnly라 **에러 없이 무시됐고**
    "쿨다운이 안 걸린다"로 보였다. 표시만 볼 때는 클라 쪽 값(`clientReadyAt`)을
    직접 심는 편이 확실하다.
  - ⚠ **`ui_lint` L016(행이 화면 중앙에서 벗어남)은 오탐이다.** 우측 정렬이 의도다.
    L023(Key와 Cool 5px 겹침)은 **진짜였고** Cool 상자를 줄여 고쳤다.
  **T24-6 장착 버튼 글자를 "스킬"로 ✅** (대표 지시: *"장착 버튼에 있는 텍스트인 장착은
  이제 스킬로 바꿔도 좋을거 같아보이네"*. 창 제목이 이미 `스킬 장착`이고 안에 든 것도
  전부 스킬이라 버튼만 어긋나 있었다. `장착` → **`스킬`**) /
  다음: 대표 플레이 판정
  - **대화창은 이미 원래 크기다** (대표 요청 *"대화창 원래 크기로 돌려줄래?"*).
    T24-5에서 되돌렸고, **T24-4 이전 커밋(`6c4e58b`)의 값과 소수점까지 같다**:
    `pos (368.973, -251.805) / size (697.723, 438.755)`. 런타임 실측도 같은 값이다.
  - ⚠ **대화창은 사각형만큼 그려지지 않는다.** 칸은 698x439인데 화면에는 **아이콘과
    지금 있는 메시지만** 그려져서 늘 작아 보인다. 크기를 눈으로 재려 하면 틀린다 —
    `RectSize`로 확인할 것.
  **T24-5 버튼 3개를 우측 맨 위로 (+ 대화창 원위치) ✅** (대표 정정: *"이거 원래
  있던 자리로 돌려놔 그리고 저 자리에 버튼 3개를 축소해서 넣으라는거야 나는"*.
  스탯·장착·가방을 **우측 맨 위 한 줄**(x 1210~1690 / y 28~116)로 옮기고 235 → **152**로
  줄였다. 대화창은 **원래 자리**(x 20~718 / y 32~471)로 되돌렸다.
  실측: 셋 다 대화창·진척도와 겹치지 않고 시스템 예약 구역도 안 넘는다) /
  다음: 대표 플레이 판정 — **버튼이 눌리는지**를 꼭 봐 주실 것
  - **T24-4를 뒤집는다.** 겹침을 **대화창을 밀어서** 풀었는데 대표가 원한 것은
    반대였다 — 비켜야 하는 쪽은 버튼이다. 버튼을 치우니 대화창이 원래 자리로 돌아갔다.
  - **띠의 경계는 둘 다 남이 정한 것이다.** 오른쪽 **1710~**은 엔진 시스템 UI(친구·메뉴)
    예약 구역이고, 아래 **150~**은 방 진척도 패널이다. 그 사이가 480 x 122라
    235 폭 셋(741)이 안 들어간다 — 그래서 **152로 줄였다**(높이 88·폰트 30은 유지.
    글자가 두 자라 남는다).
  - **앵커를 top-right로 뒀다.** 진척도·미니맵과 **같은 모서리에 매달아야** 화면비가
    달라져도 한 덩어리로 붙어 있는다. 예전처럼 middle-left면 넓은 화면에서 셋만
    왼쪽으로 떨어져 나온다.
  - ⚠⚠ **`patch()`는 pivot을 유지한다** (T24-1에서 겪은 것 그대로). 앵커를 top-right로
    바꿔도 pivot은 옛 (0, 0.5)로 남으므로 **명시했다.** 안 하면 좌표가 통째로 어긋난다.
  - ⚠ **대화창이 비운 왼쪽 위는 이제 완전히 대화창 몫이다.** 거기에 다시 버튼을 놓으면
    T23-8의 "채팅이 클릭을 먹는" 사고가 그대로 돌아온다.
  - ⚠ **내 합성 클릭은 UI에 안 먹는다** (T23-8). 버튼이 실제로 눌리는지는 대표가
    눌러 주는 로그로만 잴 수 있다 — 자리는 실측했지만 클릭은 못 쟀다.
  ~~**T24-4 대화창을 왼쪽 버튼과 안 겹치게**~~ → **T24-5에 뒤집혔다** (대표 지시:
  *"대화창을 왼쪽의 버튼이랑 겹치지 않게 해주면 좋을거 같다"*. 겹침은 **진짜였다** —
  대화창 x 20~718 / y 32~471에 버튼 열 x 14~249 / y 300~588이 물려 스탯이 통째로,
  장착이 절반쯤 들어가 있었다. 그걸 **대화창을 밀어서** 풀었는데 대표가 원한 것은
  반대였다. 아래 관찰은 T24-5에도 그대로 살아 있다)
  - **왜 대화창을 옮겼나** (버튼이 아니라): 버튼을 내리면 아래에서 **모바일
    조이스틱**(x 210~410 / y 750~950)과 부딪힌다. 위로는 대화창이 화면 맨 위(y 32)부터
    시작해 비킬 자리가 없다. 대화창을 옆으로 미는 것이 아무것도 안 건드리는 유일한 방향이다.
  - ⚠ **아래 왼쪽으로는 못 보낸다.** 원작 메이플은 채팅이 왼쪽 아래인데, 여기서는
    그 자리를 **모바일 조이스틱이 쓴다.** PC에서는 안 보여서 옮겨 놓고 나서야 드러난다.
  - ⚠ **겹침은 앵커·피벗을 환산해서 재야 한다.** 대화창은 top-left 앵커 + pivot 0.5,
    버튼은 middle-left 앵커라 `anchoredPosition`을 그대로 비교하면 아무 말도 안 된다.
    화면 왼쪽 위를 (0,0)으로 환산한 사각형끼리 비교했다.
  **T24-3 조작 UI를 오른쪽 엄지 아래로 ✅** (대표 지시: *"스킬 우측으로 빼줄래?
  그리고 포션도 그 위에 올려줬으면 해 모바일에서 편하게 하도록 하려고
  그리고 점프키랑 공격 키 없어도 될테니 모바일에서 없애주면 좋을거 같네"*.
  스킬 5칸 아래 가운데 → **오른쪽 아래 구석**(y 20), 포션 2칸을 **그 바로 위**(y 120).
  모바일 점프·공격 버튼은 **지웠다**) /
  다음: 대표 **실기(모바일)** 판정
  - **셋을 한 번에 한 이유는 셋이 같은 사각형을 다투기 때문이다.** 점프·공격 버튼이
    204x204 두 개로 오른쪽 아래를 통째로 먹고 있어 그걸 안 치우면 스킬을 뺄 자리가 없다.
  - **점프·공격을 없애도 되는 근거**: 이 게임은 **탑다운(RectTile)**이라 점프라는 동작
    자체가 없고, 공격은 **자동**이다(2026-08-13 결정). 둘 다 눌러도 아무 일이 없는 버튼이었다.
  - ⚠⚠ **`Enable=false`로는 못 끈다.** 두 버튼은 `ActivePlatform=Mobile`이라 **PC에서는
    엔진이 알아서 꺼 둔다** — 그래서 PC 로그의 `Enable=false`는 내가 끈 증거가 아니다
    (조이스틱도 똑같이 false로 나온다). 모바일에서 엔진이 다시 켤 수 있어 **엔티티를
    지웠다.** `PlatformType`에는 PC(1)·Mobile(2)·All(255)뿐이고 **"어디에도 안 뜸"이 없다.**
  - 되살리려면 `ui/DefaultGroup.ui`에 버튼 둘을 다시 놓는다 (bottom-right, 204x204,
    공격 (-174.9, 332) / 점프 (-392, 193.57), 컴포넌트는 Transform+Sprite+Button뿐).
    **스크립트에서 참조하는 곳은 없다** — 엔진이 이름으로 찾아 쓴다.
  - ⚠ **스킬 칸의 자리는 `.ui`가 아니라 `SkillBar.Refresh`가 정한다.** 슬롯이 차수에
    따라 1~5로 변하는데 `.ui`에 5칸을 고정으로 박으면 **1칸일 때 버튼 하나가 화면
    가운데쯤 외따로 떠서** 물약과 멀어진다 (실측 — 처음에 그렇게 났다).
    `GetActiveSkills`가 **구멍 난 목록**을 줄 수도 있어 번호대로 놓으면 빈자리가 남는다.
    그래서 **찬 칸만 순서대로 오른쪽 끝부터** 채운다.
  - **칸 순서는 안 뒤집었다.** Skill1이 여전히 가장 왼쪽이라 키 `Z X C V B`와 화면이
    같은 방향으로 읽힌다. 오른쪽 정렬은 자리만 바꾸고 순서는 안 바꾼다.
  - 실측: `.ui`에는 Skill1이 -580인데 런타임에 **-20으로 옮겨져** 구석에 붙었다.
    포션 2칸은 바로 위(y 120)에 오른쪽 끝을 맞춰 섰고, `DefaultGroup` 자식은
    `UIJoystick`과 `UIChat` 둘만 남았다 (조이스틱 `Axis_8` 유지).
  **T24-2b 상태창을 아래 가운데로 + 글씨 검정 ✅** (대표 지시: *"hp 상태창 있잖아 그거는
  좀 중앙 아래쪽으로 배치해주면 안될까? 그리고 글씨가 흰색으로 되어 있어서 좀 잘
  안보여져서 검은색으로 하자"*. 왼쪽 위 → **아래 가운데**, 흰 글씨 → **검정**.
  실측: `Lv 20` · `dasdf` · `1000 / 1000` · `18 / 172`가 화면 아래 가운데에서 읽힌다) /
  다음: 대표 플레이 판정
  - ⚠ **아래 가운데는 이미 스킬 바가 쓴다** (bottom-center y 120~208). 그래서 그
    **아래**(y 8~112)에 앉혔다 — 원작 메이플도 게이지가 맨 아래, 퀵슬롯이 그 위다.
    실측으로 두 사각형이 안 겹치는 것을 확인했다.
  - ⚠ **글씨 색만 바꾸면 이름이 사라진다.** 프리셋 이름판이 **검정 30%**라 검은 글씨가
    통째로 묻힌다. 판을 **흰색 72%**로 뒤집어야 한다 — 색은 글씨와 바탕이 한 쌍이다.
  - ⚠ **외곽선도 같이 뒤집는다.** 검정 글씨에 검정 테두리를 두면 획이 뭉갠다.
    흰 외곽선을 둬야 빨강·연두 게이지 위에서도 글씨가 산다.
  **T24-2 상태창을 MSW 프리셋으로 교체 ✅** (대표가 Maker 프리셋 목록에서 실물을
  가져다 줬다 — **상태창·인벤토리·스킬퀵슬롯·월드맵 4종**이 `ui/WorldMap.ui` 안에
  들어와 있다. 그중 **상태창(UIMyInfo)**을 우리 `PlayerHud`에 그대로 재현했다.
  실측: `Lv 19` / 닉네임 / 검정 30% 이름판 / HP·EXP 게이지가 프리셋 그림 그대로) /
  다음: 대표 플레이 판정
  - **프리셋 파일은 워크스페이스에 없다.** Maker 편집기 라이브러리에만 있고 월드에
    **넣어야** `.ui`로 생긴다. MCP에 넣는 도구는 없다 — 대표가 직접 넣어 줘야 한다.
  - **프리셋은 넣을 때 선택돼 있던 그룹 안으로 들어간다.** 이번엔 `ui/WorldMap.ui`
    안으로 들어갔다. 참고용으로 남기되 **비활성화**해 뒀다 — 안 끄면 화면에 그대로 뜬다.
  - ⚠⚠ **프리셋 상태창의 `info_top` 배경은 alpha 0이다.** 판이 보이는 게 아니라
    **이름 뒤에만 검정 30% 판**을 깐다. 둘 다 불투명하게 두면 흰 슬래브가 되어
    흰 글자가 통째로 묻힌다 (실측 — 처음에 그렇게 났다).
  - **게이지는 색으로 구분한다.** 홈·채움 스프라이트는 HP·MP·EXP가 전부 같은 RUID이고
    `Color`만 다르다 (HP 1,0.4,0.4 / EXP 0.66,0.91,0.36).
  - **채움 막대는 왼쪽 pivot이어야 한다.** 가운데 pivot이면 폭을 줄일 때 양쪽에서 줄어든다.
  - **MP 줄은 뺐다** — 이 게임에 MP가 없다. 프리셋 3단(HP/MP/EXP)을 2단으로 줄였다.
  - 우리 `PlayerHud` 로직은 **한 줄도 안 고쳤다** — 프리셋과 같은 방식(게이지 폭을
    줄여 채움)이라 바인딩만 새 엔티티로 옮기면 됐다. 닉네임 한 줄만 더했다.
  **T24-1 왼쪽 버튼 열 정렬 ✅** (스탯·장착·가방을 레퍼런스 `ButtonGroup` 규격으로 통일 —
  235x88, 한 열, 세로 간격 99, 버튼 스프라이트 `6efba31a…`(primary action button).
  실측: 세 버튼이 균등 간격 한 열로 정렬된 화면 확인) /
  다음: 대표 플레이 판정
  - **앵커만 레퍼런스와 다르게 뒀다.** 레퍼런스는 `Center` 앵커라 화면이 넓어지면
    버튼이 왼쪽 가장자리에서 떨어져 나온다. `middle-left`로 두면 1920x1080에서는
    **같은 자리**(x=-829 → 왼쪽에서 131px)이면서 다른 화면비에서도 가장자리에 붙는다.
  - ⚠⚠ **`patch()`는 pivot을 유지한다** (builder-protocol-ui §3.4). 앵커를 바꿔도
    pivot은 안 따라오므로 **명시해야 한다.** 안 했더니 원래 top-left였던 둘(pivot 0,1)과
    bottom-left였던 가방(pivot 0,0)이 섞여 **가방이 장착 위에 겹쳐 그려졌다** —
    그룹 순서가 높은 가방(10)이 장착(6)을 덮어 "장착 버튼이 사라진" 것처럼 보였다.
    엔티티는 Enable·Visible·alpha 전부 정상이라 로그로는 안 잡힌다.
  - ⚠ **스프라이트 RUID를 넣을 때 `Color`를 흰색으로 되돌릴 것.** 빌더 기본값이
    어두운 반투명이라 그대로 두면 버튼 아트가 그 색으로 물든다.
  - `EquipWindow`의 `ui_lint` 경고 190건은 **기존 오탐**이다 (격자 칸이 런타임 배치라
    `.ui` 좌표가 전부 0,0 — T20-3에 기록됨).
- 진행 중: **T23 다듬기** (지시서 밖, 2026-08-24 대표 지시 묶음).
  **T23-11 마을 확장 + 사방 길 (+ 진척도에서 포션 제거) ✅** (대표 지시: *"잔디 마당인데
  길이 사방으로 이어져 있는? NPC로 이어지면 좋을거 같고 상하좌우로 길 넣을거니까"* +
  *"드랍 UI에 아직 포션의 내역이 출력되고 있네 저거 없애고 기본으로 드랍되게"*.
  마을 112칸 → **448칸(28x16, 사냥방과 같은 규격)**, 잔디 마당에 사방 돌길 + 가운데 광장.
  NPC를 길 끝으로 옮겼다: 환생 서쪽(-12,1) / 여신 북쪽(0,7) / 동쪽 문(13,1)) /
  다음: 대표 플레이 판정
  - ⚠⚠ **`RoomTable.portal_x`는 그 맵의 반폭이다.** 마을을 넓혔으면 6 → **13**으로
    같이 고쳐야 한다. 안 고치면 사냥방에서 마을로 들어올 때 타일 밖에 떨어져
    문 트리거에 얹혀 즉시 쫓겨난다 (T13-1c에서 겪은 그 사고다).
  - **타일 재질을 처음에 거꾸로 알았다** (실측으로 잡음):
    타일 **없음** = 갈색 맨땅(배경) / `Henesys_soil_*` = **잔디** /
    `Henesys_stone_*` = 돌 포장 / `Henesys_water_*` = 물(8종 통과 불가).
    이름만 보고 soil을 흙이라 짐작하면 틀린다 — **깔아 보고 확인할 것.**
  - **`_1`이 민무늬 채움이고 `_3/4/5`는 테두리가 아니라 잔무늬 변형**이다.
    기존 맵이 `_1` 93% + `3/4/5` 흩뿌림으로 칠해져 있다 — 같은 방식을 따랐다(10%).
  - ⚠ **타일 칠하기는 MapBuilder 커버리지 밖이라 `tileMap` 배열만 직접 썼다**
    (builder-protocol-map §1.6이 갭에 한해 허용). 형식은 `{type, position, tileIndex}`로
    단순하고 `tileIndex`는 `.tileset`의 `datas` 배열 순번이다. 엔티티 이동은 빌더가 했다.
  - **난수에 고정 씨앗을 썼다.** 실행할 때마다 무늬가 달라지면 지도를 손볼 때
    "내가 뭘 바꿨는지"를 diff로 못 읽는다.
  - **진척도 HUD에서 소비 아이템을 뺐다.** 포션이 `drop_from=*`(T23-5)가 되면서
    **모든 방에 늘 뜨는 줄**이 됐고, 3줄 제한 때문에 정작 모아야 할 장비(2~5%)를
    밀어냈다. 그 자리는 "이 방에서 뭘 더 모아야 하나"를 적는 곳이고,
    포션은 모으는 것이 아니라 쓰는 것이다. **드랍 자체는 그대로다.**
  **T23-10b 지도를 우측 상단 상시 미니맵으로 ✅** (대표 정정: *"전직 스킬을 얻을 수 있는
  시험의 방은 표기 안해주는게 맞을거 같아보이네"* + *"전체 맵을 열 수 있는 방식이 아니라
  우측 상단에 배치해서 볼 수 있도록"*.
  전체 화면 창 → **우측 상단 상시 HUD**, 시험의 방 **제외**) /
  다음: 대표 플레이 판정
  - **규칙을 "시험의 방을 빼라"가 아니라 "연결로 닿는 방만 그린다"로 뒀다.**
    시험의 방은 숨은 NPC로만 가는 방이라 지도에 적으면 숨긴 의미가 없어진다.
    규칙으로 두면 앞으로 숨은 방을 더 만들어도 여기를 고칠 일이 없다.
  - **열고 닫는 창이면 안 보게 된다.** 지도는 "지금 어디쯤인가"를 곁눈질하는 것이다.
  - **암막도 버튼도 없앴다** — 레이캐스트를 먹는 요소가 **0개**다(실측).
    T23-8에서 겪은 "UI가 클릭을 먹는" 사고의 원천을 아예 없앴다.
  - **자리는 방 진척도 패널(우측 상단 y 150~350) 아래**(y 370~580), 폭도 440으로 맞춰
    한 줄로 정렬돼 보이게 했다. 시스템 UI 예약 구역(220x130)은 피했다.
  - **지역 밖에서는 숨긴다.** 마을은 모든 지역의 출입구라 어느 지역 지도를 띄워도
    거짓말이 되고, 숨은 방은 지도에 없는 방이다.
  - **지금 서 있는 방에는 "잠김"을 안 적는다.** 이미 들어와 있으니 잠금은 지난 이야기고,
    그 자리에서 알고 싶은 것은 무엇이 사는지다.
  - ⚠ **긴 방 이름은 `bestfit`으로 줄인다.** 안 하면 "이끼 낀 관문"이 두 줄로 깨진다(실측).
  - ⚠ **0.5초 타이머로 갱신하되 방이 바뀌었을 때만 다시 그린다.** 매번 다 그리면
    방마다 게이트 판정을 계속 돌리게 된다.
  **T23-10 월드 맵 보기 UI ✅** (대표 지시: *"area로 가면 각 맵의 구성을 표시해주는
  해당 월드의 맵 보기ui를 넣어주면 좋을거 같아보이네"*. 화면 왼쪽 "지도" 버튼으로 열면
  지역의 방들이 **연결 그대로** 격자에 그려진다 — 방 이름 / 몬스터와 마릿수 /
  잠긴 문의 사유. 실측: 무너진 서고가 이끼 낀 관문 **북쪽**에 붙고, 나머지는 한 줄로
  이어지며, 연결이 없는 시험의 방은 아래에 따로 선다) /
  다음: 대표 플레이 판정
  - **지도를 손으로 배치하지 않는다.** `RoomTable`의 연결을 **너비 우선으로 훑어
    격자 좌표를 매긴다.** 방을 추가하면 CSV 한 줄로 지도에 나타난다.
  - **잠김은 진짜 게이트에 물어본다** (`_RoomGate:Evaluate`). 여기서 조건을 다시
    해석하면 판정이 두 곳에 생겨 "지도엔 열려 보이는데 문은 막힌" 상태가 난다.
  - ⚠⚠ **방 연결은 `room.conn_east`가 아니라 `room.connections.east`다.**
    `GameData.LoadRooms`가 4컬럼을 한 테이블로 접는다. 컬럼 이름으로 읽었더니
    전부 nil이 되어 **6개 방 중 5개가 "연결 없음"으로 한 줄에 늘어섰다** (실측).
    읽는 창구는 `_GameData:GetRoomConnection(id, "east")` 하나로 둘 것.
  - ⚠ **top-center 앵커에서 자식의 y는 음수다.** 양수를 주면 칸 **위로** 올라가
    글자가 상자 밖에 뜬다 (실측 — 처음에 그렇게 났다).
  - ⚠ **이모지를 쓰지 말 것.** 자물쇠 이모지가 □로 깨졌다 (빌더 문서의 경고 그대로).
    기호는 `●` `◆` 처럼 기본 폰트에 있는 것만 쓴다.
  - **여는 버튼은 `Root` 밖에 둔다.** 안에 두면 창을 닫는 순간 여는 버튼까지 사라진다.
    자리는 **키 설정 버튼이 있던 곳**(bottom-left, y=300)을 그대로 물려받았다.
  - ⚠ **칸·연결선은 풀에서 재사용한다**(칸 12 / 선 16). `.ui`는 정적이라 방 수만큼
    만들 수 없다. 넘치면 경고를 남긴다 — 조용히 잘리면 아무도 못 알아챈다.
  **T23-9b 여신 안내를 창으로 ✅** (대표 지시: *"메시지가 뜨는게 아니라 환생 창처럼
  나와주면 좋겠네"* + *"그때 열림 : 오라 블레이드 같은거는 빼주는게 좋을거 같아 …
  스킬 창에만 알려주는게 좋을거 같아보이네"*.
  배너 → **창**(`ui/GoddessWindow.ui` + `UI/GoddessPanel.mlua`), 열릴 스킬 이름 **삭제**) /
  다음: 대표 플레이 판정
  - **배너와 창은 쓰임이 다르다.** 배너는 2.5초 뒤 사라져 "잠겼다" 같은 한 줄 통보에
    맞고, 전직 안내는 **읽고 판단하는 정보**라 사라지면 다시 말을 걸어야 한다.
  - **열릴 스킬을 빼는 이유는 확장성이다** (대표 지적). 차수·직업이 늘수록 이 창이
    목록으로 변해 간다. **무엇이 열리는지는 스킬 창의 일**이고(거기엔 이미 `[잠김] N차`가
    있다), 여신은 **언제 열리는지**만 말한다.
  - **NPC는 문구를 만들지 않는다.** 터치를 받아 창을 열기만 하고 내용은 창이 정한다 —
    RebirthNpc → RebirthConfirmPanel과 같은 꼴이다. 두 곳에서 만들면 언젠가 어긋난다.
  - **규칙 문구(`RuleText`)는 `.ui`에 정적으로 둔다** — 대표가 Maker에서 직접 고칠 수
    있어야 한다(환생 창과 같은 판단). 스크립트가 채우는 것은 살아 있는 숫자뿐이다.
  - ⚠ **암막이 딸린 창은 반드시 닫아 두어야 한다.** 켜진 채로 남으면 최상단 그룹의
    전체 화면 레이캐스트가 화면 전체의 클릭을 먹는다 (T23-8에서 겪은 것과 같은 꼴).
    실측으로 "닫은 뒤 암막이 클릭을 먹는 상태인가 = false"를 확인했다.
  - ⚠ **`ui_lint` L023(겹침)이 실제 버그를 잡았다.** 창 높이 600에 본문이 575까지
    차서 닫기 버튼과 겹쳤다 — 760으로 키웠다. L009(팝업 `DefaultShow=true`)는
    **의도된 예외**다: 그룹을 끄면 컨트롤러의 `OnBeginPlay`가 안 돌아 창을 열 수 없다.
  **T23-9 여신 NPC (전직 안내) ✅** (대표 지시: *"전직 npc는 메이플스토리 여신으로 해줘.
  그리고 차수 전직을 안내하는 역할이야"*. 마을 오른쪽(3,3)에 **메이플월드의 여신**이
  서 있고, 누르면 지금 차수 · 슬롯 · 다음 전직 레벨 · 그때 열릴 스킬을 알려 준다.
  실측 3종: `2차 / 슬롯 3칸 · 다음 전직 Lv60 (54 남음) · 그때 열림: 오라 블레이드` /
  `아직 전직 전 / 슬롯 1칸 · 다음 전직 Lv20 (14 남음) · 그때 열림: 슬래시 블러스트, 아이언 바디` /
  `5차 / 슬롯 5칸 · 마지막 차수까지 왔다`) /
  다음: 대표 플레이 판정
  - **전직을 시키지 않는다. 알려 줄 뿐이다.** 차수는 레벨로 저절로 오르므로
    (`GetTier`), 여신이 전직을 실행하면 판정 규칙이 두 곳에 생겨 언젠가 어긋난다
    (RebirthNpc가 환생을 직접 실행하지 않는 것과 같은 판단).
  - **안내 문구가 T23-7과 한 몸이다.** 차수가 환생해도 유지되므로 "환생하면 다시
    잠긴다"고 적으면 여신이 거짓말을 한다. 그래서 규칙(T23-7)을 먼저 넣고 NPC를 뒤에 만들었다.
  - **안 가진 스킬은 안 적는다.** 없는 것을 미리 알려 주면 안내가 아니라 스포일러가 되고,
    목록이 길어져 정작 내 것이 안 보인다. 3개까지만 적고 넘치면 `외`를 붙인다.
  - **클라 전용이다.** 읽는 값이 전부 @TargetUserSync라 서버를 왕복할 이유가 없고,
    아무것도 바꾸지 않으므로 위조 걱정도 없다 — 틀린 안내를 자기 화면에 띄울 뿐이다.
  - ⚠ **`TeleportToMapPosition`의 인자는 `(엔티티, 좌표, 맵이름)`이다.** 맵을 먼저 넘겼다가
    `[LEA-3005] InvalidArgument`를 맞았다. 맵 이름에 `map://` 접두어도 붙이지 않는다.
  - ⚠ **여신 스프라이트는 `animationclip`이다**(223x274). `SpriteRUID`는 클립을 그대로
    받아 프레임을 다 재생하므로 오라가 살아 움직인다. 정지 그림을 쓰면 밋밋해진다.
  **T23-8 스탯·장착 버튼 먹통 수정 + 키 설정 버튼 제거 ✅** (대표 발견:
  *"키 설정 버튼 안없앤거 같고 스탯이랑 장착 버튼이 안눌리네"*.
  원인: **`ui/RebirthConfirm.ui` 안에 `DefaultGroup`의 채팅창이 통째로 복제**돼
  있었다 — 좌표·크기가 바이트 단위로 같고 `ChatComponent`까지 붙어 있었다.
  `RebirthConfirm`은 환생 창을 가방 창 위에 띄우려고 **최상단 그룹**으로 올려 둔 곳이라,
  거기 얹힌 채팅창이 **그 사각형 안의 클릭을 전부 가로챘다**) /
  다음: 대표 플레이 판정
  - **왜 위쪽 둘만 안 됐나**: 채팅창이 덮는 범위가 x 20~718 / 화면 아래 기준 y 609~1048이다.
    스탯(y 832)·장착(y 756)은 그 안이고 가방(~400~488)·키설정(~300~388)은 밖이다.
    그룹 순서(4·6 vs 9·10)는 우연이었고 **진짜 기준은 채팅창 사각형**이었다.
  - ⚠ **채팅은 월드에 하나만 둔다.** 두 개가 겹치면 위에 있는 쪽이 조용히 클릭을 먹는다.
    `.ui`를 통째로 복사·병합할 때 딸려 들어오기 쉽다 — 다른 `.ui` 12개는 깨끗했다.
  - ⚠ **`ChatComponent`는 자식 엔티티에 `RaycastTarget`을 안 남긴다.** 그래서 "레이캐스트
    켜진 것 찾기"로는 안 잡힌다. 겹침을 의심하면 **엔티티 목록에서 채팅을 직접** 찾을 것.
  - ⚠⚠ **내 합성 클릭(`maker_mouse_input`)은 UI에 안 먹는다.** `ScreenTouchEvent`까지는
    들어오는데 `ButtonClickEvent`가 안 뜬다 — 정상 동작하는 버튼에서도 그렇다.
    **UI 클릭 판정은 대표가 눌러 주는 로그로만 잴 수 있다** (키 입력과 같다).
  - ⚠ **내가 한 번 오진했다.** 런타임에서 `WorldPosition`(월드 단위)과 `RectSize`(픽셀)를
    섞어 계산해 "버튼 넷이 44px씩 겹친다"고 진단했는데 **틀렸다.** 앵커 좌표로 보면
    위에서 190·290·592·692px로 깨끗하게 떨어져 있다. **UI 좌표는 `.ui`의 앵커 값으로 볼 것.**
  - **키 설정 버튼 제거**: 대표 지시(*"키 설정은 없어도 될거 같아"*)를 앞서 T13-5c 태스크
    취소로만 좁게 받아들여 버튼을 안 지웠었다. 이번에 화면에서 없앴다.
    **창과 스크립트는 남긴다** — 키 배치 저장·적용을 스킬 바가 계속 쓰고 있어
    지우면 키가 전부 초기화된다. 되살리려면 `.ui`에 버튼을 다시 놓고 `openBtn`에 물린다.
  **T23-7 전직 차수 환생 유지 ✅** (대표 지시: *"전직퀘스트는 한번만 하면 되는거야
  따라서 환생하면 전직 차수는 유지되는 형식"*. 차수를 **영구 기록**으로 만들었다 —
  `BestTier`(갱신형) vs `TierFromLevel()` 중 큰 쪽이 `GetTier()`다.
  실측: Lv30 → 차수 2 슬롯 3 → **환생 후 Lv1인데 차수 2 슬롯 3 유지** /
  저장·복원 왕복 `Perm.best_tier=2` 확인) /
  다음: 대표 플레이 판정
  - **슬롯도 같이 유지된다** (슬롯 = 차수 + 1). 전에는 환생하면 1칸으로 돌아갔다 —
    환생의 비용이 그만큼 가벼워진다.
  - ⚠ **`RecomputeSlotCount`가 `RefreshBestTier`를 먼저 부른다.** 순서가 바뀌면
    전직한 그 순간에는 옛 차수로 칸을 세고 다음 갱신 때에야 늘어난다.
  - ⚠ **로드 순서 함정이 이걸로 완화됐다.** 전에는 `ApplyRun`(레벨 복원) 뒤에
    슬롯을 세야 했는데, 이제 차수가 영구 기록에서도 나오므로 `ApplyPerm`만으로도
    옳은 값이 나온다. 그래도 순서는 그대로 두었다 — 첫 전직 순간은 레벨이 근거다.
  **T23-6 스킬 쿨다운 표시 ✅** (T23-3/4/5에서 찾은 것을 고쳤다. **스킬 바에 쿨다운이
  한 번도 안 보였다** — 서버는 5초 남았다고 보는데 클라는 0.00으로 읽었다.
  원인은 포션과 같다: `SkillReadyAt`이 **서버 시각**인데 `SkillBar`가 **클라 시계**로
  뺐다. 서버는 `StartCooldown`에서 **길이(초)**만 보내고 클라가 자기 시계로 센다.
  같이 한 것: **쿨다운을 거는 곳을 네 군데에서 한 군데로 모았다**(`StartCooldown`)) /
  다음: 대표 플레이 판정
  - **창구를 하나로 모은 이유가 곧 T23-1이다.** 일반·방어·던지기·돌진 네 갈래가
    같은 두 줄을 손으로 적고 있었고 **던지기에서 빠뜨려 바위가 무한 연타됐다.**
    갈래가 늘 때마다 다시 빠뜨릴 자리가 생기므로 창구를 하나로 둔다.
  - ⚠ **`EquipMonsterSkill`은 클라에서 안 먹는다** (서버 권한). 클라 스크립트로
    끼우고 시험했더니 슬롯이 그대로여서 **엉뚱한 스킬을 재고 있었다** (실측).
    장착은 서버에서, 사용·표시는 클라에서 재야 한다.
  **T23-3/4/5 포션 3종 ✅** (대표 지시 묶음: *"맵에서 포션 드랍되는 내역은 출력되지
  않도록"* / *"몬스터들이 다 포션을 드랍하도록"* / *"포션도 쿨타임이 있으면 좋을거 같아
  한 3초정도"*.
  **드랍**: `ItemTable.drop_from`이 `*`(모든 몬스터)와 파이프 목록을 받는다 —
  포션을 `*`로 뒀다. 몬스터를 나열하면 새 몬스터를 넣을 때마다 이 줄을 고쳐야 하고,
  안 고치면 그 몬스터만 조용히 포션을 안 떨군다.
  **표시**: 소비 아이템은 획득 배너를 띄우지 않는다. 알림의 값어치는 희소성에서 나온다 —
  포션이 사냥 내내 뜨면 정작 알려야 할 장비 드랍(2~5%)이 그 사이에 묻힌다.
  **쿨다운**: `potion_cooldown` 3초. **종류별이 아니라 하나**다 — 종류별이면 물약을
  두 종 들고 번갈아 마셔 쿨을 없앨 수 있다. 퀵슬롯은 아이콘을 어둡게 깔고 남은 초를 띄운다) /
  다음: 대표 플레이 판정
  - ⚠⚠ **`_UtilLogic.ElapsedSeconds`는 서버와 클라에서 다르다** (실측 2026-08-24:
    같은 순간 클라 181.26 / 서버 87 — **94초** 차이. 다른 회차에서는 10초, 1.3초.
    **고정 오차가 아니다**). 그래서 **서버가 적은 절대 시각을 클라에서 빼면 안 된다.**
    서버는 **길이(초)**만 RPC로 보내고 클라가 자기 시계로 다시 센다 (`clientReadyAt`).
  - ⚠⚠ 같은 이유로 스킬 바의 쿨다운 표시도 깨져 있었다 (실측: 서버 5.00초 남음 /
    클라 0.00 → 화면에 아무것도 안 그렸다). → **T23-6에서 고쳤다.**
  - ⚠ **쿨다운은 효과가 실제로 들어간 뒤에만 건다.** 만피라 안 마신 경우까지 걸면
    아무 일도 없이 3초가 잠긴다. 반대로 쿨다운 검사는 **만피 검사보다 먼저** 해야 한다 —
    뒤에 두면 "체력이 가득해 쓰지 않았다"가 뜨는데 진짜 이유는 쿨다운이라 화면이 거짓말을 한다.
  - ⚠ **`GateNotice.ShowNotice`는 `Enable`이 아니라 `SetVisible`을 쓴다.** 배너가 떴는지
    `Enable`로 재려다 두 번 헛짚었다 — 글자(`titleText`/`reasonText`)를 보는 것이 맞다.
  - ⚠ **`maker_execute_script`의 서버 스크립트는 긴 `wait` 뒤에서 끊긴다** (실측:
    `wait(4.0)`/`wait(5.0)` 다음 줄이 안 돌았다). 두 단계 시험은 호출을 갈라서 할 것.
  - **대표 지시로 T13-5c(퀵슬롯 키 재설정)는 취소됐다.**
  **T23-2 바위 던지기 8방향 + 조준 ✅** (대표 발견: *"좌우로만 바위가 날아가고 몬스터가
  맞지 않는거 같아"*. 둘 다 사실이었다. **(1) 좌우뿐** — `ThrowSkill`이 조준값을 인자로
  받지도 않았다. **(2) 거의 안 맞았다** — 바위가 **항상 정확히 사거리만큼** 떨어졌는데
  T19-5에서 사거리를 3.5 → 10.5로 올려, 몬스터가 10.5±0.8에 서 있어야만 맞았다.
  **바로 옆 몬스터는 절대 안 맞았다.** 고친 방식(대표 선택): 겨눈 45도 부채꼴 안에서
  **가장 가까운 몬스터 자리**에 떨어뜨린다. 없으면 최대 사거리로 날아가 빗나간다) /
  다음: 대표 플레이 판정
  - **자동 조준이 아니다. 방향은 여전히 내가 겨눈다.** 사라진 것은 "거리까지 맞춰야
    하는" 부분뿐이고, 그건 재미가 아니라 사고였다.
  - **부채꼴 반각 22.5도는 우연이 아니다** (`projectile_aim_degrees`). 조준이 8방향
    격자라 22.5도면 **평면이 정확히 8등분**된다 — 어느 몬스터든 여덟 방향 중 정확히
    한 쪽에만 속한다. 겹치지도 비지도 않는다 (실측: 21도 몬스터는 0도로 겨눌 때만 잡히고
    45도로 겨눌 때는 안 잡혔다).
  - **몬스터 목록은 `RoomSpawner.spawned`에서 읽는다.** 그것이 스폰의 단일 원장이라,
    따로 훑으면 두 목록이 어긋난다.
  - ⚠ **T19-5(사거리 3배)가 이 스킬을 망가뜨린 것을 그때 못 봤다.** "멀리 날아간다"만
    확인하고 **맞는지를 안 쟀다.** 사거리를 건드리면 명중을 같이 재야 한다.
  - ⚠ **보스 쪽은 원래 멀쩡했다** — `MonsterAttack.ThrowSkill`은 사거리 안의 사람을
    골라 그 자리로 던진다. 플레이어 쪽만 "고정 거리"였다.
  **T23-1 바위 던지기 쿨다운 ✅** (대표 발견: *"바위 던지기가 쿨타임이 없더라고 갑자기
  연속으로 사용이 되고"*. `PlayerAttack.ThrowSkill`이 **쿨다운을 안 걸고 `return true`** 했다 —
  같은 파일의 다른 세 갈래(일반·방어·돌진)는 전부 걸고 있었고 **던지기만 빠져 있었다.**
  CSV에는 `cooldown 5`가 처음부터 있었다 — 데이터가 아니라 코드 문제다.
  실측: 1회차 `true` 남은쿨 5.00 / 2~4회차 `false` / 착탄 판정 정상) /
  다음: 대표 플레이 판정
  - **설계 이력에 적어 둔 규칙이 한쪽에만 구현돼 있었다.** 2026-08-23에 *"던지는 스킬은
    던진 것만으로 쿨다운이 돈다"*고 정해 놓고 **몬스터 쪽에만** 넣었다. 규칙을 정할 때
    "누가 쓰는 경로가 몇 개인가"를 세지 않으면 이렇게 반쪽만 들어간다.
  - ⚠ **쿨다운을 착탄 시각에 걸면 안 된다.** 비행 0.35초 동안 안 걸려 있어 **연타가 된다**
    (이번 증상). 빗나갔을 때 공짜로 다시 던지는 문제와 별개다.
  - ⚠ **빌드 로그의 `RequestUseSkill(integer activeIndex)` 서명 에러는 낡은 항목이다.**
    `maker_clear_logs` 뒤 refresh를 해도 같은 타임스탬프로 남는다. 실제 3인자 호출은
    클라·서버 양쪽에서 정상 동작한다 (실측 `ok=true`).
  - **대표 지시로 T13-5c(퀵슬롯 키 재설정)는 취소됐다** (*"키 설정은 없어도 될거 같아"*).
- 진행 중: **T22 조작 (모바일 대응)** (지시서 밖, 2026-08-24 대표 지시).
  **T22-2 모바일 조이스틱 8방향 ✅** (대표 판정: *"모바일이 상하좌우밖에 안되네?
  그리고 몸통박치기가 8방향으로 안사용되는거 같아"* — T22-1이 모바일에서 안 통한 이유.
  `ui/DefaultGroup.ui`의 `JoystickComponent.Axis`가 **`Axis_4`(0)로 눌려 있었다**.
  엔진 기본값은 `Axis_8`(1)인데 이 월드만 4방향이었다. **한 칸을 1로 바꿨다.**
  실측 `Axis=Axis_8`) /
  다음: 대표 **실기(모바일)** 판정
  - **조이스틱은 아날로그가 아니라 방향키를 눌러 주는 장치다.** `RightArrow`·`UpArrow` 같은
    키를 그대로 매핑해 두고 스틱이 그 키를 누른다 — `Axis_4`면 대각선에서 **키가 하나만**
    눌리고, `Axis_8`이면 둘이 같이 눌린다. 그래서 이 한 칸이 **이동과 돌진을 동시에** 좌우한다.
  - ⚠ **T22-1의 "모바일 조이스틱은 아날로그라 360도가 나온다"는 틀렸다.** 아날로그가
    아니므로 스냅이 모바일을 8방향으로 만든 것이 아니다. 4방향이었던 원인은 이 설정이다.
  - ⚠ **PC 경로는 원래부터 멀쩡했다** (실측). 대표가 키를 눌러 준 로그에서
    `눌린키 [RightArrow UpArrow]` → `이동입력 0.71,0.71 45도`가 그대로 나왔고
    `GetAim8`도 `-0.71,-0.71`을 돌려줬다. **엔진은 처음부터 대각선을 준다.**
  - ⚠ **내 키 입력 시뮬레이션은 게임 창에 안 들어간다** (`maker_keyboard_input`, 두 번 시도).
    그래서 T22-1을 **실제 입력으로 검증하지 못한 채 올렸다** — 값을 손으로 심어
    배선만 확인한 것이었다. **입력이 걸린 문제는 대표가 눌러 주는 로그로만 잴 수 있다.**
  - ⚠ **모바일 실기는 여전히 검증 못 했다.** Maker는 PC 플레이만 된다.
  **T22-1 스킬 조준 8방향 ✅** (대표 지시: *"몸통박치기를 쓸때 상하좌우밖에 이동이
  안되서... 360도 전체 이동되도록"* + 선택 *"8방향으로 통일해줘"*.
  돌진 방향을 **클라가 재서 서버로 실어 보낸다** — `Player/PlayerAimInput.mlua`) /
  다음: 대표 플레이 판정
  - ⚠ **`KinematicbodyComponent.MoveVelocity`는 항상 0이다** (실측). 돌진 방향을
    여기서 읽고 있었는데 늘 0이라 **바라보는 쪽(좌우)으로만** 나갔다 —
    대표가 겪은 "상하좌우밖에 안 된다"의 정체다.
  - **입력은 클라에만 있다.** 서버는 누가 어느 키를 쥐고 있는지 모른다.
    그래서 `SkillBar.Use`가 누른 순간의 방향을 재서 `RequestUseSkill`에 실어 보낸다.
  - **`ChangedMovementInputEvent`가 플랫폼 공통 창구다.** `MovementComponent`가
    이동 입력이 바뀔 때마다 `Direction`(Vector2)을 쏜다 — 키보드든 모바일
    조이스틱이든 여기로 들어온다. 그래서 한 코드로 양쪽을 덮는다.
  - **PC는 방향키 4개라 8방향이 최대다.** 조준은 `GetAim8`이 **45도 격자로 스냅**한다 —
    기기가 달라도 조작감이 같아야 한다는 대표 결정.
    ~~모바일 조이스틱은 아날로그라 360도가 나오지만~~ → **2026-08-24 정정(T22-2).
    조이스틱은 아날로그가 아니라 방향키를 누르는 장치다.** 모바일이 4방향이었던 것은
    스냅 때문이 아니라 `JoystickComponent.Axis`가 `Axis_4`였기 때문이다.
  - **엔진이 대각선을 주는지는 안 믿는다.** `GetAim8`이 눌린 방향키를 직접 세어
    대각선을 만든다. 엔진이 입력을 4방향으로 눌러도 결과가 같다.
    (실측 결과 **엔진은 실제로 대각선을 준다** — 이 이중화는 그대로 두지만
    문제의 원인은 아니었다. T22-2 참조)
  - ~~⚠ 바위 던지기는 아직 좌우뿐이다.~~ → **2026-08-24 T23-2에서 고쳤다** (8방향 +
    겨눈 부채꼴 안 가장 가까운 몬스터에 착탄).
- 진행 중: **T21 직접 만든 이펙트** (지시서 밖, 2026-08-23 대표 지시).
  **T21-2 몸통 박치기 돌진 ✅** (대표 지시: *"몸통 박치기 스킬 있잖아 원하는 위치에
  빠르게 돌진해서 추가 데미지를 주도록"* + 정정 *"원하는 위치란 가고 있는 방향쪽으로"*.
  제자리에서 때리던 스킬이 **가고 있는 쪽으로 3.5유닛 돌진하고 도착한 자리에서**
  계수 ×1.8로 때린다. 서 있으면 바라보는 쪽으로 나간다 — 안 그러면 멈춰서 쓰면
  아무 일도 안 일어나 먹통으로 보인다) /
  다음: 대표 플레이 판정
  - ⚠ **플레이어 이동은 서버가 못 민다.** 서버에서 `MoveToDirection`을 0.4초 돌려도
    **이동거리 0.00**이고, 같은 코드를 클라에서 돌리면 움직인다 (실측). 그래서
    움직임은 `Player/PlayerDash.mlua`(@Logic, 클라)가 하고 판정만 서버가 한다.
  - ⚠ **서버가 보는 플레이어 좌표는 크게 늦다.** 돌진이 끝난 시각에 실제 도착은
    -3.42인데 서버는 -1.4를 봤다. 0.15초를 더 기다려도 같았다. 그래서 살아 있는
    좌표를 안 읽고 **출발점 + 방향 x 거리**로 도착 지점을 계산해 그 자리에서 잰다.
  - **쿨다운은 떠나는 순간 건다.** 도착해서 걸면 벽에 막혀 못 간 경우 공짜로
    다시 쓸 수 있다 (던지는 스킬과 같은 규칙).
  **T21-1 몬스터 스킬 이펙트 4종 ✅** (대표 지시: *"이펙트도 직접 만들어보자"*.
  T19-2에서 비슷한 원작 스킬에서 빌려 왔던 시전 이펙트를 **직접 그린 것으로 바꿨다** —
  몸통 박치기=충격 별 / 푸른 껍질=방어막 고리 / 포자 살포=포자 구름 / 뿔 들이받기=꿰뚫는 획.
  바위 던지기는 그대로 없다 — 날아가는 바위가 곧 연출이다) /
  다음: 대표 플레이 판정
  - ⚠ **animationclip은 올릴 수 없다.** 자산 업로드가 받는 category가
    sprite / audioclip / avataritem뿐이라 `_EffectService:PlayEffectAttached`(클립 재생)를
    쓸 길이 없다. 그래서 **한 장을 띄우고 `SkillCastEffect`가 움직인다** —
    커지고·옅어지고·돌고·흐른다. 투사체(T19-3)와 같은 꼴이다.
  - **`effect_style`이 갈림선이다.** 비어 있으면 `effect_ruid`는 원작 클립(히어로 5종),
    채워져 있으면 우리 스프라이트다. 한 칸에 섞으면 런타임에 구분할 방법이 없고
    **클립 자리에 그림을 넣으면 에러 없이 안 뜬다.**
  - ⚠ **`OrderInLayer` 6으로는 캐릭터 뒤에 깔린다** (실측). 500으로 올렸다.
  - ⚠ **투명도는 끝에서만 뺀다.** 처음부터 옅어지게 했더니 0.4초짜리 연출이
    뜨자마자 묻혀 "번쩍했나?"가 됐다 (실측).
  - ⚠ **이펙트는 아이콘과 색을 똑같이 두면 안 될 때가 있다.** 포자 구름을 아이콘과
    같은 초록으로 뒀더니 **잔디 위에서 묻혔다.** 아무 배경 위에나 뜨므로 색조가 아니라
    **명도 대비**로 버텨야 한다.
  - **일반 몬스터는 스킬을 안 쓴다** (대표 확인 요청 2026-08-23). 이미 그렇게 돼 있었다 —
    `StartBossSkills`가 보스방이 아니면 물러나고, 보스방은 `monster_count=1`에
    환생 보너스도 제외라 **스킬을 쓰는 몬스터는 한 방에 한 마리뿐**이다.
- 진행 중: **T19 보스 스킬** (지시서 밖, 2026-08-23 대표 지시).
  **T19-1 보스가 자기 스킬을 쓴다 ✅** (히어로 5종 · 돌 정령 1종. 쓸 스킬은 `npc_skill_ids` →
  없으면 `drop_skill_id`에서 **유도**한다 — 새 컬럼 없음. `boss_skill_interval` 3초.
  **스킬은 평타를 대신한다** — 별도 타이머면 i-frame에 씹혀 절반이 헛돈다.
  실측: 히어로 5종 전부 100% 명중 / 돌 정령 바위 던지기 271 vs 평타 241 /
  일반 사냥방은 스킬 없음 / 빈 방에서 안 씀) /
  **T19-2 원작 이펙트·소리 ✅** (`SkillTable.effect_ruid`/`sfx_ruid` + `Combat/SkillEffect.mlua`.
  **아이콘 RUID로 원본 팩을 역추적**해 같은 팩의 `effect`/`_audio/Use`를 꺼냈다 — 10종 전부.
  방향은 서버가 `FlipX`로 넘긴다. 실측: 히어로 슬래시가 플레이어 쪽으로 뻗는 화면 확인,
  플레이어 포자 살포도 확인) /
  **T19-3 바위 던지기 투사체 ✅** (`projectile_ruid` + Body 없는 투사체 엔티티.
  포물선 + 회전. 사거리 1.0 → 3.5 — 1칸은 던지기로 안 보인다) /
  **T19-4 착탄 판정 ✅** (**바위가 닿을 때** 아프다. 조준한 자리로 떨어지므로
  **비켜서면 빗나간다**. 보라색 시전 이펙트 제거 — 바위만 날아간다) /
  **T19-5 바위 사거리 3배 ✅** (3.5 → **10.5**. 비행 시간은 0.35초 그대로 두어
  **회피 창은 거리와 무관하게 일정**하다 — 멀수록 더 빨리 날아온다) /
  다음: 대표 플레이 판정
- 진행 중: **T18 차수·환생 재설계** (지시서 밖, 2026-08-21 대표 지시).
  **T18-1 차수를 레벨로 ✅** (`tier_level_1~5` = 20/30/60/100/200. `GetTier`가
  **레벨에서** 차수를 센다(시작 0차). 몬스터 스킬을 전부 **0차**로 내렸다 —
  안 그러면 Lv20 전에 쓸 스킬이 없다. 슬롯 = **차수 + 1**(상한 5) —
  `max(차수,1)`이면 0차와 1차가 둘 다 1칸이라 첫 전직에 보상이 없다.
  레벨 캡 30 → **200**. `PlayerStats.RefreshTierRewards`를 레벨업에 걸었다) /
  **T18-2 환생 개편 ✅** (**조건 없음** — 얼마든 환생한다. 보상은 `BestRebirthLevel`
  (환생한 레벨 중 **최고치**, 갱신형)에서 나온다: 레벨업당 스탯 `5 + floor(최고/10)`,
  사냥방 몬스터 `+floor(최고/10)`(상한 5, **보스방 제외**). — 단위는 2026-08-23에 5 → 10. 환생 창에 규칙과 예시를 길게
  적었고, 폐기된 직업 선택 UI를 걷어내 그 자리를 썼다) /
  **T18-3 환생 UI 정리 ✅** ("환생 가능" 버튼 삭제 — 마을 NPC로만 환생한다.
  환생 창을 팝업 레이어 최상단으로(`GroupType` 2 · `GroupOrder` 30) 올려 가방 창 위에 뜬다.
  규칙 문구는 `ui/RebirthConfirm.ui`의 `RuleText`에 **정적으로** 두어 대표가 Maker에서
  직접 고칠 수 있다 — 스크립트가 채우는 것은 살아 있는 숫자(`RecordText`)뿐이다) /
  **T18-4 직업 시스템 완전 제거 ✅** (강타·돌진이 스킬 바에 뜨던 원인. 설계 이력 참조) /
  **T18-5 환생 보너스 단위 5 → 10레벨 ✅** (대표 판정 "스텟이 조금 과한거 같아보였어".
  스탯과 몬스터 수가 한 키에서 나오므로 둘 다 절반. Lv9 이하 환생은 보상 0) /
  다음: 대표 플레이 판정
  - ~~⚠ 환생하면 차수가 0으로 떨어진다.~~ → **2026-08-25 대표 지시로 뒤집혔다.**
    전직은 퀘스트라 한 번만 하면 되므로 **차수도 슬롯도 환생해도 유지된다** (T23-7).
  - ⚠ **로드 순서**: 슬롯 계산은 반드시 `ApplyRun`(레벨 복원) **뒤**에 해야 한다.
    차수가 레벨에서 나오므로 `ApplyPerm`에서 부르면 항상 0차로 읽힌다
    (실측: Lv20인데 슬롯이 1칸이었다).
  - ⚠ `.model`에 `TransformComponent.Position`을 박으면 **`placeModel`의 `pos`를 덮는다**
    (실측: (-11,6)에 놓았는데 (0,0,0)에 섰다). 위치는 배치하는 쪽(`.map`)이 정한다.
  - ⚠ **아바타 몬스터는 `SpriteRendererComponent`가 없다.** `MonsterAttack.OnBeginPlay`가
    스프라이트 클립에서 공격 상자를 유도하고 **그 콜백 안에서 공격 타이머를 시작**하는데,
    스프라이트가 없으면 콜백이 아예 안 돌아 **에러 없이 데미지가 0**이 된다.
    아바타면 사람 크기 상자(0.67×1.42)로 갈라 타이머를 직접 건다.
  - ⚠ 공격 **모션**은 아직 없다. `PlayAttackPose`가 SpriteRUID를 갈아끼우는 방식이라
    아바타에서는 그냥 물러난다. 아바타는 `BodyActionStateChangeEvent`로 가야 하는데
    `AIChase`가 상태를 되돌리는 문제(T11-1)를 다시 만난다 — 별도 태스크.
  - **순서를 바꿨다** (대표 승인 2026-08-20): 원래 T16-3 → T16-4였는데, 모험가가
    직업군 스킬을 **한꺼번에** 주므로 차수 잠금이 없으면 1회차에 상위 차수 스킬을
    그대로 끼운다. 그래서 T16-4의 차수 부분만 먼저 뽑아 T16-4a로 넣었다.
  - ⚠ T16-4 정정 (대표 2026-08-16): 슬롯은 **환생할 때만** 하나씩 늘어야 하고
    레벨 캡은 **30 → 60 → 100 → 200**으로 확장돼야 한다.
    → **둘 다 완료.** 슬롯은 T16-4b(상한 5·차수 축), 레벨 캡은 T18-1에서
    `initial_level_cap` 30 → **200**으로 올려 전직 5단(20/30/60/100/200)을 다 담는다.
- 진행 중: **T14 스킬 조작** (지시서 밖). T14-1 키보드 수동 사용(Z X C V B N M) ✅ /
  T14-2a 키 배치 저장·적용 계층 ✅ / T14-2b-1 전체 키 + 밀어내기 ✅ /
  T14-2b-2 키 설정 창 ✅ (`ui/KeyConfig.ui` + `UI/KeyConfigPanel.mlua`)
- **2026-08-13 재플레이 판정 (대표)**: ~~T10-3c ✅ 확정~~ / ~~T9 체감 "슬롯 확장" ✅ 예~~
  → ⚠ **2026-08-28 확인: 둘 다 무효다.** T10-3c는 `monster_atk_base` 24를 확정한 것인데
  T28이 곡선을 갈아 지금은 `157 + 11.5x(Lv-1)`이다. "슬롯 확장"은 **보스 축** 시절
  답인데 T16-4b가 그 축을 없애고 **차수** 하나로 통일했다. 둘 다 다시 받아야 한다
  (`docs/인수인계_현재상태.md` §6.1에 지금 빌드 기준 판정표를 다시 뽑아 뒀다).
  나머지 체감 3개(게이트 통과 / **차수 전직** / 스탯 갈림)와 최종 검증 질문은 아직 미답.
  — "직업 선택"은 직업이 폐기되면서 **차수 전직의 체감**으로 바뀌었다.
- 다음: 남은 판정을 받고 그에 따라 결정. 최종 질문이 "예"면 확장(T13 아이템 / area_02),
  "아니오"면 콘텐츠를 넣지 않고 게이트 간격·표시 UX·성장 템포를 재설계한다.
- 마지막 갱신: 2026-08-30

### 태스크 체크리스트
- [x] T0 데이터 뼈대
- [x] T1 방 단위 이동
- [x] T2 스탯 + 분배 UI
- [x] T3 전투 + 데미지 공식
- [x] T4 레벨업 + 경험치 곡선
- [x] T5 포획 + 스킬 장착
- [x] T6 게이트 시스템
- [x] T7 환생 + 랜드마크
- [x] T8 저장 + 로깅
- [x] T9 통합 검증
- [x] T10 스탯이 판마다 의미 있게 (T9 실측에서 파생 — T10-1/1b/2/3/4 완료.
      **T10-3c도 2026-08-13 실플레이로 확정** — `monster_atk_base` 24 그대로 간다)
- [x] T11 연출·UI 보강 (지시서 밖 — 몬스터 공격 모션/효과음, 상시 HUD)
- [x] T12 공간 확장 (지시서 밖 — 지역 데이터, 마을, 마을 게이트)

> **최종 판정은 아직 남아 있다.** 작업지시서 T9의 체감 판정 3개(게이트 통과의 쾌감 /
> 슬롯 확장의 체감 / 직업 선택의 이유)와 최종 검증 질문은 대표가 **직접 한 회차를
> 플레이한 뒤**에만 답할 수 있다. T10 결정들이 게임에 반영된 뒤 판정하는 것을 권한다.

- 2026-08-16 — **방어 스킬도 슬롯을 먹는다.** 회복·실드도 스킬이므로 공격 스킬과 **같은 칸**을
  놓고 경쟁한다 (대표: *"스킬이잖아?"*). 물약 퀵슬롯처럼 갈라 주지 않는다.
  - 이로써 슬롯 규칙이 하나로 통일된다: **몬스터 스킬 · NPC 스킬 · 방어 스킬 전부 같은 슬롯.**
    무엇을 끼울지가 곧 빌드다 — 슬롯 3칸에서 "공격 3"과 "공격 2 + 생존 1"이 갈린다.
  - 물약과 다른 점: 물약은 소모품이라 무한히 다시 살 수 있고, 스킬은 한 번 고르면
    그 회차 동안 자리를 차지한다. 그래서 물약만 별도 퀵슬롯을 유지한다 (2026-08-13 결정).
- 2026-08-16 — **첫 지역의 테마를 헤네시스로 확정하고, 방마다 다른 몬스터를 세운다.** (대표 지시)
  방 5개에 원작 레벨 순서대로 **달팽이(Lv1) → 파란 달팽이(Lv3) → 주황 버섯(Lv5/방 8) →
  뿔버섯(Lv12) → 돌 정령(보스)**. 이전에는 r_01·r_02가 **같은 슬라임**이었다.
  - 빠진 것: 초록 슬라임·스켈레톤 사병(헤네시스가 아니다). **딸린 것이 같이 움직였다** —
    그 둘의 포획 스킬과 **아이템 드랍처**(포션·검·갑옷). 안 옮기면 아이템이 영영 안 나오고
    컬렉션도 못 채운다.
  - LUK 계수 스킬(2026-08-08 결정)은 **뿔버섯이 승계**한다 — 스켈레톤과 같은 Lv12 자리라
    LUK 빌드가 스킬을 얻는 시점이 안 바뀐다.
  - 컬렉션 종 수 **4 → 5**. 슬롯은 3칸 그대로라 "무엇을 끼울지"가 더 좁아진다.

### 설계 변경 이력
- 2026-08-31 — **직업 캐릭터의 스킬은 뺏지 않고, 히든 보스 보상은 인벤토리 패시브로 준다.**
  첫 적용은 히어로의 검: 1개당 STR +1%, 최대 5개(+5%), 장착·직업 제한 없이 보유 즉시 적용.
  히어로 스킬 5종은 플레이어 보상에서 빠지고 히어로의 전투 패턴으로만 유지한다.
  이유: 몬스터의 능력을 빼앗는 성장 정체성은 살리되 직업 고유 스킬까지 훔치는 모순을 피하고,
  어떤 직업도 더 높은 히든 보스를 노릴 수 있는 공통 장기 성장축을 만들기 위해서다. (T56)
- 2026-08-29 — **포획 패시브를 절반만 둔다. 패시브는 슬롯을 안 먹는다.** (대표 지시)
  *"버프, 액티브가 존재해야 하니까"* — 지역당 5종 = **액티브 3(공격 2 + 방어/버프 1) + 패시브 2**.
  **T38-1에서 구현했다** (기존 17종 중 3종을 패시브로: 뿔 들이받기 STR / 물방울 터뜨리기 INT /
  강철 가죽 DEX — 지역당 1종씩, 대표 미지정분이라 내가 골랐다).
  - **패시브가 슬롯을 안 먹는 것이 핵심이다.** 먹게 두면 칸을 잡아먹으면서 아무 일도 안 하고,
    *"무엇을 끼울지가 곧 빌드"*라는 핵심 설계가 그만큼 얇아진다.
  - 장당 값은 **첫 장 1P + 추가 장 0.25P** (`collection_passive_first` / `_extra`, 대표 결정 안 B).
    만작 1종 = 2P라 **T15-2의 계수 강화(1.0 → 1.8)와 같은 모양**이다.
  - ⚠ **주는 스탯은 `passive_stat` 새 컬럼이다.** `scaling_stat`에 넣으면 안 된다 —
    그 칸은 **파생 수치 이름**(ATK/INT/LUK)이고 패시브가 주는 것은 **분배 스탯**(STR/DEX/INT/LUK)이라
    2026-08-27이 갈라 놓은 **이름 체계가 둘인 함정**에 정확히 걸린다.
  - ⚠ **게이트에는 안 센다** (실측: 컬렉션 만작에서도 `STAT_TOTAL=0`). 장비와 똑같이
    `GetTotalX`·`GetMaxHp`에만 얹힌다 — `GetAllocatedTotal`·`GetStatWithoutEquip`은 안 건드린다.
  - 실측: 컬렉션 0장 → 1장씩 → 만작에서 보정이 **0 → 1.00 → 2.00**, 물공 170 → 177 → 184
    (STR 1P=5 · DEX 1P=2 그대로). 패시브 장착 시도는 거절(false), 액티브는 통과(true).
- 2026-08-29 — ⚠⚠⚠ **발견: 신규/환생 직후 Lv1이 첫 달팽이를 못 잡는다.** (미해결, 대표 판단 대기)
  실측: 분배 0 · 장비 0 · 컬렉션 0에서 **물공 10 · 방어 10 · HP 1000**.
  Lv1 달팽이(HP 144 · DEF 80 · ATK 157)를 **108대**에 잡고 **5.6대** 맞으면 죽는다.
  - **원인은 T27-2에서 안 따라 올라간 기본값이다.** 스탯 계수를 5배로 올릴 때
    (`str_atk_per_point` 1 → 5) **`player_base_atk`·`player_base_int`·`player_base_def`
    셋(각 10)을 안 올렸다.** 옛 체계에서 기본 10은 "1P를 10번 찍은 것"이었는데 지금은 **2P어치**다.
    반대로 몬스터 DEF 기본은 T28에서 5 → **80**이 됐다.
  - ⚠ **T28의 설계 목표와 어긋난다.** T28은 *"DEF는 동레벨 플레이어의 물리 공격력과 같게 뒀다"*고
    적었는데, Lv1에서 그 값이 80이 아니라 **10**이다.
  - **패시브로는 못 고친다.** 패시브 40종 만작이면 물공 248(0.6대)이 되지만
    **컬렉션이 없는 신규 계정은 여전히 108대**다.
  - 고치려면 `player_base_atk`/`_int`/`_def`를 **50 정도**로 올린다
    (신규 Lv1 물공 50 → 달팽이 9대). 밸런스 변경이라 **지시 없이 안 건드렸다.**
- 2026-08-29 — **포획 스킬이 패시브로 능력치를 준다. 중복 장수도 센다.** (대표 지시)
  **⚠ 승인만 됐고 아직 구현 안 했다.** 설계는 `docs/지역설계_area04-area20.md` §6.
  - **왜**: 재 봤더니 **환생 직후 Lv1의 몸이 신규 계정과 정확히 같다**(둘 다 물공 70 / HP 1200).
    환생 보상 둘이 **다 Lv1에서 안 나온다** — 레벨업당 스탯은 레벨이 올라야 받고,
    사냥방 몬스터는 4→7마리뿐이다. 거기에 T34로 `exp_base`가 60이 되어 Lv1→2에 **12마리**다.
    직전까지 Lv30~40이던 사람이 달팽이를 4대씩 12마리 잡는다 (대표: *"환생하면 1레벨에서
    뭔가 잘 안잡히는 느낌"*).
  - **주는 스탯은 새 컬럼 없이 유도한다**: `skill_kind=defense` → **DEX** /
    `attack`이고 `scaling_stat=INT` → **INT** / 나머지 → **STR**. 지금 17종 = STR9 / INT5 / DEX3.
  - **중복을 센다** (대표 판단이 내 권장을 뒤집었다). 근거: *"중복으로 잡으면 오히려
    **다음 지역에 대한 완화제**를 줄 수 있다"* — 막히면 지금 지역을 갈아서 넘는 길이 열린다.
    대안이던 **환생당 포인트 증가**는 그냥 숫자가 커지는 것이라 버렸다 — 패시브는
    *"얻는 재미와 성장의 경험"*이 그대로 힘이 된다.
  - ⚠⚠ **장당 1P로 두면 만작에서 무너진다** (실측): 85종 만작 425장 = **+425P**,
    물공 **1345** — **Lv1인데 Lv50 STR 몰빵(1295)과 같다.** 완화제가 아니라 대체재가 된다.
    권장은 **첫 장 1P + 추가 장 0.25P**(만작 1종 = 2P) — T15-2의 계수 강화 `1.0→1.8`과
    **같은 모양**이라 중복의 값어치가 두 축에서 일관된다. **장당 값은 대표 미결.**
  - ⚠ **게이트에는 안 센다.** `RoomGate`의 `STAT_TOTAL`은 `GetAllocatedTotal()` =
    `StatStr+StatDex+StatInt+StatLuk`, 즉 **실제로 분배한 포인트**만 본다. 여기 더하면
    컬렉션만으로 문이 열려 2026-08-12 결정("게이트가 재는 것은 성장이어야 한다")과 부딪힌다.
    → **장비와 똑같이 다룬다**: `GetEquipBonus` 옆에 `GetCollectionBonus`를 두고
    `GetTotalAtk/Int/Def/Luk`·`GetMaxHp`에만 얹는다. **이미 장비가 쓰는 이음매다.**
  - **상한을 안 둬도 저절로 감쇠한다** — 분배 포인트가 레벨당 5씩 쌓이므로
    +17이 Lv1에선 물공 70→121이지만 Lv50이면 1295→1346(3.9%)다.
- 2026-08-29 — **지역은 그 대역이 시작되기 직전 레벨에 열린다.** (대표 지시)
  `LandmarkTable`에서 대역 `[10k+1, 10k+10]`인 지역은 `Lv 10k`에 연다.
  - **왜**: 지금은 **대역을 다 지난 뒤에야 열린다** — 엘리니아 몬스터가 Lv11~20인데
    문이 Lv20에 열려서 정작 그 구간에는 못 간다. 대표: *"그게 레벨 구간이니까."*
  - **T38-2에서 구현했다**: 엘리니아 Lv20 → **Lv10**, 페리온 Lv30 → **Lv20**
    (실측: `Lv10 area=area_03` · `Lv20 area=area_02`).
- 2026-08-29 — **모든 지역은 마을 동쪽 문 하나로 드나든다** (2026-08-10 설계의 확인).
  진입 방은 전부 `conn_west = r_town`, `r_town`의 conn 네 칸은 **비운다**.
  `GameDataVerify.VerifyRooms`가 **대상이 마을이면 역방향을 안 묻는다.**
  - **왜**: 지역이 늘 때마다 마을 방향을 하나씩 쓰면 **4개에서 막힌다.** 그런데 그 링크는
    이동에 안 쓰인다 — `r_town`의 `Portal_E`에는 `RoomPortal`이 아니라 `TownGate`가 붙어
    있어(T26-1) **conn 네 칸이 전부 장식**이다. 검증기만 그걸 요구하고 있었다.
  - ⚠ 내가 처음에 *"마을 방향이 넷뿐이라 area_05에서 막힌다"*를 **설계 문제로 잘못 봤다.**
    대표 정정: *"1번은 그냥 마을에서 우측으로 가자는거였잖아."* 방향을 지역에 배정하는
    구조가 애초에 없었다.
  - **T38-2에서 구현했다.** `r_town`의 conn 네 칸을 비우고 `VerifyRooms`에 면제를 넣었다
    (실측: `south/west/north -> r_town (마을 — 역방향 면제)` · 깨진 연결 0개).
    ⚠ **진입 방 쪽 링크는 안 건드렸다** — `r_11.conn_south` · `r_21.conn_north`는 실제
    포탈이 있는 걸어 나오는 길이라 방향을 바꾸면 맵의 포탈도 옮겨야 한다. 마을만 안 가리키면 된다.
    새로 만드는 지역(area_04~)은 `conn_west = r_town`으로 통일한다.
- 2026-08-28 — **지역을 20칸으로 확정하고 EXP 곡선을 거기 맞춘다.** (대표 지시)
  **지역 20개 · 각 10레벨 · Lv1~200.** 지역 하나가 10레벨을 담당하고 그 안의 방마다
  다른 몬스터를 실제로 플레이한다.
  - **왜**: *"그냥 지나치는 맵이 아닌, 사이사이에 있던 몬스터들을 플레이하고 그걸 체감하도록"*.
    옛 곡선에서 10레벨 구간은 앞이 4분 뒤가 111분이라 **앞은 지나쳐 버리고 뒤는 벽**이었다.
  - `exp_base` 20 → **60**, `exp_ratio` 1.12 → **1.105**. 지역 하나가 10분~24분(2.4배)이 된다.
  - **테마던전을 뺐다.** 원작 Lv31~50이 요정학원 엘리넬·골드비치 같은 **인스턴스 스토리
    던전**인데 우리 구조는 방이 포탈로 이어진 필드다. 그 두 칸을 **커닝시티·노틸러스**로
    바꿔 Lv1~60이 전부 빅토리아가 되게 했다 — 61의 슬리피우드로 나가는 것이 자연스러워진다.
  - **파생: 9~16번 지역의 레벨이 원작보다 높다.** 원작은 Lv71~120 다섯 칸을 미나르숲
    하나로 때운다. 20칸을 서로 다른 지역으로 채우려면 올릴 수밖에 없다 —
    **몬스터는 원작에서 가져오되 레벨은 우리 사다리에 맞춘다**는 기존 방식(스켈레톤 지휘관
    원작 65 → 우리 30)의 반대 방향이다.
  - ~~**파생: 지역 선택 창을 고쳐야 한다.**~~ → **T35에서 해소.** 20칸 + 스크롤이 됐다.
  - **파생: 랜드마크가 18줄 는다.** `LandmarkTable`이 레벨 → 지역 개방을 담당한다.
- 2026-08-27 — **분배 스탯의 이름을 STR/DEX/INT/LUK으로 바꾼다** (T27 재설계의 마지막 조각).
  `StatAtk`→`StatStr` · `StatDef`→`StatDex`, 저장 키 `stat_atk`→`stat_str` · `stat_def`→`stat_dex`,
  `Allocate("ATK")`→`"STR"`, 스탯 창 라벨 ATK/DEF → STR/DEX.
  - **왜**: T27이 스탯의 뜻을 바꿔 놓고 이름은 그대로 뒀다. 그 결과 **"ATK"가 두 가지를
    뜻했다** — 분배하는 스탯(= 지금의 STR)이면서 파생 수치(물리 공격력)이기도 했다.
    같은 문자열이 두 체계에 걸쳐 있으면 조용히 틀린 값을 읽게 된다.
  - **체계를 둘로 못박았다**: (1) 분배 스탯 STR/DEX/INT/LUK = 포인트 수 (2) 파생 수치
    ATK/INT/DEF/LUK = 그 포인트가 만드는 값. **파생 수치 이름은 안 바꿨다** — 겹침이
    사라졌으므로 더 바꿀 이유가 없다.
  - **파생: 저장이 한 방향으로 넘어간다.** 읽기는 `새 키 or 옛 키`로 하위 호환이지만
    쓰기는 새 키만 한다. 새 빌드로 저장한 계정을 옛 빌드로 로드하면 STR/DEX가 0이 된다.
- 2026-08-27 — **LUK 회피를 없앤다.** (대표 판정 *"회피는 없애는 방향으로 너무 크다"*)
  T27-1에서 승인했던 `LUK 1P당 회피 +0.002`(상한 0.6)를 **폐기한다.** 계수와 키를 지웠다.
  - **왜**: T28이 몬스터 곡선을 "동레벨 3대에 잡고 **12대 맞으면 죽는다**"로 다시 세웠고,
    그 변경의 값어치가 **난이도가 읽힌다**는 것이었다 — 곡선이 아니라 방의
    `monster_level`·`monster_count`가 난이도 전부를 정한다. 회피는 그 "몇 대"에 정확히
    난수를 꽂는다: 상한에서 12대가 30대가 되고, **매번 다르다.** 방을 세울 때
    몇 대짜리인지 말할 수 없게 되므로 T28의 값어치가 사라진다.
  - **덜 맞게 하는 자리는 실드다** (`PlayerHit.GetDamageReduction`, 상한 0.9).
    피해를 깎을 뿐이라 "몇 대"가 그대로 계산된다 — 회피와 달리 설계와 안 부딪힌다.
  - **파생: LUK의 이름 문제가 드러났다.** 남은 효과가 물리 공격력 +2 · 공속 +0.002 ·
    이속 +0.001(= 민첩) 대 포획률 +0.1%p · 드랍률 보정(= 운)으로 **2:2**다.
    `AGI`(민첩) 개명안이 나왔으나 **어긋남이 사라지는 게 아니라 반대편으로 옮겨갈 뿐**이고
    (포획률·드랍률은 기획서 4.7·4.8이 적어 둔 LUK의 존재 이유다), 저장 마이그레이션이
    걸린 이름 변경 태스크(`StatAtk`/`StatDef` → STR/DEX)와 **같은 7개 파일**을 훑으므로
    거기 얹어 한 번에 정하기로 했다. **아직 안 정했다.**
- 2026-08-27 — **`ItemTable`의 스탯 컬럼은 값이 아니라 "스탯 포인트 상당"이다.**
  (대표 지시 *"장비 수치부터 잡자"*)
  - **왜**: T27이 스탯마다 다른 계수를 주면서(STR→물공 5 / DEX→방어 10 / LUK→포인트 1)
    **같은 +5가 아이템마다 다른 값**이 됐다 — 무쇠 검 +5는 1P어치인데 행운의 목걸이 +5는
    5P어치라 **목걸이가 갑옷의 10배**였다. 값만 곱해 올리면 이 뒤틀림이 그대로 남는다.
  - **변환은 `GameData.GetStatPointValue` 한 곳에서만 한다.** `PlayerStats`가 분배 포인트에
    쓰는 것과 **같은 계수**를 읽으므로, 밸런스 계수를 조정하면 장비가 저절로 따라온다.
  - **CSV 숫자는 안 바뀌었다.** 뜻만 바뀌어 특정 스탯 장비 넷이 전부 **5P**로 같아졌다
    (반지는 올스탯 3P). 만작(5개) = 25P = **5레벨치**로, T27 이전과 같은 값어치다.
  - **파생: 가방 창이 표의 숫자를 그대로 못 쓴다.** `InventoryPanel.StatSummary`가
    스탯 창과 같은 창구에서 값을 받도록 고쳤다 — 안 그러면 가방과 스탯 창이 다른 숫자를 띄운다.
  - **파생: 올스탯은 포인트로 표시한다**(`올스탯+15P`). 네 수치가 전부 다른 값이 되어
    한 줄에 다 쓸 수 없다.
- 2026-08-27 — **`boss_skill_damage_multiplier`를 0.25에서 1로 되돌린다** (= 안 깎는다).
  (대표 지시 *"보스 스킬 배율부터 잡자"*)
  - **왜**: T25-3에서 0.25를 넣은 이유는 `boss_atk_multiplier`가 3이라 그 위에 스킬 계수
    (최대 2.3)를 겹치면 두 대에 죽어서였다. **T28에서 그 배율이 1.5로 내려와 근거가 사라졌고**,
    0.25를 그대로 두면 히어로 레이징 블로우가 평타의 **48%** — 보스 스킬이 평타보다 약해진다.
  - **이 값은 난이도 knob이 아니다** (시뮬레이션으로 확인). 스킬은 보스 타격의 1/3뿐이라
    (주기 3초 · i-frame 1초) 0.25~1.0 어디에 둬도 **승패 경계가 Lv25로 똑같다.**
    정하는 것은 "위협이 평타에서 오나 스킬에서 오나"다.
  - **파생: 보스 화력을 정하는 곳을 하나로 줄였다.** 두 knob이 같은 일을 하면 한쪽을
    고칠 때마다 다른 쪽이 조용히 어긋난다 — 이번이 정확히 그 사고였다.
    앞으로 보스가 세다/약하다는 `boss_atk_multiplier`로만 조절한다.
  - **파생: `SkillTable.coefficient`가 그대로 말하게 됐다.** 계수는 원래 자동 공격(1.2)
    기준으로 적혀 있다 — 1.15 ≈ 평타, 2.3 ≈ 평타의 1.92배. 그 사다리가 의도된 설계다.
  - **`boss_skill_range_multiplier` 0.667은 그대로다.** 근거가 스탯이 아니라 방 크기
    (시험의 방 144칸 vs 사냥방 448칸)라 T28이 바꾼 것이 아니다.
- 2026-08-27 — **몬스터 곡선을 기하급수에서 선형으로 바꾼다.** (대표 지시 *"몬스터 곡선부터 잡자"*
  + 난이도 목표·보스 배율 선택)
  | | 전 | 후 |
  |---|---|---|
  | 몬스터 HP | 30 × 1.09^(Lv-1) | **144 + 34.2 × (Lv-1)** |
  | 몬스터 DEF | 5 × 1.08^(Lv-1) | **80 + 19 × (Lv-1)** |
  | 몬스터 ATK | 24 × 1.08^(Lv-1) | **157 + 11.5 × (Lv-1)** |
  | 몬스터 EXP | 5 × 1.10^(Lv-1) | 그대로 (기하급수) |
  | 보스 배율 | HP·DEF·ATK·EXP 전부 ×3 | HP ×3 / **DEF ×1** / **ATK ×1.5** / EXP ×3 |
  - **왜**: T27에서 플레이어 스탯이 레벨에 선형이 됐다(1P당 +5 고정). **선형과 지수는 딱 한 번
    교차하므로** 교차점(Lv55) 아래는 전부 한 방, 위는 전부 불가능이었다 — 값을 바꾸면 교차점만
    움직인다. 레벨 캡이 200인 이상 기하급수로는 어느 값을 넣어도 못 맞춘다.
  - **설계 목표(대표 선택)**: 동레벨 일반 몬스터를 **3대에 잡고, 12대 맞으면 죽는다.**
    공격 주기와 i-frame이 둘 다 1초라 대수가 곧 초다.
  - **DEF는 동레벨 플레이어의 물리 공격력과 같게 뒀다.** 데미지가 `계수 × A²/(A+D)`라 `D = A`면
    딜이 정확히 절반이 되어 방어력이 의미를 갖는다. D가 A보다 훨씬 작으면 방어력이 무의미해진다.
  - **EXP만 기하급수로 남는다.** 전투 수치는 플레이어 스탯(선형)을 쫓고 EXP는 필요 EXP 곡선
    (`exp_ratio` 1.12, 기하급수)을 쫓는다. 둘이 다른 것을 쫓으므로 모양이 갈리는 것이 맞다.
  - **파생: 난이도를 만드는 곳이 곡선에서 방으로 옮겨갔다.** 어느 레벨에서나 같은 비율이므로
    `RoomTable`의 `monster_level`·`monster_count`가 난이도 전부를 정한다. 고레벨 콘텐츠를
    추가할 때 곡선을 다시 안 건드려도 된다.
  - **파생: 보스 DEF 배율을 뺐다.** DEF 배율은 싸움을 길게, ATK 배율은 위험하게 만드는데
    **둘이 곱해진다.** 이 겹침이 T26-6에서 보스가 이긴 진짜 이유다. 보스의 무게는 HP ×3이 진다.
  - ⚠ **파생: `boss_skill_damage_multiplier` 0.25가 근거를 잃었다.** T25-3에서 그 값을 정한 이유가
    "보스 ATK ×3에 스킬 계수까지 곱하면 두 대에 죽는다"였는데 ATK 배율이 1.5가 됐다. 지금은
    히어로 레이징 블로우가 평타의 48%다 — 보스 스킬이 평타보다 약하다. **대표 지시로 정한
    값이라 안 건드렸다.**
  - **파생: 레벨링이 약 6배 느려진다.** 한 마리가 0.5대 → 3대이므로 의도한 결과다.
- 2026-08-27 — **스탯을 ATK/INT/DEF/LUK → STR/DEX/INT/LUK로 재설계하고, 스탯마다
  여러 효과를 준다.** (대표 제안 + 시뮬레이션 후 보정안 승인)
  | 스탯 | 효과 (1P당) |
  |---|---|
  | STR | 물리 공격력 +5, **최대 HP +50** |
  | DEX | 방어력 +10, 물리 공격력 +2 |
  | INT | 마법 공격력 +5 |
  | LUK | 물리 공격력 +2, 공속 +0.002, 회피 +0.002, 이속 +0.001 |
  - **왜 바꾸나**: ① 플레이어 HP가 1000 고정이라 Lv34 이상 보스가 무조건 한 방이었다
    (T26-6에서 실측으로 막혔다). STR의 HP가 그 벽을 푼다. ② 지금은 네 스탯이
    `atk_per_stat_point` 하나(=1)를 공유해서, DEF나 LUK에 몰빵하면 **딜이 1~3**이 되어
    게임이 멈춘다 — 죽은 빌드가 있다. 모든 스탯에 공격력을 조금씩 주면 사라진다.
  - **대표 원안에서 고친 것 셋** (시뮬레이션으로 근거를 냈다):
    ① 공격력 계수를 STR 5 / INT 20 / LUK 20 → **5 / 5 / 2**로. 원안대로면 LUK이
    공격력 최고치(INT와 동률)에 속도·회피까지 얹어 **다른 스탯을 고를 이유가 없다.**
    ② 데미지가 `계수 x ATK²/(ATK+DEF)`라 ATK≫DEF 구간에서 **데미지 ≈ 1.2 x ATK**다.
    +20/P면 Lv30 한 방이 3697 — 게임에서 가장 단단한 스텀피 보스(HP 777)를
    **한 대에 4.8번 죽인다.** 5/2로 낮추면 한 방이 394라 2대 싸움이 된다.
    ③ 회피 0.0001 → **0.002**. 원안은 1000P를 부어도 10%라 죽은 수치였다.
  - **파생**: **LUK은 더 이상 스킬 계수 스탯이 아니다** (2026-08-08 결정 폐기).
    그때 LUK을 계수 스탯으로 쓴 이유는 "LUK을 찍으면 포획만 빨라지고 전투력이
    그대로"여서였는데, 이제 LUK이 물리 공격력에 직접 +2씩 기여하므로 근거가 사라졌다.
    `SkillTable`의 LUK 계수(뿔 들이받기)를 ATK로 옮겼다. `GetTotalLuk`은 이제
    전투 수치가 아니라 **분배한 포인트 수**라, 계수 스탯으로 쓰면 데미지가 조용히 1/5이 된다.
  - **파생**: 공속·회피는 선형이라 상한이 필요하다 (`player_attack_interval_min` 0.25 /
    `player_evade_max` 0.6). 회피 1.0은 무적이 된다 — 실드 감쇄 0.9 상한과 같은 이유.
  - **파생**: `atk_per_stat_point`(구 설계)는 아직 `GetStatWithoutEquip`이 읽고 있어
    남겨 두었다. 공격력 배선이 새 계수로 옮겨갈 때 지운다.
- 2026-08-25 — **전직 차수는 환생해도 유지된다.** (대표 지시)
  *"전직퀘스트는 한번만 하면 되는거야 따라서 환생하면 전직 차수는 유지되는 형식"*
  → **2026-08-21의 "환생하면 차수가 0으로 떨어진다"를 뒤집는다.**
  - 차수는 여전히 레벨에서 유도되지만(`TierFromLevel`), 영구 기록
    **`BestTier`(갱신형)**과 비교해 **큰 쪽**이 내 차수다(`GetTier`). 한 번 오르면 안 내려간다.
  - 파생: **슬롯도 유지된다**(슬롯 = 차수 + 1). 환생 직후에도 칸이 안 줄어든다 —
    전에는 1칸으로 돌아갔다. 환생의 비용이 그만큼 가벼워진다.
  - 파생: **상위 차수 스킬이 다시 잠기지 않는다.** 이미 전직을 마쳤기 때문이다.
  - 근거: 전직은 **퀘스트**다. 레벨이 되돌아가도 이미 마친 퀘스트가 취소되지는 않는다.
- 2026-08-23 — **던지는 스킬은 바위가 닿을 때 아프다.** (대표 지시: *"돌만 던져서
  캐릭터에 갈때 데미지 들어가는거로 수정해줄래?"*) T19-3의 "순수 연출"을 뒤집는다.
  - `projectile_ruid`가 채워져 있으면 **판정 방식 자체가 갈린다**:
      1) 사거리 안에서 조준할 사람을 고른다 (없으면 던지지 않는다)
      2) 바위를 그 자리로 날린다
      3) 도착 시각에 맞춰 **떨어진 자리에서** 판정한다 (`projectile_hit_radius` 0.8)
  - 파생: **비켜서면 빗나간다.** 조준한 자리로 떨어지므로 날아오는 동안 움직이면
    안 맞는다 (실측: `→ 0명 (비켜섰다)`). 보스가 원거리 견제를 갖게 된 대가로
    플레이어에게 회피가 생겼다 — 서로 주고받는 관계가 된다.
  - 파생: 던지는 스킬은 **던진 것만으로 쿨다운이 돈다.** 닿을지는 0.35초 뒤에
    정해지므로 시전 시점에 알 수 없고, 빗나갔다고 공짜로 다시 던지게 두면
    비켜서는 것이 아무 의미가 없어진다.
  - ⚠ **비행 시간은 한 곳(`projectile_seconds`)에서만 읽는다.** 그림과 판정이 각자
    다른 값을 쓰면 바위가 닿기 전에 아프거나 지나간 뒤에 아프다.
  - 파생: 바위 던지기의 `effect_ruid`를 **비웠다** (대표: *"가운데에 사슬같은 보라색
    이펙트 나오는데 그거 없애주고"*). 대체로 넣었던 서먼 스트라이킹 브릭이었다 —
    바위가 실제로 날아가니 시전 이펙트가 오히려 방해가 된다. 소리는 남긴다.
- 2026-08-23 — **바위 던지기는 실제로 바위를 던진다.** (대표 지시: *"골렘 바위 던지기잖아
  uuid중에 바위를 찾아서 던지는 형태로 가면 좋을거 같아보이네"*)
  - `SkillTable.projectile_ruid` 신설 + `Models/Effects/SkillProjectile.model`(**Body 없음**) +
    `Combat/SkillProjectile.mlua`. 서버가 스폰하면 저절로 복제되므로 클라 RPC가 필요 없다.
  - **판정과 무관한 순수 연출이다.** 데미지는 스킬을 쓰는 순간 이미 들어갔고 바위는
    아무것도 때리지 않는다. 때리게 만들면 판정 지점이 둘로 갈라지고, 날아가는 도중에
    대상이 죽거나 방을 나가는 경우를 따로 다뤄야 한다.
  - ⚠ Body를 달면 안 된다 — 타일 충돌에 막히고 물리가 매 프레임 위치를 덮어쓴다.
    이동은 타이머가 아니라 `OnUpdate(delta)`다(타이머 0.1초면 10Hz라 순간이동처럼 끊긴다).
    좌표는 속도를 누적하지 않고 **지나온 비율**로 정한다 — 프레임이 튀어도 도착점이 정확하다.
  - 파생: **바위 던지기의 사거리를 1.0 → 3.5로 늘렸다.** 1칸 날아가는 건 던지기로
    안 보인다. 골렘이 진짜 원거리 견제를 갖게 되므로 **난이도가 올라간다** —
    되돌리려면 CSV의 `range` 한 칸이다.
  - ⚠ **바위 스프라이트는 이름이 없다.** 리소스 검색이 `names: undefined`를 준다.
    썸네일 URL(`getResource(...).payload.thumbnail`)을 받아 **직접 보고** 골랐다 —
    이름 없는 자원을 고르는 방법이다. `findPacksContaining`으로 팩을 역추적해 이름을
    얻는 방법(T19-2에서 통했던 것)은 여기서 안 통했다 — 대부분 어느 팩에도 안 들어 있다.
- 2026-08-23 — **스킬에 원작 이펙트와 소리를 붙인다.** (대표 지시: *"이펙트도 좀 원작의
  이펙트랑 같이 uuid 찾아서 넣어주고"*) `SkillTable`에 `effect_ruid` · `sfx_ruid` 신설.
  - **아이콘 RUID로 원본 팩을 역추적해** 같은 팩의 `effect`/`_audio/Use`를 꺼냈다.
    아이콘과 이펙트가 서로 다른 스킬에서 온 채로 섞일 수 없다.
  - 재생은 `Combat/SkillEffect.mlua`(@Logic) 한 곳에서만 한다. 스킬을 쓰는 곳이 둘이라
    (플레이어·보스) 각자 띄우면 높이·볼륨·실행공간 규칙이 두 벌 생긴다.
  - **판정이 성공한 뒤에만** 띄운다. 안 맞은 스킬이 화면에서 터지면 i-frame이 버그로 읽힌다.
  - ⚠ **원작 이펙트는 오른쪽을 향해 그려져 있고, 붙인 부모의 스케일을 따라가지 않는다.**
    옵션 없이 띄웠더니 왼쪽에 선 플레이어를 두고 오른쪽으로 뻗었다. 방향은 **서버가**
    정해 `FlipX`로 넘긴다 — 시전자가 아바타냐 스프라이트냐에 따라 "어디를 보고 있나"를
    읽는 곳이 다른데, **맞은 대상의 좌표**는 어느 쪽이든 같은 방법으로 안다.
  - 원작 대응이 없는 스킬 5종(몬스터 스킬)은 **비슷한 원작 스킬로 대체**했다:
    몸통 박치기←돌진 / 푸른 껍질←로얄 가드 / 포자 살포←포이즌 미스트 /
    바위 던지기←서먼 스트라이킹 브릭 / 뿔 들이받기←아머 피어싱.
    **아이언 바디도 대체다** — 원작 팩에 이펙트가 없어(버프라 아이콘뿐) 파워 가드를 썼다.
  - ⚠ 높이는 `skill_effect_height` 하나로 전부 맞춘다. 이펙트마다 자기 pivot이 달라
    어떤 것은 머리 위로 뜬다(포이즌 미스트 실측). 스킬별로 맞추려면 컬럼이 하나 더 필요하다.
- 2026-08-23 — **보스는 자기가 주는 스킬을 직접 쓴다.** (대표 지시: *"히어로 같이 보스
  몬스터들은 얻는 스킬을 사용하는게 좋을거 같아"*) 대상은 **히어로 + 돌 정령**(대표 선택).
  - **새 컬럼을 만들지 않는다.** 쓸 스킬을 "주는 것"에서 그대로 유도한다 —
    `npc_skill_ids`(격파 지급)가 있으면 그것, 없으면 `drop_skill_id`(포획 지급).
    칸을 따로 두면 언젠가 어긋나 "안 주는 스킬을 쓰는 보스"가 생긴다.
  - **보스방에서만** 쓴다(`room_type=boss`). 종이 아니라 방으로 가르는 이유는 보스 배율과
    같다 — 돌 정령을 나중에 일반 사냥방에 세워도 초반이 조용히 어려워지지 않는다.
  - 이기면 뺏을 스킬을 상대가 **먼저 보여 준다**. 무엇을 얻는지가 설명이 아니라
    맞아 보는 것으로 전해진다.
  - ⚠ **스킬은 평타를 대신한다. 평타에 더해지지 않는다.** 별도 타이머로 돌렸더니
    실측 6번 중 3번이 데미지 0이었다 — 플레이어 i-frame이 1초인데 보스 평타가
    0.5초라 스킬이 **자기 평타가 만든 무적 구간**에 떨어져 씹혔다. 에러도 로그도 없다.
    같은 틱에 태우고 스킬 쓴 틱은 평타를 건너뛰니 100% 명중이 됐고, DPS도 같이 묶였다.
  - ⚠ **빈 방에서는 시도하지 않는다.** 공격 스킬은 맞은 사람이 0명이라 저절로 걸러지지만
    **실드는 대상이 필요 없어 그냥 성공한다** — 실측: 플레이어가 마을로 간 뒤에도
    12초마다 아이언 바디가 계속 걸렸다.
  - ⚠ **몬스터에는 스탯이 ATK 하나뿐이라 스킬의 `scaling_stat`이 무시된다.** 지금 보스가
    쓰는 6종이 전부 ATK라 어긋나지 않지만, INT/LUK 스킬을 보스에게 주면 조용히 ATK로
    계산된다. 그때는 몬스터에도 계수 스탯을 만들어야 한다 — CSV만 고치면 안 된다.
- 2026-08-23 — **환생 보너스 단위를 5레벨 → 10레벨로 내린다.** (대표 판정: *"스텟이 조금 과한거 같아보였어"*)
  `rebirth_bonus_level_unit` 5 → **10**. 스탯과 몬스터 수가 **한 숫자에서 나오므로 둘 다** 절반이 된다
  (대표 선택: "둘 다 10레벨로"). Lv22 기준 레벨업당 9P → **7P**, 사냥방 +4 → **+2마리**.
  - 파생: **Lv9 이하 환생은 보상이 0이다**(전에는 Lv5에서 +1). 조건 없는 환생의 억제가
    그만큼 강해진다 — 일찍 돌면 정말로 아무것도 못 받는다.
  - 파생: 몬스터 상한(+5)에 닿는 지점이 Lv25 → **Lv50**으로 밀렸다.
- 2026-08-21 — **직업 시스템을 코드에서 완전히 걷어냈다** (2026-08-16 폐기 결정의 뒷정리).
  `JobTable.csv`·`.userdataset` 삭제, `GameData.LoadJobs/GetJob/GetAllJobIds`, `PlayerStats.CurrentJob`,
  `PlayerSkillSlots.UnlockedJobs`/`GetJobSkills`, `SkillTable`의 `s_job_*` 4줄, `LandmarkTable`의
  직업 보상 줄, 저장 키 `current_job`/`unlocked_jobs`를 전부 제거했다.
  - **왜 지금**: 폐기를 결정했을 때 데이터·표시만 지우고 **판정 코드를 남겨** 뒀다.
    `Rebirth.Execute`가 여전히 직업을 심었고 `GetJobSkills()`가 거기서 스킬을 유도해
    **환생만 하면 강타·돌진이 스킬 바에 떴다** (대표 발견: *"강타랑 돌진이 왜 있어?"*).
  - **교훈**: 설계를 폐기할 때는 데이터만 지우지 말 것. 그 데이터를 읽는 코드가 남아 있으면
    기본값으로 되살아난다 — 에러 없이, 화면에만.
  - 파생: `GetJobSkills` 자리를 `GetOwnedSkillIds`(컬렉션 조회)가 물려받았다. 열쇠 게이트가
    보는 "보유 스킬" 목록은 그대로 동작한다.
  - 파생: 기획서 §4.4 링크스킬식 계승(`inherit_skill_id`)은 근거째 사라졌다.
    되살리려면 새 설계가 필요하다 (인수인계 3절 E).
- 2026-08-21 — **차수는 환생 횟수가 아니라 전직 레벨로 오른다.** (대표 지시)
  시작 **0차**, **Lv20·30·60·100·200에서 1~5차**. 이유: 환생이 "얼마든 가능"으로 바뀌면서
  환생 횟수가 차수의 근거가 될 수 없어졌다(반복하면 공짜로 오른다).
  - 파생: 몬스터·직업 스킬을 **전부 0차**로 내린다. 시작이 0차인데 스킬이 1차면
    Lv20 전에는 쓸 수 있는 스킬이 하나도 없어 초반이 자동 공격만 남는다.
  - 파생: 슬롯 = **차수 + 1**(상한 5). 그냥 `차수`면 시작이 0칸이고, `max(차수,1)`이면
    0차와 1차가 둘 다 1칸이라 첫 전직에 슬롯 보상이 사라진다.
  - ~~파생: 환생하면 차수가 0으로 떨어진다.~~ → **2026-08-25에 뒤집혔다** (위 항목 참조).
    차수는 영구 기록(`BestTier`)이라 환생해도 유지되고 상위 스킬도 다시 잠기지 않는다.
  - 파생: 레벨 캡 30 → **200**. 환생이 캡과 무관해져 단계별 캡을 둘 이유가 없어졌다.
- 2026-08-20 — **슬롯 상한 3 → 5, 늘어나는 축은 차수(환생) 하나로 통일.** (대표 지시)
  1차 1칸 → 2차 2칸 → … → 5차 5칸. **보스 클리어 축은 없앤다**(2026-08-16 "환생할 때만 +1"의
  완성). 몬스터 스킬과 NPC 스킬은 **합쳐서 그 5칸**을 놓고 경쟁한다 —
  2026-08-16의 "몬스터·NPC·방어 전부 같은 슬롯"이 그대로 유지된다.
  - 중간에 "몬스터 슬롯과 캐릭터 슬롯을 갈라 1슬롯=1차·2슬롯=2차로 묶자"는 안이 나왔다가
    **대표가 곧바로 철회**했다("내가 잘못 본 거 같아"). 가르지 않으므로 스킬 바도 한 줄 그대로다.
- 2026-08-20 — **모험가 NPC는 모험가 직업군 스킬을 *다* 준다. 플레이어는 거기서 골라 낀다.**
  (대표 지시) 스킬 하나를 뺏는 게 아니라 **묶음**을 뺏고, 슬롯 3칸에 무엇을 넣을지가
  그때부터 진짜 선택이 된다. 부수 조건 셋:
  - **UUID와 효과도 원작과 같아야 한다.** 스킬 자원은 `msw-search`로 실제 RUID를 찾아 쓴다.
    ⚠ 아이템 아이콘과 마찬가지로 **스킬 애니메이션 RUID도 검색 결과에 이름이 안 붙는다**
    (`names: undefined`). 게임에 띄워 눈으로 골라야 한다 — 스킬 수만큼 반복된다.
  - **NPC 스킬은 1장 고정.** 중복 강화(5장)는 몬스터 스킬만. `source == "monster"` 조건이
    이미 그렇게 갈라 두었으므로 `source=npc`로 넣으면 그대로 성립한다.
  - **격파하면 확실히 뺏는다** (포획 확률 아님). 굉장히 강한 상대라 확률까지 걸면
    이겨 놓고 미끄러지는 좌절이 커진다.
  - 숨은 NPC는 **r_04 돌무덤 길**. 게이트 없이 갈 수 있는 가장 안쪽 사냥방이라
    1회차에도 닿을 수 있으면서 "안쪽까지 들어온 사람이 만나는 것"이 된다.
  - **NPC는 원작의 실제 직업이다. 새 직업을 지어내지 않는다** (대표 정정 2026-08-20).
    모험가 **전사 전직군 = 히어로 · 팔라딘 · 다크나이트** 셋이고, 각각이 NPC로 서서
    이기면 그 직업의 스킬을 준다. **현재 지역(헤네시스)에서는 히어로만** 만든다 —
    팔라딘·다크나이트는 지역이 늘어날 때 같은 방식으로 얹는다.
    - ⚠ 한 번 틀렸다: 정식기사/상급기사/성기사라는 **기사 몹으로 3단 사다리를 지어냈다.**
      그건 전직군의 직업이 아니라 새로 만든 직업이었다. 상대는 반드시 원작 직업이어야 한다.
    - 히어로가 주는 것은 **히어로 계보 전체**(1차 전사 → 2차 파이터 → 3차 크루세이더 →
      4차 히어로). 한 번에 다 얻고 차수가 오를 때마다 풀린다.
  - ⚠ **직업 캐릭터는 `npc/`·`mob/` 팩에 없다. 아바타 아이템으로 있다.**
    `모험가 히어로 아머`(longcoat) / `모험가 히어로 소드`(weapon) / `모험가 히어로 부츠`(shoes) /
    `히어로 헤어`(hair). 팔라딘·다크나이트도 같은 이름 규칙으로 한 벌씩 있다.
    → 히어로 NPC는 **몬스터 스프라이트가 아니라 아바타로 세운다** (T16-3b).
    UI에 정지 이미지로 띄울 때는 **`thumbnail://` 접두어**가 필요하다 — 안 붙이면 조용히 안 그려진다.
  - ⚠ **`npc/` 리소스 팩은 전투에 못 쓴다.** `stand` 한 장뿐이라 공격·피격·사망 동작이 없다
    (다크로드·헬레나·아프리엔·성기사 NPC 팩 실측). 상대는 **`mob/` 팩**에서 고른다 —
    `stand`/`move`/`attack1`/`hit1`/`die1` + 오디오가 들어 있어 우리 몬스터 모델에 그대로 맞는다.
  - ⚠ **스킬은 이름으로 정확히 고를 수 있다.** `animationclip`으로 찾으면 이름이 안 붙지만,
    **`resource_pack` + `skill`**로 찾으면 이름이 붙고 **팩 id가 원작 스킬 id 그 자체**다
    (슬래시 블러스트 = `skill/100.img/skill/1001005`, 힐 = `skill/230.img/skill/2301002`).
    id 앞자리가 계열·차수를 담는다 — 100 전사1차 / 200 마법사1차 / 230 클레릭 / 300 궁수1차 /
    400 도적1차.
    - ⚠ **정정 (2026-08-23, T19-2).** 여기 "팩 내용은 아이콘이다"라고 적어 뒀던 것은 **틀렸다.**
      아이콘만 든 팩(아이언 바디 같은 버프)을 보고 일반화한 것이다. 공격 스킬 팩에는
      **`effect`(시전 애니메이션) · `hit/0~N`(피격) · `_audio/Use` · `_audio/Hit`**가 들어 있다.
      즉 **원작 이펙트와 소리를 이름으로 정확히 꺼낼 수 있다** — 눈으로 고르지 않아도 된다.
    - **아이콘 RUID에서 팩을 역추적할 수 있다**: `findPacksContaining(iconRuid)` → 그 스킬의
      원본 팩. 아이콘과 이펙트가 서로 다른 스킬에서 온 채로 섞일 수 없다.
- 2026-08-16 — **직업과 전직을 없앤다. 스킬에 차수를 두고 환생이 차수를 올린다.** (대표 지시)
  - **직업 선택 없음 · 전직 없음.** 숨은 **모험가 NPC를 쓰러뜨리면 그 NPC의 스킬을 뺏는다**
    (포획과 같은 획득 방식). 고를 직업이 없으므로 `JobTable`의 "직업을 고른다"는 사라진다.
  - **스킬마다 차수가 있다.** 보유는 아무 때나 가능하지만 **내 차수가 그 스킬의 차수 이상이어야
    장착·사용할 수 있다.** 2차 스킬을 1회차에 뺏어도 환생해야 쓴다.
  - **차수 = 환생 횟수.** 레벨 캡(30 → 60 → 100 → 200)을 채우고 환생하면 차수가 오르고
    그때 잠겨 있던 스킬이 풀린다. 슬롯도 환생마다 +1 (아래 2026-08-16 슬롯 항목).
  - **NPC 스킬은 몬스터 스킬과 같은 슬롯을 놓고 경쟁한다.** 직업이 없어져 "직업 슬롯은
    고정"의 근거(= 직업을 선택하는 이유)가 사라졌기 때문이다. 무엇을 끼울지가 곧 빌드다.
    → **핵심 설계의 "직업 스킬 슬롯: 고정" 항목은 이 결정으로 폐기된다.**
  - ~~⚠ 미해결: `r_03` 열쇠 게이트가 영구히 잠긴다~~ → **2026-08-16 해소.** 게이트 종류를
    바꿨다 (아래 항목).
- 2026-08-16 — **게이트에 4번째 종류 `boss`를 추가한다. `r_03`의 열쇠를 스킬 → 스테이지 보스로
  바꾼다.** `gate_key`가 보스방의 방 id이고, **한 번이라도** 그 보스를 잡았으면 열린다.
  이유: r_03의 열쇠가 직업 스킬(`s_job_war_02` 돌진)이었는데 직업 선택이 사라지면서
  획득 경로가 없어져 **문이 영구히 잠기게 됐다.** 스테이지 끝 보스를 조건으로 두면 획득
  경로가 늘 존재하고, "안쪽을 다 헤친 사람에게 열리는 샛길"이 된다.
  - **회차가 아니라 영구다** (`HasEverClearedBoss`, `IsBossClearedThisRun`과 갈라 뒀다).
    환생했다고 이미 연 문이 다시 잠기면 "성장의 결과로 열린 땅"이 회차마다 도로 닫힌다.
  - 파생: 열쇠 스킬 게이트(`gate_type=key`) 자체는 코드에 그대로 남는다. 쓰는 방이
    없어졌을 뿐이고, 몬스터 스킬을 열쇠로 쓰는 문은 나중에 만들 수 있다. (대표 지시, T16-3)
- 2026-08-16 — **슬롯은 환생할 때만 +1** (보스 클리어 축을 뺀다). 레벨 캡은 회차마다
  **30 → 60 → 100 → 200**으로 확장된다. 지금 구현은 `1 + 보스클리어수 + 환생횟수`(상한 3)에
  캡 30 고정이라 둘 다 다르다 — 상한과 보스의 새 역할은 T16-4에서 확정한다. (대표 지시)
- 2026-08-14 — **같은 아이템·스킬을 5개까지 중복으로 얻고 수치를 쌓는다.** 규칙이 둘로 갈린다:
  - **장비 = 개당 수치 × 보유 개수** (+5 → +10 → +15 → +20 → +25). 슬롯은 3개뿐이라
    5개를 동시에 낄 자리가 없으므로 **장착 개수가 아니라 보유 개수**를 센다.
  - **스킬 = 계수 × (1 + 0.2 × (보유수-1))** — 5장 만작에서 ×1.8.
  - 왜 다른가: 장비 스탯이 정수 5라서 비례 20%를 얹으면 5 → 6 → 7이 되어 체감이 없다.
    반대로 스킬에 배수를 얹으면 5장에서 데미지가 5배가 되어 자동 공격(기준 계수 1.2)이
    무의미해지고 T3 완료 조건("Lv1 몬스터 3~4타")과 몬스터 계단 밸런스가 무너진다.
  - 파생: **게이트는 여전히 장비를 세지 않는다**(`GetStatWithoutEquip`). 중복으로 스탯이
    커져도 문이 열리는 시점은 안 바뀐다 — 2026-08-12 결정이 그대로 유지된다.
  - 파생: **직업 스킬은 중복이 없다.** 포획이 아니라 직업 선택으로 붙으므로 항상 1장이다.
    `source == "monster"`만 쌓는다 — 안 가르면 직업 스킬이 보유수 0으로 읽혀 계수가 깎인다.
  - 파생: 만작이면 **포획 판정 자체를 건너뛴다**(천장 카운터도 안 센다). 컬렉션을 다 모은
    뒤에도 포획에 이유가 생기고, 그 이유가 끝나는 지점도 분명해진다.
  - 파생: 상한에 닿은 아이템은 **드랍 판정 자체를 건너뛴다.** 떨어뜨려도 `AddItem`이 0을
    돌려주며 버려서 "주웠는데 아무 일도 안 일어난다"로 보인다. (대표 지시, T15)
- 2026-08-04 — Lv30 랜드마크만 3축 동시 지급(직업 해금 + 슬롯 +1 + 구역 개방).
  이유: 8주 슬라이스 안에서 슬롯 확장 2축을 모두 체감시키려는 한정 예외. (기획서 §4.5)
- 2026-08-21 — **환생에 조건이 없다. 보상은 "성장이 빨라진다"로 바뀐다.** (대표 지시)
  ~~레벨 캡 AND 보스 격파~~ → **아무 때나 환생 가능.**
  - 보상의 근거는 **`BestRebirthLevel`(환생한 레벨 중 최고치)** 하나다. **갱신형**이라
    같은 자리에서 반복하면 안 오른다 — 안 그러면 Lv5 반복 환생이 최적 전략이 된다
    (대표와 함께 검토해 정한 것).
  - 보상 둘: 레벨업당 스탯 `5 + floor(최고/단위)` / 사냥방 몬스터 `+floor(최고/단위)`
    **상한 5, 보스방 제외**. 단위는 처음 5였다가 **2026-08-23에 10**이 됐다(아래 항목).
    지금 값으로: Lv10 환생 → 6P·+1마리, Lv20 → 7P·+2마리, Lv50 → 10P·+5마리(상한).
    - 몬스터를 늘리는 이유: 스탯 +1은 눈에 안 보이는데 빽빽해진 방은 들어서는 순간
      보인다. 방을 헤쳐나가는 것이 코어라 보상이 코어를 직접 강화한다.
    - 보스방을 뺀 이유: 보스는 이미 배율 ×3이라 한 판이 사건이다. 둘이 되면 사고다.
  - 조건을 안 둬도 되는 이유: 보상이 밟은 레벨에 비례하므로 **일찍 환생하면 덜 받는 것
    자체가 억제**다. Lv1에서도 환생되고, 얻는 것이 0일 뿐이다.
  - 파생: `CanRebirth`(캡 도달 플래그)와 `BossesCleared`는 환생 판정에서 빠진다.
    값은 남는다 — 각각 캡 도달 연출과 보스 게이트 원장에 쓰인다.
- 2026-08-07 — ~~환생 조건을 `레벨 캡 AND 영역 보스 격파`(둘 다 필요)로 확정.~~
  **2026-08-21에 뒤집혔다(위 항목).** 당시 이유: 한쪽만으로 열리면 성장·공간 두 축 중
  하나가 무의미해진다.
- 2026-08-14 — **보스는 방에 새로 들어올 때마다 다시 선다** (2026-08-07 "회차당 1회"를 뒤집는다).
  이유: 한 회차에 보스가 한 판뿐이라 Lv30까지 잡몹만 잡는 구간이 길었다
  (대표 판정 *"너무 구간이 루즈해지네 몬스터만 잡다보니까"*).
  - 2026-08-07이 막으려던 것은 **대기 없는 리스폰**이었고, 그건 **재입장 요구**로 여전히 막힌다 —
    방을 나갔다 돌아오는 왕복이 곧 대기다. 잡은 자리에 서 있으면 다시 서지 않는다.
  - 파생: 보스 수치를 올린다 — HP·DEF·ATK **×3**, EXP **×3**
    (`boss_*_multiplier`). 반복할 수 있게 됐으니 한 판이 사건이어야 한다.
  - 파생: 슬롯은 여전히 **최초 1회만** 지급된다(`ClearBoss`가 자체적으로 막는다).
    환생 조건도 누적 `BossesCleared`를 보므로 영향 없다. 둘 다 실측 확인.
  - ⚠ ATK ×3은 실측 **보스 한 대 = 230 데미지**(플레이어 DEF 20 기준)로 HP 1000에 5대다.
    세다고 느껴지면 `boss_atk_multiplier`부터 내린다 — HP·EXP는 두고. (대표 지시, T16-1)
- 2026-08-07 — ~~보스는 **회차당 1회만** 스폰.~~ **2026-08-14에 뒤집혔다(위 항목).**
  당시 이유: 즉시 리스폰돼 격파감이 없고, 대기 없는 보스방이 최고 효율 사냥터가 됐다. (T9-1 실측)
- 2026-08-08 — **1회차는 직업 없음**을 의도로 확정. 직업은 첫 환생에서 처음 고른다.
  파생 규칙: 직업 스킬을 열쇠로 쓰는 게이트는 곧 "2회차 이상" 게이트다(r_03). (기획서 §4.3/§4.4)
- 2026-08-08 — 자동 공격의 계수 스탯을 `ATK 고정 → max(ATK, INT)`로.
  이유: 자동 공격이 총딜의 83%인데 ATK 고정이라 INT 빌드가 성립할 수 없었다. (T9-3 실측)
- 2026-08-08 — 상시 몬스터 레벨을 `Lv5 천장 → 방별 계단(최고 12)`으로.
  이유: ATK 45(Lv8)에 모든 방이 1타가 돼 그 위 22레벨이 무의미했다. (T9-2 실측)
- 2026-08-08 — 컬렉션 종 수 `3 → 4`(1종 추가). 이유: 3종이면 누구나 다 모아 LUK이 무의미.
- 2026-08-08 — 플레이어 `INT/DEF 기본값 10`(ATK와 대칭). LUK만 0 유지.
  이유: 첫 1P부터 계열 선택이 성립해야 한다. LUK에 10을 주면 기본 포획률이 3%가 아니라
  4%가 되어 기획서 §4.8과 어긋난다.
- 2026-08-08 — **LUK을 일부 몬스터 스킬의 계수 스탯으로도 쓴다** (포획·드랍률 역할은 유지).
  이유: T9-3에서 "컬렉션이 얇은 한 LUK 빌드는 성립하지 않는다"가 나왔다 — LUK을 찍으면
  포획만 빨라지고 전투력이 그대로라 그냥 느린 판이 됐다. 첫 사례는 스켈레톤 사병의
  `s_mon_skeleton`(계수 1.2). 자동 공격은 그대로 max(ATK, INT)이므로 LUK은 **스킬을 얻어
  장착해야** 딜이 되고, 슬롯 구조가 그대로 걸린다. (기획서 §4.7, T10-4)
- 2026-08-09 — 몬스터 공격 모션은 **상태머신이 아니라 `SpriteRUID` 직접 교체**로 만든다.
  이유: `AIChaseComponent`가 매 프레임 BT를 돌려 IDLE/MOVE를 다시 쓰기 때문에
  `ChangeState("ATTACK")`이 **0.1초 만에 되돌아간다**(실측: `ATTACK IDLE IDLE …`).
  ActionSheet의 `attack` 키는 그대로 두고, 클립 RUID를 조회하는 용도로만 쓴다. (T11-1)
- 2026-08-08 — 죽음의 대가 = **시작방 귀환**. 이유: 잃는 것이 "탐험 진행"이라 방 단위
  탐험 코어와 맞고, 레벨·스탯을 안 건드려 환생의 비용 구조와 충돌하지 않는다.
- 2026-08-10 — **마을(`r_town`) 신설**, 시작방을 r_01 → r_town으로 이동.
  이유: 환생·귀환·재출발이 모이는 자리가 사냥방이면 "돌아왔다"는 감각이 없다. (대표 지시, 기획서 §3.3)
- 2026-08-10 — **환생 게이트를 문 엔티티로 만들지 않는다. 마을 게이트(마을 동쪽 문)가
  지역 선택기를 겸한다.** 잠긴 지역은 잠긴 문으로 보이는 대신 **목록에 뜨지 않는다.**
  이유: 지역 이동의 출입구가 하나뿐이면 "세계가 열린다"가 그 목록 한 줄로 표현된다.
  갈 수 있는 곳이 하나면 바로 이동, 둘 이상이면 선택 창. (대표 지시, 기획서 §3.3, T12-3)
- 2026-08-13 — **키는 키보드 전체를 걸 수 있다** (대표 지시: "원작에 맞게").
  `KeyboardKey.CastFrom(string)`으로 이름을 그대로 받는다 — A~Z / Alpha0~9 / F1~F15 /
  Keypad / Space / Semicolon / LeftShift …. 단 **이동·취소 키는 예약**해 막는다
  (`SkillBar.reservedKeys` = 방향키 4개 + Escape). 중복 키를 고르면 **밀어내기** —
  그 키를 쓰던 슬롯이 비고, 교환하지 않는다. (T14-2b-1)
- 2026-08-13 — **키 설정 창에는 여는 단축키를 두지 않는다.** 화면의 "키 설정" 버튼으로만 연다.
  이유: 키보드 전체를 스킬에 걸 수 있게 한 순간, 창을 여는 키를 하나 박으면 그 키를
  플레이어에게서 빼앗는다. 스탯·장착 창도 같은 방식(버튼)이라 조작이 일관된다. (T14-2b-2)
- 2026-08-13 — **스킬은 자동이 아니라 수동으로 쓴다.** 키보드는 맨 왼쪽 슬롯부터
  `Z X C V B N M` 순서이고 **재설정 가능**해야 한다. 자동 공격은 지금처럼 자동 유지.
  이유: 슬롯 확장(1→3)이 "DPS가 올랐다"가 아니라 **"쓸 수 있는 버튼이 늘었다"**로
  체감돼야 한다. 그리고 열쇠 스킬은 특정 위치에서 써야 하므로 수동이 필수다.
  (대표 지시, T14-1)
- 2026-08-12 — **수치 게이트는 `분배한 스탯 포인트의 총합`을 본다** (`gate_key=STAT_TOTAL`).
  어디에 넣었는지는 묻지 않아 1스탯·2스탯·올스탯 빌드가 같은 시점에 통과한다.
  **장비는 포함하지 않는다.** 이유: 스탯 하나를 지정해 재면(예전 `gate_key=ATK`) 그 스탯을
  안 찍은 빌드가 **영구히 막혔다** — 재분배가 없고 보스가 게이트 뒤라 환생으로도 못 나온다.
  r_02는 `ATK/20` → `STAT_TOTAL/10`. 순수 빌드의 통과 시점(Lv3)은 그대로다.
  (대표 지시, 기획서 §3.2, T13-1b)
  - T13-4에서 구현으로 굳혔다: 전투는 `GetTotalX`(장비 포함), 게이트는
    `GetStatWithoutEquip`(장비 제외). 두 함수를 물리적으로 갈라 둔 이유는
    게이트가 재는 것이 성장이어야 하고, 장비를 세면 그게 운이 되기 때문이다.
    파생: **장비 LUK은 포획률에 얹힌다**(기획서 §4.7의 LUK 역할 그대로).
- 2026-08-12 — **마을(`room_type=town`) 진입 시 HP 전회복.** 이유: 그 전까지 회복 수단이
  하나도 없어서(HP 1000 고정, 레벨로 자라지 않음, 채우는 경로는 사망 시 `Respawn()`뿐)
  **일부러 죽는 것이 유일한 회복법**이었다. 죽음의 대가가 대가가 아니라 회복 보상으로
  뒤집혀 있었다. 대가를 "걸어 돌아오는 시간"으로 옮긴다. (대표 승인, T13-1)
- 2026-08-12 — **장비 아이템은 특정 스탯 장비 + 올스탯 장비 2종.** 이유: 최종적으로
  올스탯 장비를 노리게 만든다. (대표 지시. 데이터는 T13-2로 들어갔다 — `ItemTable.csv`)
- 2026-08-13 — **아이템은 바닥에 떨어지고 밟으면 줍는다** (즉시 획득이 아니라 원작 메이플식).
  그리고 **LUK이 드랍률을 올린다**(기획서 §4.7). 단 포획처럼 정액이 아니라 **비례**다 —
  `드랍률 = drop_rate × (1 + LUK × drop_luk_multiplier)`. 이유: 정액(+0.05%p 식)이면
  2% 희귀템이 LUK 100에서 7%가 되어 **최종 목표템(올스탯 반지)이 희귀하지 않게 된다.**
  비례는 희귀도 서열을 지킨 채로 LUK을 보상한다. (대표 지시, T13-6)
- 2026-08-13 — **아이템 퀵슬롯은 스킬 바와 분리한다.** 물약을 스킬 슬롯과 같은 칸에
  섞지 않는다. 이유: 몬스터 슬롯 1→3 확장이 이 게임 성장 체감의 축이고 대표가 이미
  *"슬롯 늘어나는건 실감이 돼"*로 확인했는데, 물약이 같은 바에 들어오면 슬롯이 늘어난 것이
  "쓸 수 있는 스킬이 늘었다"가 아니라 "칸이 늘었다"로 희석된다. 원작 메이플도 둘이 따로다.
  ~~키 설정 창(T14-2b-2)에는 나중에 "아이템 슬롯" 줄을 덧붙인다.~~ → **2026-08-24 대표 지시로 취소.**
  퀵슬롯 키는 `1`·`2` 고정이다. (대표 승인)
  - T13-5b에서 구현: 퀵슬롯은 화면 **오른쪽 아래** 2칸, 키는 `1`·`2` 고정.
    올라가는 것은 **자동 배정**(가진 소비 아이템을 앞 칸부터) — 소비가 1종뿐이라
    고를 것이 없다. 종류가 늘면 수동 배정을 붙인다.
    `SkillBar.reservedKeys`에 `Alpha1`·`Alpha2`를 넣어 스킬이 같은 키를 못 쓰게 막았다.
- 2026-08-13 — **인벤토리는 MSW `inventory-package`를 쓰지 않고 직접 만든다.**
  이유: 그 패키지가 자기 `PlayerDBManager` 컴포넌트를 요구해서 우리 T8 저장 시스템과
  이름이 충돌한다. GameData/Save 구조를 그대로 쓰는 편이 싸다. (대표 결정)
- 2026-08-13 — **장비 슬롯은 weapon / armor / accessory 3종.** 올스탯 장비는 accessory에
  둔다. 이유: 계열 장비 4종(ATK/INT/DEF/LUK)이 각자 자리를 갖고, 최종 목표템인 올스탯
  반지가 그와 경합하지 않고 얹힌다. (구현 판단 — 슬롯 수는 기획서에 없었다. T13-2)
- 2026-08-13 — **인벤토리 전체(장비 + 소비)를 영구 데이터로 둔다.** 장비가 영구인 것은
  확정이고, 소비만 회차 데이터로 갈라놓지 않는다. 이유: 그렇게 하면 "환생하면 물약이
  사라진다"는 규칙을 새로 만드는 셈인데 그건 승인 없이 정할 일이 아니다. 바꾸려면
  `PlayerInventory` 상단 주석부터 고칠 것. (구현 판단. T13-3)
- 2026-08-12 — **장비는 영구 데이터. 환생해도 남는다.** 이유: 기획서 §2 "환생은 리셋이
  아니라 더 깊은 세계를 여는 열쇠"와 방향이 같다. 파생: 환생 비용이 그만큼 가벼워지므로
  2회차 방 난이도를 올릴 여지를 남긴다. (대표 지시. 미구현)
- 2026-08-10 — Maker는 `.map`에서 **스크립트 컴포넌트를 지운 것을 `refresh`로 반영하지 않는다**
  (추가는 반영된다). 실측: 마을 문에서 `script.RoomPortal`을 파일에서 제거했는데 런타임에
  남았고, 프로퍼티는 기본값으로 초기화돼 포탈이 안 보이고 트리거를 이중으로 물었다.
  → `RoomPortal.OnBeginPlay`에 "같은 엔티티에 TownGate가 있으면 물러난다" 가드를 넣었다. (T12-3)
